<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
  <title>ifwant ^_^</title>
  
  
  <link href="https://ifwant.tech/atom.xml" rel="self"/>
  
  <link href="https://ifwant.tech/"/>
  <updated>2021-01-19T14:06:49.611Z</updated>
  <id>https://ifwant.tech/</id>
  
  <author>
    <name>Allen Yi</name>
    
  </author>
  
  <generator uri="https://hexo.io/">Hexo</generator>
  
  <entry>
    <title>synchronized</title>
    <link href="https://ifwant.tech/2021/01/19/gbf_2/"/>
    <id>https://ifwant.tech/2021/01/19/gbf_2/</id>
    <published>2021-01-19T14:06:39.000Z</published>
    <updated>2021-01-19T14:06:49.611Z</updated>
    
    <content type="html"><![CDATA[<h1 id="使用方法"><a href="#使用方法" class="headerlink" title="使用方法"></a>使用方法</h1><p>synchronized关键字最主要的三种使用方式:</p><ul><li>普通方法，只对当前实例对象加锁</li><li>静态方法，对类的所有对象加锁</li><li>方法块<ul><li><code>this</code> ：对当前实例对象加锁</li><li><code>Classname.class</code>：对类的所有对象加锁</li></ul></li></ul>]]></content>
    
    
      
      
    <summary type="html">&lt;h1 id=&quot;使用方法&quot;&gt;&lt;a href=&quot;#使用方法&quot; class=&quot;headerlink&quot; title=&quot;使用方法&quot;&gt;&lt;/a&gt;使用方法&lt;/h1&gt;&lt;p&gt;synchronized关键字最主要的三种使用方式:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;普通方法，只对当前实例对象加锁&lt;/li&gt;
&lt;</summary>
      
    
    
    
    
    <category term="学习" scheme="https://ifwant.tech/tags/%E5%AD%A6%E4%B9%A0/"/>
    
    <category term="JAVA" scheme="https://ifwant.tech/tags/JAVA/"/>
    
    <category term="高并发" scheme="https://ifwant.tech/tags/%E9%AB%98%E5%B9%B6%E5%8F%91/"/>
    
  </entry>
  
  <entry>
    <title>操作系统</title>
    <link href="https://ifwant.tech/2021/01/17/os/"/>
    <id>https://ifwant.tech/2021/01/17/os/</id>
    <published>2021-01-17T08:13:39.000Z</published>
    <updated>2021-01-18T13:13:00.383Z</updated>
    
    <content type="html"><![CDATA[<h1 id="概述"><a href="#概述" class="headerlink" title="概述"></a>概述</h1><h2 id="四大基本特征"><a href="#四大基本特征" class="headerlink" title="四大基本特征"></a>四大基本特征</h2><ul><li>并发：一段时间内多个程序同时运行（并行是一个时刻多个指令同时运行）</li><li>共享：资源共享</li><li>虚拟：虚拟地址，为物理硬件提供逻辑接口来降低使用难度</li><li>异步：进程走走停停，上下文切换</li></ul><h2 id="基本功能"><a href="#基本功能" class="headerlink" title="基本功能"></a>基本功能</h2><ul><li>进程管理</li><li>内存管理</li><li>文件管理</li><li>设备管理</li></ul><h2 id="用户态和内核态"><a href="#用户态和内核态" class="headerlink" title="用户态和内核态"></a>用户态和内核态</h2><h3 id="区别"><a href="#区别" class="headerlink" title="区别"></a>区别</h3><p>操作系统的两种运行级别</p><ul><li>用户态：<ul><li>R3特权级，特权最低</li><li>进程所能访问的内存空间和对象受到限制</li><li>占有的处理器可被抢占</li></ul></li><li>内核态：<ul><li>R0特权级，特权最高；</li><li>能访问所有的内存空间和对象</li><li>占有的处理器不允许被抢占</li></ul></li></ul><h3 id="用户态到内核态的切换（三种中断）"><a href="#用户态到内核态的切换（三种中断）" class="headerlink" title="用户态到内核态的切换（三种中断）"></a>用户态到内核态的切换（三种中断）</h3><p>其实就是三种中断</p><ul><li><p><strong>外中断</strong></p><p>当外围设备完成用户请求的操作后，会向CPU发出相应的中断信号，这时CPU会暂停执行下一条即将要执行的指令转而去执行与中断信号对应的处理程序</p></li><li><p><strong>异常</strong></p><p>当 CPU在执行运行在用户态下的程序时，发生了某些事先不可知的异常，这时会触发由当前运行进程切换到处理此异常的内核相关程序中，也就转到了内核态，比如缺页异常</p></li><li><p><strong>系统调用</strong></p><p>这是用户态进程主动要求切换到内核态的一种方式<strong>，</strong>用户态进程通过系统调用申请使用操作系统提供的服务程序完成工作。比如前例中fork()实际上就是执行了一个创建新进程的系统调用。常见的系统调用还有 <code>read, write, open, create, close, readv, writev, fork, wait, exit, execve, clone</code> 等等</p></li></ul><h2 id="宏内核和微内核"><a href="#宏内核和微内核" class="headerlink" title="宏内核和微内核"></a>宏内核和微内核</h2><p>宏内核是将操作系统功能作为一个紧密结合的整体放到内核。</p><p>微内核是将一部分操作系统功能移出内核，从而降低内核的复杂性。在微内核结构下，操作系统被划分成小的、定义良好的模块，只有微内核这一个模块运行在内核态，其余模块运行在用户态。因为需要频繁地在用户态和内核态之间进行切换，所以会有一定的性能损失。</p><h1 id="进程管理"><a href="#进程管理" class="headerlink" title="进程管理"></a>进程管理</h1><h2 id="进程与线程的区别和联系"><a href="#进程与线程的区别和联系" class="headerlink" title="进程与线程的区别和联系"></a>进程与线程的区别和联系</h2><p>进程：资源分配的基本单位，由<strong>线程+内存+文件/网络句柄</strong>构成</p><p>线程：独立调度的基本单位，由**栈+PC(程序计数器)+TLS(线程本地存储)**组成</p><p>比如QQ和浏览器是两个进程，浏览器内的http请求是一个线程、渲染是一个线程。</p><p><strong>联系：</strong></p><p>进程是线程的容器，一个进程中可以并发多个线程，不同的线程共享进程的资源（堆），同一进程的线程各自维护自己的栈。</p><p><strong>区别：</strong></p><ul><li>资源：操作系统会给每个进程分配资源，但线程不会，只能共享进程的资源</li><li>开销：由于要分配回收资源，创建撤销进程的开销远大于创建撤销线程的开销</li><li>通信：线程通信更简单，直接读写同一进程中的数据；进程通信需要通过IPC（进程间通信）</li></ul><h2 id="进程状态切换"><a href="#进程状态切换" class="headerlink" title="进程状态切换"></a>进程状态切换</h2><p><img src="https://ifwant-pic-1303131123.cos.ap-nanjing.myqcloud.com/photo/01171.png"></p><h2 id="进程调度算法"><a href="#进程调度算法" class="headerlink" title="进程调度算法"></a>进程调度算法</h2><p>不同环境的调度算法目标不同，因此需要针对不同环境来讨论调度算法。</p><h3 id="批处理系统"><a href="#批处理系统" class="headerlink" title="批处理系统"></a>批处理系统</h3><p>没有太多的用户操作，目标是保证吞吐量和周转时间（从提交到终止的时间）</p><h4 id="先来先服务"><a href="#先来先服务" class="headerlink" title="先来先服务"></a>先来先服务</h4><p>first-come first-serverd（FCFS）</p><p>非抢占式的调度算法，按照请求的顺序进行调度</p><ul><li><p>优点：任何进程不会饥饿</p></li><li><p>缺点：不利于短作业。</p><p>短作业必须一直等待前面的长作业执行完毕才能执行，而长作业又需要执行很长时间，造成了短作业等待时间过长。</p></li></ul><h4 id="短作业优先"><a href="#短作业优先" class="headerlink" title="短作业优先"></a>短作业优先</h4><p>shortest job first（SJF）</p><p>非抢占式的调度算法，按估计运行时间最短的顺序进行调度</p><ul><li>优点：对短作业友好</li><li>缺点：长作业可能会饥饿（解决方案，设定阈值，年龄超过阈值使用先来先服务）</li></ul><h4 id="最短剩余时间优先（抢）"><a href="#最短剩余时间优先（抢）" class="headerlink" title="最短剩余时间优先（抢）"></a>最短剩余时间优先（抢）</h4><p>shortest remaining time next（SRTN）</p><p>抢占式调度算法，按剩余运行时间顺序调度。</p><p>当一个新的作业到达时，其整个运行时间与当前进程的剩余时间作比较。如果新的进程需要的时间更少，则挂起当前进程，运行新的进程。否则新的进程等待。</p><h3 id="交互式系统"><a href="#交互式系统" class="headerlink" title="交互式系统"></a>交互式系统</h3><p>有大量的用户交互操作，目标是快速响应</p><h4 id="时间片轮转（抢）"><a href="#时间片轮转（抢）" class="headerlink" title="时间片轮转（抢）"></a>时间片轮转（抢）</h4><p>所有就绪进程按先来先服务原则排成一个队列，给队首一个时间片执行，执行结束放到队尾，再给新的队首一个时间片执行。</p><p>效率取决于<strong>时间片长度</strong>的设置，太短切换浪费过多时间，太长实时性得不到保证</p><h4 id="优先级调度"><a href="#优先级调度" class="headerlink" title="优先级调度"></a>优先级调度</h4><p>为每个进程分配一个优先级，按优先级进行调度。</p><p>为了防止低优先级的进程永远等不到调度，可以随着时间的推移增加等待进程的优先级。</p><h4 id="多级反馈队列（抢）"><a href="#多级反馈队列（抢）" class="headerlink" title="多级反馈队列（抢）"></a>多级反馈队列（抢）</h4><p>一个进程需要执行 100 个时间片，如果采用时间片轮转调度算法，那么需要交换 100 次。</p><p>多级队列是为这种需要连续执行多个时间片的进程考虑，它设置了多个队列，每个队列时间片大小都不同，例如 1,2,4,8,..。进程在第一个队列没执行完，就会被移到下一个队列。这种方式下，之前的进程只需要交换 7 次。</p><p>每个队列优先权也不同，最上面的优先权最高。因此只有上一个队列没有进程在排队，才能调度当前队列上的进程。</p><p>可以将这种调度算法看成是时间片轮转调度算法和优先级调度算法的结合。</p><p><img src="https://ifwant-pic-1303131123.cos.ap-nanjing.myqcloud.com/photo/01173.png"></p><h3 id="实时系统"><a href="#实时系统" class="headerlink" title="实时系统"></a>实时系统</h3><p>实时系统要求一个请求在一个确定时间内得到响应。</p><p>分为硬实时和软实时，前者必须满足绝对的截止时间，后者可以容忍一定的超时。</p><h2 id="进程同步"><a href="#进程同步" class="headerlink" title="进程同步"></a>进程同步</h2><p>同步：多个进程因为合作产生的直接制约关系，使得进程有一定的先后执行关系。</p><h3 id="临界区"><a href="#临界区" class="headerlink" title="临界区"></a>临界区</h3><p>对临界资源进行访问的那段代码称为临界区</p><p>同一时刻内同一段<strong>代码</strong>可能会有多个<strong>进程</strong>在执行，同一时刻只允许一个进程位于临界区内。</p><p>伪代码：</p><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">do</span>&#123;</span><br><span class="line"><span class="comment">//进入区</span></span><br><span class="line"><span class="comment">//临界区</span></span><br><span class="line"><span class="comment">//退出区</span></span><br><span class="line"><span class="comment">//剩余区</span></span><br><span class="line">&#125;<span class="keyword">while</span>(TRUE)</span><br></pre></td></tr></table></figure><p>临界区实现方式：</p><ul><li>非抢占内核：没有竞争</li><li>抢占内核：<ul><li>软件实现：使用两个变量记录是否有进程位于临界区以及哪个进程位于临界区</li><li>硬件实现：锁。进程在进入临界区前检测并申请锁，离开后释放锁。</li></ul></li></ul><h3 id="信号量"><a href="#信号量" class="headerlink" title="信号量"></a>信号量</h3><p>信号量是用来协调进程对共享资源的访问</p><ul><li>优点：可以同步进程</li><li>缺点：信号量有限</li></ul><p>信号量是一个特殊的整型变量，程序对其访问都是<strong>原子操作</strong>，且只允许对它进行等待(<code>P(sv)</code>) 和发送(<code>V(sv)</code>)信息操作。</p><ul><li><strong>P(sv)等待：</strong>如果 <code>sv</code> 的值大于零，就给它减 <code>1</code> ；如果它的值为零，就<strong>挂起</strong>该进程的执行</li><li><strong>V(sv)发送：</strong>如果有其他进程因等待 <code>sv</code> 而被挂起，就让它恢复运行，如果没有进程因等待 <code>sv</code> 而挂起，就给它加 <code>1</code> 。</li></ul><p>举个例子，就是两个进程共享信号量 <code>sv</code> ，一旦其中一个进程执行了 <code>P(sv)</code> 操作，它将得到信号量，并可以进入临界区，使 <code>sv</code> 减1。而第二个进程将被阻止进入临界区，因为当它试图执行 <code>P(sv)</code> 时，<code>sv</code> 为 <code>0</code> ，它会被挂起以等待第一个进程离开临界区域并执行 <code>V(sv)</code> 释放信号量，这时第二个进程就可以恢复执行。</p><h2 id="进程通信"><a href="#进程通信" class="headerlink" title="进程通信"></a>进程通信</h2><p>进程同步与进程通信很容易混淆，它们的区别在于：</p><ul><li>进程同步：控制多个进程按一定顺序执行；</li><li>进程通信：进程间传输信息。</li></ul><p>进程通信是一种手段，而进程同步是一种目的。也可以说，为了能够达到进程同步的目的，需要让进程进行通信，传输一些进程同步所需要的信息。</p><h3 id="管道（无名）"><a href="#管道（无名）" class="headerlink" title="管道（无名）"></a>管道（无名）</h3><p>当一个进程创建了一个管道，并调用 <code>fork</code> 创建自己的一个子进程后，父进程关闭读管道端，子进程关闭写管道端，这样提供了两个进程之间数据流动的一种方式。 </p><p><img src="https://ifwant-pic-1303131123.cos.ap-nanjing.myqcloud.com/photo/01181.png"></p><p>无名管道创建：</p><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">pipe</span><span class="params">(<span class="keyword">int</span> filedis[<span class="number">2</span>])</span></span></span><br></pre></td></tr></table></figure><p>当一个管道建立时，它会创建两个文件描述符(<strong>fd</strong>)：</p><ul><li><code>filedis[0]</code> 用于读管道。</li><li><code>filedis[1]</code> 用于写管道。</li></ul><p>特点：</p><ul><li><strong>半双工</strong>，数据只能单向流动</li><li>只能在父子进程或者兄弟进程中使用</li><li>数据被进程从管道读出后，在管道中该数据就不存在了</li><li>进程去<strong>读取空管道</strong>的时候，进程会阻塞</li><li>进程去<strong>写入满管道</strong>的时候，进程会阻塞</li><li>管道容量为 <strong>64KB</strong>(缓存区有限)</li><li>必须在系统调用<code>fork( )</code>前调用<code>pipe( )</code>，否则子进程将不会继承文件描述符</li></ul><h3 id="FIFO"><a href="#FIFO" class="headerlink" title="FIFO"></a>FIFO</h3><p>有名管道，和无名管道基本相同，去除了管道只能在父子进程中使用的限制，<strong>不相关的进程也能交换数据</strong></p><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">mkfifo</span><span class="params">(<span class="keyword">const</span> <span class="keyword">char</span> * pathname, mode_tmode)</span></span></span><br></pre></td></tr></table></figure><p>一旦创建了一个FIFO，就可用open打开它，一般的文件访问函数（close、read、write等）都可用于FIFO。FIFO 常用于客户-服务器应用程序中，FIFO 用作汇聚点，在客户进程和服务器进程之间传递数据</p><img src="https://ifwant-pic-1303131123.cos.ap-nanjing.myqcloud.com/photo/01182.png" style="zoom: 67%;" /><ul><li>优点：可以实现任意关系的进程间的通信</li><li>缺点：缓冲区有限</li></ul><h3 id="消息队列"><a href="#消息队列" class="headerlink" title="消息队列"></a>消息队列</h3><p>消息队列是消息的链表，存放在内核中并由消息队列标识符标识</p><p>建立一个队列，先放入队列的消息被最先取出。和管道不同的是，消息队列<strong>允许多个进程放入消息，也允许多个进程取出消息</strong>。每个消息可以带有一个整数识别符( <code>message_type</code> )。你可以通过识别符对消息分类 (极端的情况是将每个消息设置一个不同的识别符)。某个进程从队列中取出消息的时候，可以按照<strong>先进先出</strong>的顺序取出，也可以<strong>只取出符合某个识别符的消息</strong>(有多个这样的消息时，同样按照先进先出的顺序取出)。</p><p>克服了<strong>信号传递信息少</strong>、<strong>管道只能承载无格式字节流以及缓冲区大小受限</strong>等缺点</p><h3 id="信号量-1"><a href="#信号量-1" class="headerlink" title="信号量"></a>信号量</h3><p>它是一个计数器，用于为多个进程提供对共享数据对象的访问。</p><h3 id="共享内存"><a href="#共享内存" class="headerlink" title="共享内存"></a>共享内存</h3><p>共享内存就是<strong>映射一段能被其他进程所访问的内存</strong>，这段共享内存<strong>由一个进程创建，但多个进程都可以访问</strong>。</p><p>共享内存是⭐<strong>最快</strong>⭐的 <code>IPC(interprocess communication)</code> 方式，它是<strong>针对其他进程间通信方式运行效率低而专门设计</strong>的。它往往与其他通信机制，如<strong>信号量配合使用</strong>，来实现进程间的同步和通信。</p><ul><li><p>优点：无须复制，快捷，信息量大</p></li><li><p>缺点</p><ul><li><p>通信是通过将共享空间缓冲区直接附加到进程的虚拟地址空间中来实现的，因此进程间的读写操作的同步问题(映射到<strong>文件映射段</strong>)</p></li><li><p>利用内存缓冲区直接交换信息，内存的实体存在于计算机中，只能同一个计算机系统中的诸多进程共享，不方便网络通信</p></li></ul></li></ul><h3 id="信号"><a href="#信号" class="headerlink" title="信号"></a>信号</h3><p>通知接收进程某个事件已经发生</p><h3 id="套接字"><a href="#套接字" class="headerlink" title="套接字"></a>套接字</h3><p>可用于不同机器间的进程通信</p><ul><li>优点：<ol><li>传输数据为<strong>字节级</strong>，传输数据<strong>可自定义</strong>，数据量小效率高</li><li>传输数据<strong>时间短</strong>，<strong>性能高</strong></li><li>适合于客户端和服务器端之间信息<strong>实时交互</strong></li><li>可以<strong>加密</strong>，<strong>数据安全性强</strong></li></ol></li><li>缺点：需对传输的数据进行解析，转化成应用级的数据。</li></ul><h2 id="多线程间通信和同步"><a href="#多线程间通信和同步" class="headerlink" title="多线程间通信和同步"></a>多线程间通信和同步</h2><ul><li><p>锁机制：</p><ul><li>互斥锁/量（<code>mutex</code>）：提供了以排他方式防止数据结构被并发修改的方法。</li><li>读写锁（<code>reader-writer lock</code>）：允许多个线程同时读共享数据，而对写操作是互斥的。</li><li>自旋锁（<code>spin lock</code>）与互斥锁类似，都是为了保护共享资源。互斥锁是当资源被占用，申请者进入睡眠状态；而自旋锁则循环检测保持者是否已经释放锁。</li><li>条件变量（<code>condition</code> ）：可以以原子的方式阻塞进程，直到某个特定条件为真为止。对条件的测试是在互斥锁的保护下进行的。<strong>条件变量始终与互斥锁一起使用</strong>。</li></ul></li><li><p>信号量机制(<code>Semaphore</code>)</p><ul><li>无名线程信号量</li></ul></li><li><p>命名线程信号量</p></li><li><p>信号机制( <code>Signal</code> )：类似进程间的信号处理</p></li><li><p>屏障（ <code>barrier</code> ）：屏障允许每个线程等待，直到所有的合作线程都达到某一点，然后从该点继续执行。</p></li></ul><p>线程间的通信目的主要是用于<strong>线程同步</strong>，所以线程没有像进程通信中的用于数据交换的通信机制。</p><h2 id="协程"><a href="#协程" class="headerlink" title="协程"></a>协程</h2><p>是一种比线程更加轻量级的存在，主要就是为了减少线程切换的资源消耗。</p><p><strong>一个线程里可以有多个协程</strong>，它不是被操作系统内核所管理的，而是完全由程序所控制，也就是在<strong>用户态</strong>执行。这样带来的好处是性能大幅度的提升，因为不会像线程切换那样消耗资源。</p><p>一个线程的多个协程虽然可以切换，但运行是<strong>串行</strong>的。当一个协程运行时，其它协程必须挂起。</p><p>同一个线程中通过保存代码执行段状态，进行代码段的分次执行，以及多个代码段的交织执行。</p><h2 id="上下文切换"><a href="#上下文切换" class="headerlink" title="上下文切换"></a>上下文切换</h2><ul><li><strong>进程</strong>的切换者是操作系统，切换时机是根据操作系统自己的切换策略，用户是无感知的。进程的切换内容包括页全局目录、内核栈、硬件上下文，切换内容保存在内存中。进程切换过程是由“用户态到内核态到用户态”的方式，切换效率低。</li><li><strong>线程</strong>的切换者是操作系统，切换时机是根据操作系统自己的切换策略，用户无感知。线程的切换内容包括内核栈和硬件上下文。线程切换内容保存在内核栈中。线程切换过程是由“用户态到内核态到用户态”， 切换效率中等。</li><li><strong>协程</strong>的切换者是用户（编程者或应用程序），切换时机是用户自己的程序所决定的。协程的切换内容是硬件上下文，切换内存保存在用户自己的变量（用户栈或堆）中。协程的切换过程只有用户态，即没有陷入内核态，因此切换效率高.</li></ul><h1 id="死锁"><a href="#死锁" class="headerlink" title="死锁"></a>死锁</h1><p>一组互相竞争资源的线程因互相等待，导致<strong>永久阻塞</strong>的现象。说白了就是：两个线程互相持有对方所需的资源，互不释放且互相等待</p><h2 id="必要条件"><a href="#必要条件" class="headerlink" title="必要条件"></a>必要条件</h2><p>产生死锁必须同时满足以下四个条件</p><ul><li><strong>互斥</strong>：进程申请的资源在一段时间内只能被一个进程使用</li><li><strong>占有和等待</strong>：已经得到了某个资源的进程可以再请求新的资源，但对自己已获得的资源保持不放。</li><li><strong>不可抢占</strong>：已经分配给一个进程的资源不能强制性地被抢占，它只能被占有它的进程显式地释放。</li><li><strong>环路等待</strong>：有两个或者两个以上的进程组成一条环路，该环路中的每个进程都在等待下一个进程所占有的资源。</li></ul><h2 id="处理方法"><a href="#处理方法" class="headerlink" title="处理方法"></a>处理方法</h2><p>主要有以下四种方法：</p><ul><li>鸵鸟策略</li><li>死锁检测与死锁恢复</li><li>死锁预防</li><li>死锁避免</li></ul><h3 id="鸵鸟策略"><a href="#鸵鸟策略" class="headerlink" title="鸵鸟策略"></a>鸵鸟策略</h3><p>把头埋在沙子里，假装根本没发生问题。</p><p>因为解决死锁问题的代价很高，因此鸵鸟策略这种不采取任务措施的方案会获得更高的性能。</p><p>当发生死锁时不会对用户造成多大影响，或发生死锁的概率很低，可以采用鸵鸟策略。</p><p>大多数操作系统，包括 Unix，Linux 和 Windows，处理死锁问题的办法仅仅是忽略它。</p><h3 id="死锁检测与死锁恢复"><a href="#死锁检测与死锁恢复" class="headerlink" title="死锁检测与死锁恢复"></a>死锁检测与死锁恢复</h3><p>不试图阻止死锁，而是当检测到死锁发生时，采取措施进行恢复。</p><p>检测：有向图有环</p><p>恢复：</p><ul><li>利用抢占恢复</li><li>利用回滚恢复</li><li>通过杀死进程恢复</li></ul><h3 id="死锁预防"><a href="#死锁预防" class="headerlink" title="死锁预防"></a>死锁预防</h3><p>在程序运行之前预防发生死锁。</p><p>破坏四个必要条件</p><ul><li><p>破坏互斥条件</p><p>例如假脱机打印机技术允许若干个进程同时输出，唯一真正请求物理打印机的进程是打印机守护进程。</p></li><li><p>破坏占有和等待条件</p><p>一种实现方式是规定所有进程在开始执行前请求所需要的全部资源</p></li><li><p>破坏不可抢占条件</p></li><li><p>破坏环路等待</p><p>给资源统一编号，进程只能按编号顺序来请求资源。</p></li></ul><h3 id="死锁避免"><a href="#死锁避免" class="headerlink" title="死锁避免"></a>死锁避免</h3><p>在程序运行时避免发生死锁</p><p><strong>银行家算法</strong></p><p>假设释放一个进程，然后把这个进程已分配的资源回收到剩余资源中，然后再找下一个能在剩余可用资源中被释放的进程。以此类推，如果都能被释放，系统处于安全状态，不会死锁。</p><p><img src="https://ifwant-pic-1303131123.cos.ap-nanjing.myqcloud.com/photo/01183.png" alt="img"></p><p>上图中有五个进程，四个资源。左边的图表示已经分配的资源，右边的图表示还需要分配的资源。最右边的 E、P 以及 A 分别表示：总资源、已分配资源以及可用资源，注意这三个为向量，而不是具体数值，例如 A=(1020)，表示 4 个资源分别还剩下 1/0/2/0。</p><p>检查一个状态是否安全的算法如下：</p><ul><li>查找右边的矩阵是否存在一行小于等于向量 A。如果不存在这样的行，那么系统将会发生死锁，状态是不安全的。</li><li>假若找到这样一行，将该进程标记为终止，并将其已分配资源加到 A 中。</li><li>重复以上两步，直到所有进程都标记为终止，则状态时安全的。</li></ul><p>如果一个状态不是安全的，需要拒绝进入这个状态。</p><h2 id="活锁"><a href="#活锁" class="headerlink" title="活锁"></a>活锁</h2><p>是指线程1可以使用资源，但它很礼貌，让其他线程先使用资源，线程2也可以使用资源，但它很绅士，也让其他线程先使用资源。这样你让我，我让你，最后两个线程都无法使用资源。</p><p>解决办法：线程谦让时，尝试等待一个随机的时间就可以了</p><h2 id="饥饿"><a href="#饥饿" class="headerlink" title="饥饿"></a>饥饿</h2><p>低优先级的线程资源一直被高优先级抢占</p>]]></content>
    
    
      
      
    <summary type="html">&lt;h1 id=&quot;概述&quot;&gt;&lt;a href=&quot;#概述&quot; class=&quot;headerlink&quot; title=&quot;概述&quot;&gt;&lt;/a&gt;概述&lt;/h1&gt;&lt;h2 id=&quot;四大基本特征&quot;&gt;&lt;a href=&quot;#四大基本特征&quot; class=&quot;headerlink&quot; title=&quot;四大基本特征&quot;&gt;&lt;/a&gt;四</summary>
      
    
    
    
    
    <category term="学习" scheme="https://ifwant.tech/tags/%E5%AD%A6%E4%B9%A0/"/>
    
    <category term="操作系统" scheme="https://ifwant.tech/tags/%E6%93%8D%E4%BD%9C%E7%B3%BB%E7%BB%9F/"/>
    
  </entry>
  
  <entry>
    <title>数据库面试点</title>
    <link href="https://ifwant.tech/2021/01/16/sql2/"/>
    <id>https://ifwant.tech/2021/01/16/sql2/</id>
    <published>2021-01-16T06:34:39.000Z</published>
    <updated>2021-01-16T17:01:04.000Z</updated>
    
    <content type="html"><![CDATA[<h1 id="索引"><a href="#索引" class="headerlink" title="索引"></a>索引</h1><h2 id="各种索引概念的区别"><a href="#各种索引概念的区别" class="headerlink" title="各种索引概念的区别"></a>各种索引概念的区别</h2><h3 id="聚簇索引和非聚簇索引"><a href="#聚簇索引和非聚簇索引" class="headerlink" title="聚簇索引和非聚簇索引"></a>聚簇索引和非聚簇索引</h3><p>聚簇索引和非聚簇索引是从物理存储方面进行划分的。 根本区别是：<strong>表记录</strong>的排列顺序和与<strong>索引</strong>的排列顺序是否一致。</p><p>聚集索引表记录的排列顺序与索引的排列顺序一致，索引中包含数据。<strong>优点</strong>是查询速度快，因为一旦具有第一个索引值的纪录被找到，具有连续索引值的记录也一定物理的紧跟其后。<strong>缺点</strong>是对表进行修改速度较慢，这是为了保持表中的记录的物理顺序与索引的顺序一致，而把记录插入到数据页的相应位置，必须在数据页中进行数据重排， 降低了执行速度。</p><p>非聚集索引指定了表中记录的逻辑顺序，但记录的物理顺序和索引的顺序不一致。聚集索引和非聚集索引都采用了B+树的结构，但非聚集索引的叶子层并不与实际的存储空间直接挂钩。</p><h3 id="主键索引和辅助键索引"><a href="#主键索引和辅助键索引" class="headerlink" title="主键索引和辅助键索引"></a>主键索引和辅助键索引</h3><p>主键索引对主键构建的索引，是在我们创建表激活后由系统自动创建的。辅助键索引是对非主键的列构建的索引，由我们自己创建。</p><h3 id="一级索引和二级索引"><a href="#一级索引和二级索引" class="headerlink" title="一级索引和二级索引"></a>一级索引和二级索引</h3><p>一级索引其实就是聚簇索引（聚簇索引必定是由主键构成的），可以直接拿到值。因为其他索引都得重新走一遍聚簇索引才能拿到值。</p><p>二级索引是和一级索引相比较的概念，是辅助键索引有时候的称呼（存在聚簇索引就可以称呼辅助键索引为二级索引），称呼为二级索引是因为在走完辅助键索引之后，还得再走一遍聚簇索引才能取到值，所以辅助键索引地位较低。</p><h3 id="单列索引和联合索引"><a href="#单列索引和联合索引" class="headerlink" title="单列索引和联合索引"></a>单列索引和联合索引</h3><p>单列索引就是对单独一个列建立的索引。联合索引是对多个字段同时建立的索引。</p><h3 id="一般-normal-索引和唯一性-unique-索引"><a href="#一般-normal-索引和唯一性-unique-索引" class="headerlink" title="一般(normal)索引和唯一性(unique)索引"></a>一般(normal)索引和唯一性(unique)索引</h3><p>一般索引就是普通索引，这没什么好讲的，而唯一性索引则是在普通索引的基础上加上了索引列的唯一性的约束（允许有空值），如果是联合索引，那么索引列的组合必须唯一。比如说，一般索引举个例子，人名，国家允许多个相同人名的人出现；唯一性索引，举个例子，身份证，不允许重复，或者说你们班级里有两个同名的人，一男一女，那么（名字 + 性别）也能作为唯一性索引，反正就是必须唯一。</p><p><strong>B+ 树索引、哈希（hash）索引、全文（full-text）索引、空间数据（R-tree）索引</strong> 这是从索引的逻辑结构进行划分的。</p><ul><li><p>B+ 树索引：将列进行树化，变成一颗 B+ 树。InnoDB 中的聚簇索引就是 B+ 树。 主索引的叶子节点 data 域记录着完整的数据记录，这种索引方式被称为聚簇索引。因为无法把数据行存放在两个不同的地方，所以一个表只能有一个聚簇索引。辅助索引的叶子节点的 data 域记录着主键的值，因此在使用辅助索引进行查找时，需要先查找到主键值，然后再到主索引中进行查找。</p></li><li><p>哈希索引（hash index）：基于哈希表实现，只有精确匹配索引所有列的查询才有效。对于每一行数据，存储引擎都会对所有的索引列计算一个哈希码（hash code），不同键值的行计算出来的哈希码也不一样。哈希索引将所有的哈希码存储在索引中，并在哈希表中保存指向每个数据行的指针。</p><ul><li>查找速度快，时间复杂度O(1)</li><li>无法用于排序与分组</li><li>只支持精确查找，无法用于部分查找和范围查找。 InnoDB 存储引擎有一个特殊的功能叫“自适应哈希索引”，当某个索引值被使用的非常频繁时，会在 B+Tree 索引之上再创建一个哈希索引，这样就让 B+Tree 索引具有哈希索引的一些优点，比如快速的哈希查找。</li></ul><p><img src="https://ifwant-pic-1303131123.cos.ap-nanjing.myqcloud.com/photo/01162.png"></p></li><li><p>全文索引：用来查找文本中的关键字，而不是直接与索引中的值相比较。fulltext索引跟其它索引大不相同，它更像是一个搜索引擎，而不是简单的where语句的参数匹配。fulltext索引配合match against操作使用，而不是一般的where语句加like。它可以在create table，alter table ，create index使用，不过目前只有char、varchar，text 列上可以创建全文索引。值得一提的是，在数据量较大时候，先将数据放入一个没有全局索引的表中，然后再用CREATE index创建fulltext索引，要比先为一张表建立fulltext然后再将数据写入的速度快很多。</p></li><li><p>R-tree 索引：MyISAM表支持空间索引，可以用作地理数据存储。和B-Tree索引不同，这类索引无须前缀索引。空间索引会从所有维度来索引数据。查询时，可以有效地使用任意维度来组合查询。必须使用MySQL的GIS相关函数如MBRCONTAINS()等来维护数据。MySQL的GIS支持并不完善，所以大部分人都不会使用这个特性。</p></li></ul><h2 id="聚簇索引和非聚簇索引用在什么地方？"><a href="#聚簇索引和非聚簇索引用在什么地方？" class="headerlink" title="聚簇索引和非聚簇索引用在什么地方？"></a>聚簇索引和非聚簇索引用在什么地方？</h2><p>一般我们提到聚簇索引和非聚簇索引，都是在 InnoDB 和 MyISAM 的比较中。 InnoDB 使用聚簇索引构建了主键的 B+ 树，其他辅助索引叶子节点找到的都是主键的值，然后通过主键去寻找聚簇索引的叶子节点，拿到具体的行数据。而 MyISAM 则都是非聚簇索引，即使是二级索引叶子节点存储的也是地址，跟利用主键构造的索引是一样的。</p><p>如下图所示，可以看到在 InnoDB 中，所有辅助键索引在查找完成之后得到的都是主键的值，还需要再次通过主键索引去查找具体的行数据（即覆盖索引，一个查询语句的执行只用从索引中就能够取得，不必从数据表中读取，减少了 IO 操作）；而在 MyISAM 中，辅助键索引能直接找到具体的行数据的地址，这就避免了再一次的主键索引，因此光是从这方面来说，MyISAM 的查询效率应该是比 InnoDB 要高的，但是加上 IO 操作的时间就不好说了。</p><p><img src="https://ifwant-pic-1303131123.cos.ap-nanjing.myqcloud.com/photo/0116.png"></p><h2 id="联合索引"><a href="#联合索引" class="headerlink" title="联合索引"></a>联合索引</h2><p>对于两个或两个以上列的索引称为联合索引。</p><p>索引的底层是一颗 <code>B+</code> 树，那么联合索引当然还是一棵 <code>B+</code> 树，只不过联合索引的健值数量不是一个，而是多个。构建一颗 <code>B+</code> 树只能根据一个值来构建，因此数据库依据联合索引最左的字段来构建B+树。<br>例子：假如创建一个(a,b)的联合索引，那么它的索引树是这样的：</p><img src="https://ifwant-pic-1303131123.cos.ap-nanjing.myqcloud.com/photo/1281680-20190117145740508-758737271.png" style="zoom: 67%;" /><p>可以看到a的值是有顺序的：1，1，2，2，3，3，而b的值是没有顺序的1，2，1，4，1，2。所以 <code>b = 2</code> 这种查询条件没有办法利用索引，因为联合索引首先是按a排序的，b是无序的。</p><p>同时我们还可以发现在a值相等的情况下，b值又是按顺序排列的，但是这种顺序是相对的。所以<strong>最左匹配原则遇上范围查询就会停止，剩下的字段都无法使用索引</strong>。例如 <code>a = 1</code> and <code>b = 2</code> a,b字段都可以使用索引，因为在a值确定的情况下b是相对有序的，而 <code>a&gt;1</code> and <code>b=2</code> ，a字段可以匹配上索引，但b值不可以，因为a的值是一个范围，在这个范围中b是无序的。</p><h3 id="最左匹配原则"><a href="#最左匹配原则" class="headerlink" title="最左匹配原则"></a>最左匹配原则</h3><p>最左优先，以最左边的为起点任何连续的索引都能匹配上。同时遇到范围查询(&gt;、&lt;、between、like)就会停止匹配。</p><p>比如a = 1 and b = 2 and c &gt; 3 and d = 4 如果建立(a,b,c,d)顺序的索引，d是用不到索引的，因为c字段是一个范围查询，它之后的字段会停止匹配。</p><h2 id="如何优化索引"><a href="#如何优化索引" class="headerlink" title="如何优化索引"></a>如何优化索引</h2><ul><li>尽量选择惟一性索引；</li><li>为经常需要排序、分组和联合操作的字段建立索引；</li><li>为常作为查询条件的字段建立索引；</li><li>限制索引的数目，索引越多，更新和插入的效率越低；</li><li>尽量使用数据量少的索引；</li><li>组合索引中的尽量把能过滤掉更多数据的字段放在前面，把最经常使用的字段放在前面（最左原则）；</li><li>尽量使用前缀来索引，针对长文本，TEXT等类型；</li><li>删除不再使用或者很少使用的索引；</li><li>数据是动态变化中的，索引的使用也需要根据数据的变化而变化。</li></ul><h2 id="B-树"><a href="#B-树" class="headerlink" title="B+树"></a>B+树</h2><ul><li>非叶子节点不存储 <code>data</code> ，只存储索引(冗余)，可以放更多的索引</li><li>叶子节点包含所有的索引字段</li><li>叶子节点用指针链接，提高区间访问的性能。</li></ul><p>同为 <code>h</code> 层的 <code>B</code> 树和 <code>B+</code> 树，<code>B+</code> 树可以存储更多的结点元素（因为B+树非叶节点不存储数据），更加 ”矮胖“。这也是 <code>B+</code> 树最大的优势所在，极大地改善了 <code>B</code> 树的查找效率。对于同样多的记录，<code>B+</code> 树的高度会更矮，并且 $ P_{next} $ 指针的出现可以帮助 <code>B+</code> 树快速访问磁盘记录且效率非常高。总之，就是 <code>B+</code> 树比 <code>B</code> 树更加好，<code>B+</code> 树的磁盘 <code>I/O</code> 会更少，<strong>相比于 <code>B</code> 树的中序遍历，<code>B+</code> 树只需要像遍历单链表一样扫描一遍叶子结点。</strong></p><h3 id="单个元素查询"><a href="#单个元素查询" class="headerlink" title="单个元素查询"></a>单个元素查询</h3><p><strong><code>B+</code> 树中查找任何一个元素都要从根结点一直走到叶子结点 。</strong>所以<code>B+</code>树的查询很稳定，每一个元素都有相同的磁盘 <code>I/O</code> 操作次数（即树的高度）。</p><p><code>B+</code> 树的非叶子结点均不存储 <code>Data</code> (即$D_i$，官方将其称为卫星数据) ，所以与 <code>B</code> 树相比，同样大小的磁盘页， <code>B+</code> 树的非叶子结点可以存储更多的索引（关键字），这也就意味着在数据量相同的情况下，<code>B+</code> 树的结构比 <code>B</code> 树更加 “矮胖”，查询时磁盘 <code>I/O</code> 次数会更少。</p><p><img src="https://ifwant-pic-1303131123.cos.ap-nanjing.myqcloud.com/photo/01164.jpg" alt="图片"></p><h3 id="插入操作"><a href="#插入操作" class="headerlink" title="插入操作"></a>插入操作</h3><p>在 <code>B+</code> 树中插入关键字时，需要注意以下几点：</p><ul><li><strong>插入的操作全部都在叶子结点上进行</strong>，且不能破坏关键字自小而大的顺序；</li><li>由于 <code>B+</code> 树中<strong>各结点中存储的关键字的个数有明确的范围</strong>，做插入操作可能会出现结点中关键字个数超过阶数的情况，此时需要将该结点进行 “分裂”；</li></ul><img src="https://ifwant-pic-1303131123.cos.ap-nanjing.myqcloud.com/photo/01165.jpg" alt="图片" style="zoom:80%;" /><p>我们依旧以之前介绍查找操作时使用的图对插入操作进行说明，需要注意的是，<code>B+</code> 树的阶数 <code>M = 3</code> ，且 <code>⌈M/2⌉ = 2（取上限）</code> 、<code>⌊M/2⌋ = 1（取下限）</code> ：</p><p><strong><code>B+</code> 树中做插入关键字的操作，有以下 4 种情况：</strong></p><ol><li><p><strong>若被插入关键字所在的结点，其含有关键字数目小于阶数 <code>M</code> ，则直接插入；</strong></p><p>比如插入关键字 <code>12</code> ，插入关键字所在的结点的 <code>[10，15]</code> 包含两个关键字，小于 <code>M</code> ，则直接插入关键字 <code>12</code> 。</p><p><img src="https://ifwant-pic-1303131123.cos.ap-nanjing.myqcloud.com/photo/01166.gif" alt="图片"></p></li><li><p><strong>若被插入关键字所在的结点，其含有关键字数目等于阶数 <code>M</code> ，则需要将该结点分裂为两个结点.</strong></p><p>一个结点包含 <code>⌊M/2⌋(1)</code> ，另一个结点包含 <code>⌈M/2⌉(2)</code> 。同时，<strong>将<code>⌈M/2⌉</code>的关键字上移至其双亲结点</strong>。假设其双亲结点中包含的关键字个数小于 M，则插入操作完成。</p><p>比如插入关键字 <code>95</code> ，插入关键字所在结点 <code>[85、91、97]</code> 包含 3 个关键字，等于阶数 <code>M</code> ，则将 <code>[85、91、97]</code> 分裂为两个结点 <code>[85、91]</code> 和结点 <code>[97]</code> , 关键字 <code>95</code> 插入到结点 <code>[95、97]</code> 中，并将关键字 <code>91</code> 上移至其双亲结点中，发现其双亲结点 <code>[72、97]</code> 中包含的关键字的个数 2 小于阶数 <code>M</code> ，插入操作完成。</p><p><img src="https://ifwant-pic-1303131123.cos.ap-nanjing.myqcloud.com/photo/01167.gif" alt="图片"></p></li><li><p><strong>在情况 2 中，如果上移操作导致其双亲结点中关键字个数大于 M，则应继续分裂其双亲结点。</strong></p><p>插入关键字 <code>40</code> ，按照第 2 种情况将结点分裂，并将关键字 <code>37</code> 上移到父结点，发现父结点 <code>[15、37、44、59]</code> 包含的关键字的个数大于 <code>M</code> ，所以将结点 <code>[15、37、44、59]</code> 分裂为两个结点 <code>[15、37]</code> 和结点 <code>[44、59]</code> ，并将关键字 <code>37</code> 上移到父结点中 <code>[37、59、97]</code> . 父结点包含关键字个数没有超过 <code>M</code> ，插入结束。</p><p><img src="https://ifwant-pic-1303131123.cos.ap-nanjing.myqcloud.com/photo/01168.gif" alt="图片"></p></li><li><p><strong>若插入的关键字比当前结点中的最大值还大，破坏了B+树中从根结点到当前结点的所有索引值，此时需要及时修正后，再做其他操作。</strong></p><p>插入关键字 <code>100</code>，由于其值比最大值 <code>97</code> 还大，插入之后，从根结点到该结点经过的所有结点中的所有值都要由 <code>97</code> 改为 <code>100</code>。改完之后再做分裂操作。</p><p><img src="https://ifwant-pic-1303131123.cos.ap-nanjing.myqcloud.com/photo/01169.gif" alt="图片"></p></li></ol><h3 id="删除"><a href="#删除" class="headerlink" title="删除"></a>删除</h3><p>在 <code>B+</code> 树中做删除关键字的操作，采取如下的步骤：</p><ol><li>删除该关键字，如果不破坏 <code>B+</code> 树本身的性质，直接完成删除操作（情况 1）；</li><li>如果删除操作导致其该结点中最大（或最小）值改变，则应相应改动其父结点中的索引值（情况 2）；</li><li>在删除关键字后，如果导致其结点中关键字个数不足，有两种方法：一种是向兄弟结点去借，另外一种是同兄弟结点合并（情况 3、4 和 5）。（注意这两种方式有时需要更改其父结点中的索引值。）</li></ol><p>详细见 <a href="https://mp.weixin.qq.com/s?__biz=MzI1MzYzMTI2Ng==&mid=2247485029&idx=1&sn=8ca8c6e4f0db55cb61d297881773e51f&chksm=e9d0c828dea7413e0f64d04337758c9b4713d6f51e33efc2d560561f06e8748f02497da71b06&scene=126&sessionid=1609566026#rd">B+树详解</a></p><h3 id="存储能力"><a href="#存储能力" class="headerlink" title="存储能力"></a>存储能力</h3><p>对于 <code>B+</code> 树而言，树的高度一般不超过 <code>4</code> 层，就 <code>MySQL</code> 的 <code>InnoDB</code> 存储引擎而言，一个结点默认的存储空间为 <strong>16Kb</strong> ( 可以通过这个命令查看 <code>SHOW GLOBAL STATUS like &#39;Innodb_page_size&#39;;</code> )， <code>MySQL</code> 的 <code>InnoDB</code> 存储引擎的索引一般用 <code>bigint</code> 存储，占用 <strong>8个byte</strong>，一个索引又会关联一个指向孩子结点的指针，这个指针占用 <strong>6个byte</strong>，也就是说结点中的一个关键字大概要用 <strong>14 byte</strong> 的空间，而一个结点的默认大小为 <strong>16kb</strong> ，那么一个结点可以存储关键的个数最多为 <strong>16384/14=1170</strong> , 就相当于阶数 ，那么对于一颗高度为 <code>3</code> 的 <code>B+</code>树而言保守估计可以存储 <strong>1170*1170*16=21902400</strong> 个关键字，也就是两千多万条记录，其中的 <code>16</code> 为假定每一个叶子结点包含的关键字的个数（由于包含 Data 指针，所以叶子结点可以容纳的关键字的个数会少一些），3层的 <code>B+</code> 树就可以存储两千多万的数据。</p><hr><h1 id="锁"><a href="#锁" class="headerlink" title="锁"></a>锁</h1><h2 id="什么是锁？MySQL-中提供了几类锁？"><a href="#什么是锁？MySQL-中提供了几类锁？" class="headerlink" title="什么是锁？MySQL 中提供了几类锁？"></a>什么是锁？MySQL 中提供了几类锁？</h2><p>锁是多用户竞争的一种临界资源，是实现数据库并发控制的重要手段，可以保证数据库在多人同时操作时能够正常运行。MySQL 提供了全局锁、行级锁、表级锁。其中 InnoDB 支持表级锁和行级锁，MyISAM 只支持表级锁。</p><h2 id="锁的划分"><a href="#锁的划分" class="headerlink" title="锁的划分"></a>锁的划分</h2><ul><li>按照锁的粒度划分：行锁、表锁、页级索、全局锁</li><li>按照锁的使用方式划分：共享锁、互斥锁（悲观锁的一种实现）</li><li>还有两种思想上的锁：悲观锁、乐观锁</li><li>在读写锁的基础上引入的意向锁，实现锁的细粒度化</li><li>InnoDB中有几种行级锁类型：Record Lock、Gap Lock、Next-key Lock<ul><li>Record Lock：在索引记录上加锁</li><li>Gap Lock：间隙锁</li><li>Next-key Lock：Record Lock+Gap Lock</li></ul></li></ul><p>(1) 行锁、表锁、全局锁：行锁是对行数据进行加锁，表锁是对整张表进行加锁，页级锁是MySQL中锁定粒度介于行级锁和表级锁中间的一种锁，一次锁定相邻的一组记录，而全局锁是对整个数据库进行加锁，典型的使用场景就是全库逻辑备份。</p><p>(2) 共享锁、互斥锁:互斥锁（Exclusive），简写为 X 锁，又称写锁。共享锁（Shared），简写为 S 锁，又称读锁。</p><ul><li>一个事务对数据对象 A 加了 X 锁，就可以对 A 进行读取和更新。加锁期间其它事务不能对 A 加任何锁。</li><li>一个事务对数据对象 A 加了 S 锁，可以对 A 进行读取操作，但是不能进行更新操作。加锁期间其它事务能对 A 加 S 锁，但是不能加 X 锁。</li></ul><p>(3) 悲观锁、乐观锁</p><table><thead><tr><th>锁</th><th>概述</th><th>使用场景</th><th>样例</th></tr></thead><tbody><tr><td><strong>悲观锁</strong></td><td>悲观锁对数据被外界修改持保守态度（悲观），因此在整个数据处理过程中，将数据出于锁定状态，而别的任务处于被阻塞的状态；</td><td>写多读少，保证写操作时的数据安全</td><td>1、JVM中的synchronized和Lock；2、分布式环境基于数据库行锁、页锁、表锁、共享锁（读锁）、排它锁（写锁）；3、基于zookeeper、Redis 的分布式锁</td></tr><tr><td><strong>乐观锁</strong></td><td>乐观锁认为数据一般情况下不会造成冲突，所以在数据进行提交更新的时候，才会正式对数据的冲突与否进行检测，如果发现冲突了，程序自动去重试（实现通常用“版本号”）</td><td>读多写少，提高系统吞吐</td><td>1、JDK并发包中的原子类；2、数据库乐观锁、缓存乐观锁</td></tr></tbody></table><p>(4) 意向锁 使用意向锁（Intention Locks）可以更容易地支持多粒度封锁。 在存在行级锁和表级锁的情况下，事务 T 想要对表 A 加 X 锁，就需要先检测是否有其它事务对表 A 或者表 A 中的任意一行加了锁，那么就需要对表 A 的每一行都检测一次，这是非常耗时的。 意向锁在原来的 X/S 锁之上引入了 IX/IS，IX/IS 都是表锁，用来表示一个事务想要在表中的某个数据行上加 X 锁或 S 锁。有以下两个规定：</p><ul><li>一个事务在获得某个数据行对象的 S 锁之前，必须先获得表的 IS 锁或者更强的锁；</li><li>一个事务在获得某个数据行对象的 X 锁之前，必须先获得表的 IX 锁。</li></ul><p>通过引入意向锁，事务 T 想要对表 A 加 X 锁，只需要先检测是否有其它事务对表 A 加了 X/IX/S/IS 锁，如果加了就表示有其它事务正在使用这个表或者表中某一行的锁，因此事务 T 加 X 锁失败。 下图是意向锁之间的兼容关系</p><table><thead><tr><th></th><th>X</th><th>IX</th><th>S</th><th>IS</th></tr></thead><tbody><tr><td>X</td><td>/</td><td>/</td><td>/</td><td>/</td></tr><tr><td>IX</td><td>/</td><td></td><td>/</td><td></td></tr><tr><td>S</td><td>/</td><td>/</td><td></td><td></td></tr><tr><td>IS</td><td>/</td><td></td><td></td><td></td></tr></tbody></table><p>解释如下：</p><ul><li>任意 IS/IX 锁之间都是兼容的，因为它们只表示想要对表加锁，而不是真正加锁；</li><li>这里兼容关系针对的是表级锁，而表级的 IX 锁和行级的 X 锁兼容，两个事务可以对两个数据行加 X 锁。（事务 T1 想要对数据行 R1 加 X 锁，事务 T2 想要对同一个表的数据行 R2 加 X 锁，两个事务都需要对该表加 IX 锁，但是 IX 锁是兼容的，并且 IX 锁与行级的 X 锁也是兼容的，因此两个事务都能加锁成功，对同一个表中的两个数据行做修改。）</li></ul><p>(5) Record Lock、Gap Lock、Next-key Lock 首先，我们要明确这三者都是 InnoDB 中<strong>行级锁</strong>的范围，并且这三种锁作用的基础和对象都是<strong>索引</strong>。</p><ul><li>行锁（Record Lock），在单条索引记录上加锁，record lock 锁住的永远是索引，而非记录本身，即使该表上没有任何索引，那么innodb会在后台创建一个隐藏的聚集主键索引，那么锁住的就是这个隐藏的聚集主键索引。所以说当一条sql没有走任何索引时，那么将会在每一条聚集索引后面加X锁，这个类似于表锁，但原理上和表锁应该是完全不同的；</li><li>间隙锁（Gap Lock），在索引记录之间的间隙中加锁，或者是在某一条索引记录之前或者之后加锁，并不包括该索引记录本身。gap lock的机制主要是解决可重复读模式下的幻读问题；</li><li>Next-Key Lock，是行锁和间隙锁的组合使用，默认情况下，InnoDB工作在可重复读隔离级别下，并且会以 Next-Key Lock 的方式对数据行进行加锁，这样可以有效防止幻读的发生。当 InnoDB 扫描索引记录的时候，会首先对索引记录加上行锁（Record Lock），再对索引记录两边的间隙加上间隙锁（Gap Lock）。加上间隙锁之后，其他事务就不能在这个间隙修改或者插入记录。</li><li>当以当前读模式<code>select * from t5 where id=7 for update;</code>获取 <code>id=7</code> 的数据时,产生了 <code>Next-Key Lock</code> ，锁住了 <code>4-10</code> 范围和 <code>id=7</code> 单个 <code>record</code> ，从而阻塞了 <code>SESSION_B</code> 在这个范围内插入数据，而在除此之外的范围内是可以插入数据的。<strong>尽量使用唯一索引,因为唯一索引会把Next-Key Lock降级为Record Lock</strong></li></ul><h2 id="什么是死锁？"><a href="#什么是死锁？" class="headerlink" title="什么是死锁？"></a>什么是死锁？</h2><p>死锁是指两个或两个以上的进程在执行过程中，因争夺资源而造成的一种互相等待的现象,若无外力作用,它们都将无法推进下去。此时称系统处于死锁状态或系统产生了死锁，这些永远在互相等待的过程称为死锁。</p><ul><li>MyISAM 不会出现死锁，因为 MyISAM 总是一次获得所需要的全部锁，要么全部满足，要么全等待；</li><li>InnoDB 除了单 SQL 事务，锁是逐步获得的，因此可能出现死锁。一般 InnoDB 能自动检测死锁，并使一个较简单的事务回退并释放锁，另一个事务获得锁，继续完成事务。</li></ul><h2 id="如何处理死锁"><a href="#如何处理死锁" class="headerlink" title="如何处理死锁"></a>如何处理死锁</h2><p>对待死锁常见的两种策略：</p><ul><li>通过 innodb_lock_wait_timeout 来设置超时时间，一直等待直到超时；</li><li>发起死锁检测，发现死锁之后，主动回滚死锁中的某一个事务，让其它事务继续执行。</li></ul><h2 id="如何判断死锁"><a href="#如何判断死锁" class="headerlink" title="如何判断死锁"></a>如何判断死锁</h2><p>(1) 超时法：如果某个事物的等待时间超过指定时限，则判定为出现死锁</p><p>(2) 等待图法：等待图法指的是如果事务等待图中出现了回路，则判断出现了死锁</p><h2 id="如何查看死锁"><a href="#如何查看死锁" class="headerlink" title="如何查看死锁"></a>如何查看死锁</h2><ul><li>使用命令 show engine innodb status 查看最近的一次死锁。</li><li>InnoDB Lock Monitor 打开锁监控，每 15s 输出一次日志。使用完毕后建议关闭，否则会影响数据库性能。</li></ul><h2 id="如何避免死锁"><a href="#如何避免死锁" class="headerlink" title="如何避免死锁"></a>如何避免死锁</h2><p>(1) 按同一顺序访问对象 如果所有并发事务按同一顺序访问对象，则发生死锁的可能性会降低。例如，如果两个并发事务获得 Supplier 表上的锁，然后获得 Part 表上的锁，则在其中一个事务完成之前，另一个事务被阻塞在 Supplier 表上。第一个事务提交或回滚后，第二个事务继续进行。不发生死锁。将存储过程用于所有的数据修改可以标准化访问对象的顺序。</p><p>(2) 避免事务中的用户交互 避免编写包含用户交互的事务，因为运行没有用户交互的批处理的速度要远远快于用户手动响应查询的速度，例如答复应用程序请求参数的提示。例如，如果事务正在等待用户输入，而用户去吃午餐了或者甚至回家过周末了，则用户将此事务挂起使之不能完成。这样将降低系统的吞吐量，因为事务持有的任何锁只有在事务提交或回滚时才会释放。即使不出现死锁的情况，访问同一资源的其它事务也会被阻塞，等待该事务完成。</p><p>(3) 保持事务简短并在一个批处理中 在同一数据库中并发执行多个需要长时间运行的事务时通常发生死锁。事务运行时间越长，其持有排它锁或更新锁的时间也就越长，从而堵塞了其它活动并可能导致死锁。 保持事务在一个批处理中，可以最小化事务的网络通信往返量，减少完成事务可能的延迟并释放锁。</p><p>(4) 使用低隔离级别 确定事务是否能在更低的隔离级别上运行。执行提交读允许事务读取另一个事务已读取（未修改）的数据，而不必等待第一个事务完成。使用较低的隔离级别（例如提交读）而不使用较高的隔离级别（例如可串行读）可以缩短持有共享锁的时间，从而降低了锁定争夺。</p><p>(5) 使用绑定连接 使用绑定连接使同一应用程序所打开的两个或多个连接可以相互合作。次级连接所获得的任何锁可以象由主连接获得的锁那样持有，反之亦然，因此不会相互阻塞</p><p>(6) SELECT语句加With(NoLock)提示 默认情况下SELECT语句会对查询到的资源加 S 锁(共享锁)，S锁与X锁(排他锁)不兼容；但加上 With(NoLock) 后，SELECT不对查询到的资源加锁(或者加 Sch-S 锁，Sch-S 锁可以与任何锁兼容)；从而可以是这两条 SQL 可以并发地访问同一资源。当然，此方法适合解决读与写并发死锁的情况，但加 With(NoLock) 可能会导致脏读。</p><h2 id="优化锁方面的建议"><a href="#优化锁方面的建议" class="headerlink" title="优化锁方面的建议"></a>优化锁方面的建议</h2><ul><li>尽量使用较低的隔离级别。</li><li>精心设计索引， 并尽量使用索引访问数据， 使加锁更精确， 从而减少锁冲突的机会。</li><li>选择合理的事务大小，小事务发生锁冲突的几率也更小。</li><li>给记录集显示加锁时，最好一次性请求足够级别的锁。比如要修改数据的话，最好直接申请排他锁，而不是先申请共享锁，修改时再请求排他锁，这样容易产生死锁。</li><li>不同的程序访问一组表时，应尽量约定以相同的顺序访问各表，对一个表而言，尽可能以固定的顺序存取表中的行。这样可以大大减少死锁的机会。</li><li>尽量用相等条件访问数据，这样可以避免间隙锁对并发插入的影响。</li><li>不要申请超过实际需要的锁级别。</li><li>除非必须，查询时不要显示加锁。 MySQL 的 MVCC 可以实现事务中的查询不用加锁，优化事务性能；MVCC 只在 COMMITTED READ（读提交）和 REPEATABLE READ（可重复读）两种隔离级别下工作。</li><li>对于一些特定的事务，可以使用表锁来提高处理速度或减少死锁的可能。</li></ul><hr><h1 id="事务相关"><a href="#事务相关" class="headerlink" title="事务相关"></a>事务相关</h1><h2 id="什么是事务"><a href="#什么是事务" class="headerlink" title="什么是事务"></a>什么是事务</h2><p>事务是<strong>一系列的数据库操作，是数据库应用的基本单位</strong>。MySQL 事务主要用于处理操作量大，复杂度高的数据。</p><h2 id="事务的特性"><a href="#事务的特性" class="headerlink" title="事务的特性"></a>事务的特性</h2><p>在 MySQL 中只有 InnDB 引擎支持事务，它的四个特性如下：</p><ul><li>原子性（Atomic）：事务开始后所有操作，要么全部做完，要么全部不做，不可能停滞在中间环节。事务执行过程中出错，会回滚到事务开始前的状态，所有的操作就像没有发生一样。也就是说事务是一个不可分割的整体，就像化学中学过的原子，是物质构成的基本单位；</li><li>一致性（Consistency）：事务的执行使得数据库从一种正确状态转化为另一种正确状态，事务开始前和结束后，数据库的完整性约束没有被破坏 。比如A向B转账，不可能A扣了钱，B却没收到；</li><li>隔离性（Isolation）：在事务正确提交之前，不允许把该事务对数据的任何改变提供给其他事务，同一时间，只允许一个事务请求同一数据，不同的事务之间彼此没有任何干扰。比如A正在从一张银行卡中取钱，在A取钱的过程结束前，B不能向这张卡转账；</li><li>持久性（Durability）：事务提交后，其结果永久保存在数据库中。</li></ul><h2 id="事务的并发问题"><a href="#事务的并发问题" class="headerlink" title="事务的并发问题"></a>事务的并发问题</h2><ul><li>脏读（select）：事务A读取了事务B更新的数据，然后B回滚操作，那么A读取到的数据是脏数据 </li><li>不可重复读（update）：事务 A 多次读取同一数据，事务 B 在事务A多次读取的过程中，对数据作了更新并提交，导致事务A多次读取同一数据时，结果不一致。 </li><li>幻读（insert/delete）：系统管理员A将数据库中所有学生的成绩从具体分数改为ABCDE等级，但是系统管理员B就在这个时候插入了一条具体分数的记录，当系统管理员A改结束后发现还有一条记录没有改过来，就好像发生了幻觉一样，这就叫幻读。</li></ul><p><strong>幻读和不可重复读的区别？</strong></p><ul><li>不可重复读的重点是修改：在同一事务中，同样的条件，第一次读的数据和第二次读的数据不一样。（因为中间有其他事务提交了修改）。</li><li>幻读的重点在于新增或者删除：在同一事务中，同样的条件，第一次和第二次读出来的记录数不一样。（因为中间有其他事务提交了插入/删除）。</li><li>解决不可重复读：锁行；解决幻读：锁表</li></ul><h2 id="MySQL-中有几种事务隔离级别"><a href="#MySQL-中有几种事务隔离级别" class="headerlink" title="MySQL 中有几种事务隔离级别"></a>MySQL 中有几种事务隔离级别</h2><p>四种隔离级别主要解决的是脏读、幻读和不可重复读的问题。</p><h3 id="读未提交-Read-uncommitted"><a href="#读未提交-Read-uncommitted" class="headerlink" title="读未提交(Read uncommitted)"></a>读未提交(Read uncommitted)</h3><p><code>select</code> 语句不加锁。可能读取到不一致的数据，即<strong>脏读</strong>。</p><p>并发最高，一致性最差的隔离级别。</p><h3 id="读已提交-Read-committed"><a href="#读已提交-Read-committed" class="headerlink" title="读已提交(Read committed)"></a>读已提交(Read committed)</h3><p>可避免<strong>脏读</strong>的发生，但是会有<strong>幻读</strong>和<strong>不可重复读</strong>的问题。在互联网大数据量，高并发量的场景下，几乎 <strong>不会使用</strong> 上述两种隔离级别。</p><p>各种系统中最常用的一种隔离级别，也是SQL Server和Oracle的默认隔离级别。</p><p>默认查询不加锁，除非显示加锁，如</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">select</span> * <span class="keyword">from</span> T <span class="keyword">where</span> <span class="keyword">ID</span>=<span class="number">2</span> <span class="keyword">lock</span> <span class="keyword">in</span> <span class="keyword">share</span> <span class="keyword">mode</span>;</span><br><span class="line"><span class="keyword">select</span> * <span class="keyword">from</span> T <span class="keyword">where</span> <span class="keyword">ID</span>=<span class="number">2</span> <span class="keyword">for</span> <span class="keyword">update</span>;</span><br></pre></td></tr></table></figure><p>使用快照机制来实现读提交：数据有多个版本， 当事务并发执行时， 某一事务读取的数据来自其中一个版本（快照）。</p><p>这种既能保证一致性又不加锁的读也被称为快照读 （<strong>Snapshot Read</strong>）。假设没有“快照读”，那么当一个更新的事务没有提交时，另一个对更新数据进行查询的事务会因为无法查询而被阻塞（因为上了X锁，即写锁，所以不能得到S锁，即读锁），这种情况下，并发能力就相当的差。而“快照读”就可以完成高并发的查询，不过，读提交只能避免脏读，并不能避免不可重复读和幻读。</p><h3 id="可重复读-Repeatable-read"><a href="#可重复读-Repeatable-read" class="headerlink" title="可重复读(Repeatable read)"></a>可重复读(Repeatable read)</h3><p>当事务启动时，就不允许进行修改操作(<code>Update</code>) 。</p><p><strong><code>MySql</code> 默认隔离级别</strong>。可避免 脏读 、不可重复读的发生。不可重复读对应的是修改，即 <code>UPDATE</code> 操作。但是可能还会有幻读问题。因为幻读问题对应的是插入 <code>INSERT</code> 操作，而不是 <code>UPDATE</code> 操作。</p><p>在这个级别下，普通的查询同样是使用的<strong>快照读</strong>，但是，和读提交不同的是，当事务启动时，就不允许进行修改操作(<code>Update</code>) 了，而“不可重复读”恰恰是因为两次读取之间进行了数据的修改，因此，“可重复读”能够有效的避免“不可重复读”。</p><h3 id="串行化-Serializable"><a href="#串行化-Serializable" class="headerlink" title="串行化(Serializable)"></a>串行化(Serializable)</h3><p>可避免 <strong>脏读、不可重复读、幻读</strong> 的发生。 <code>Serializable</code> 是一致性最好的，性能最差的。这种级别下，事务串行化顺序执行，也就是一个一个排队执行。执行效率奇差，性能开销也最大，所以基本没人会用。</p><p>以 <strong>锁表</strong> 的方式使得其他的线程只能在锁外等待。</p><table><thead><tr><th>事务隔离级别</th><th>脏读</th><th>不可重复读</th><th>幻读</th></tr></thead><tbody><tr><td>读未提交（read-uncommitted）</td><td>是</td><td>是</td><td>是</td></tr><tr><td>读提交（read-commited）</td><td>否</td><td>是</td><td>是</td></tr><tr><td>可重复读（repeatable-read）</td><td>否</td><td>否</td><td>是</td></tr><tr><td>串行化（serializable）</td><td>否</td><td>否</td><td>否</td></tr></tbody></table><p>mysql 默认的事务隔离级别为 repeatable-read</p><h2 id="什么是快照读、当前读"><a href="#什么是快照读、当前读" class="headerlink" title="什么是快照读、当前读"></a>什么是快照读、当前读</h2><ul><li><p><strong>快照读</strong>, 读取专门的快照 (对于 <code>RC</code> ，快照( <code>ReadView</code> )会在每个语句中创建。对于 <code>RR</code> ，快照是在事务启动时创建的)</p><p>普通的SELECT就是快照读</p></li><li><p><strong>当前读</strong>, 读取最新版本的记录, 没有快照。 在 <code>InnoDB</code> 中，当前读取根本不会创建任何快照。当前读会在搜索的时候加锁</p><p>UPDATE、DELETE、INSERT、SELECT … LOCK IN SHARE MODE、SELECT … FOR UPDATE是当前读</p></li></ul><h2 id="MySql如何解决幻读问题"><a href="#MySql如何解决幻读问题" class="headerlink" title="MySql如何解决幻读问题"></a>MySql如何解决幻读问题</h2><p>两点需要说明：</p><ol><li>在可重复读隔离级别下，普通查询是<strong>快照读</strong>，是不会看到别的事务插入的数据的，幻读只在<strong>当前读</strong>下才会出现。</li><li>幻读专指新插入的行，读到原本存在行的更新结果不算幻读。因为<strong>当前读的作用就是能读到所有已经提交记录的最新值</strong>。如果事务中都使用快照读，那么就不会产生幻读现象，但是<strong>快照读和当前读混用就会产生幻读</strong>。</li></ol><p><strong>幻读产生的原因</strong></p><p>行锁只能锁住行，即使把所有的行记录都上锁，也阻止不了新插入的记录。</p><p><strong>如何解决幻读？</strong></p><ol><li>在<strong>快照读</strong>情况下，<code>mysql</code> 通过<strong>mvcc</strong>来避免幻读</li><li>在<strong>当前读</strong>情况下，<code>mysql</code> 通过<strong>next-key</strong>来避免幻读</li></ol><h2 id="MVCC-是什么？"><a href="#MVCC-是什么？" class="headerlink" title="MVCC 是什么？"></a>MVCC 是什么？</h2><p>MVCC 是多版本并发控制（Multi-Version Concurrency Control）的简称，是用来解决事务中排他锁的使用导致读操作等待的。</p><p>在四种隔离级别中，不可重复读和可重复读是使用了 MVCC 的，而串行化是使用了表级锁，读未提交好像什么都没实现。</p><p>InnoDB 的 MVCC 是通过在每行记录后面保存两个隐藏的列（实际上是三个，分别是DB_TRX_ID、DB_ROLL_PTR、DB_ROW_ID，但是为了记忆方便我们还是认为是两个）来实现，这两个列一个保存了行的创建时间，一个保存行的过期时间（删除时间）。当然存储的并不是真实的时间而是系统版本号（system version number）。每开始一个新的事务，系统版本号都会自动新增，事务开始时刻的系统版本号会作为事务的版本号，用来查询到每行记录的版本号进行比较。</p><h2 id="一致性非锁定读和锁定读"><a href="#一致性非锁定读和锁定读" class="headerlink" title="一致性非锁定读和锁定读"></a>一致性非锁定读和锁定读</h2><p><strong>锁定读</strong> 在一个事务中，标准的SELECT语句是不会加锁，但是有两种情况例外。SELECT … LOCK IN SHARE MODE 和 SELECT … FOR UPDATE。 <code>SELECT ... LOCK IN SHARE MODE</code>给记录假设共享锁，这样一来的话，其它事务只能读不能修改，直到当前事务提交。<code>SELECT ... FOR UPDATE</code>给索引记录加锁，这种情况下跟UPDATE的加锁情况是一样的。</p><p><strong>一致性非锁定读</strong> 　consistent read （一致性读），InnoDB 用多版本来提供查询数据库在某个时间点的快照。如果隔离级别是 REPEATABLE READ，那么在同一个事务中的所有一致性读都读的是事务中第一个这样的读读到的快照(官方原文-“If the transaction isolation level is REPEATABLE_READ(the default level), all consistent reads within the same transaction read the snapshot established by the first such read in that transaction. You can get a fresher snapshot for you queries by commiting the current transaction and after that issuing new queries.”)；如果是 READ COMMITTED，那么一个事务中的每一个一致性读都会读到它自己刷新的快照版本（官方原文-“With ‘READ_COMMITTED’ isolation level, each consistent read within a transaction sets and reads its own fresh snapshot”）。Consistent read（一致性读）是 READ COMMITTED 和 REPEATABLE READ 隔离级别下普通 SELECT 语句默认的模式。一致性读不会给它所访问的表加任何形式的锁，因此其它事务可以同时并发的修改它们。</p><h2 id="REPEATABLE-READ（可重复读）隔离级别下-MVCC-如何工作"><a href="#REPEATABLE-READ（可重复读）隔离级别下-MVCC-如何工作" class="headerlink" title="REPEATABLE READ（可重复读）隔离级别下 MVCC 如何工作"></a>REPEATABLE READ（可重复读）隔离级别下 MVCC 如何工作</h2><ul><li>SELECT：InnoDB 会根据以下条件检查每一行记录：第一，InnoDB 只查找版本早于当前事务版本的数据行，这样可以确保事务读取的行要么是在开始事务之前已经存在要么是事务自身插入或者修改过的。第二，行的删除版本号要么未定义，要么大于当前事务版本号，这样可以确保事务读取到的行在事务开始之前未被删除。</li><li>INSERT：InnoDB 为新插入的每一行保存当前系统版本号作为行版本号。</li><li>DELETE：InnoDB 为删除的每一行保存当前系统版本号作为行删除标识。</li><li>UPDATE：InnoDB 为插入的一行新纪录保存当前系统版本号作为行版本号，同时保存当前系统版本号到原来的行作为删除标识。 这两个版本号，使大多数操作都不用加锁。它不足之处是每行记录都需要额外的存储空间，需要做更多的行检查工作和一些额外的维护工作。</li></ul><h1 id="其他"><a href="#其他" class="headerlink" title="其他"></a>其他</h1><h2 id="数据库三范式"><a href="#数据库三范式" class="headerlink" title="数据库三范式"></a>数据库三范式</h2><ul><li><p>第一范式</p><p>所有字段值都是不可分解的原子值</p></li><li><p>第二范式</p><p>每列都和主键相关，而不能只与主键的某一部分相关（主要针对联合主键而言）</p><p>也就是说在一个数据库表中，一个表中只能保存一种数据，不可以把多种数据保存在同一张数据库表中。(唯一性 一个表只说明一个事物)。比如有两个列都是主键，不能存在这样的属性，它只依赖于其中一个列，这就是不符合第二范式。这种情况需要进行表的拆分。</p></li><li><p>第三范式</p><p>每列都和主键列直接相关,而不是间接相关</p><p>非主键字段不能相互依赖，每列都与主键有直接关系，不存在传递依赖。主键在一个表里只能存在一个！因为建立主键后，DBMS会默认把建立主键的列建立聚簇索引。但是一个表是允许把几个列一起设为主键的。</p></li></ul><h2 id="MyISAM与InnoDB-的区别"><a href="#MyISAM与InnoDB-的区别" class="headerlink" title="MyISAM与InnoDB 的区别"></a>MyISAM与InnoDB 的区别</h2><ol><li><strong>InnoDB支持事务，MyISAM不支持</strong>，对于InnoDB每一条SQL语言都默认封装成事务，自动提交，这样会影响速度，所以最好把多条SQL语言放在begin和commit之间，组成一个事务；</li><li><strong>InnoDB支持外键，而MyISAM不支持</strong>。对一个包含外键的InnoDB表转为MYISAM会失败； 外键用于与另一张表的关联。是能确定另一张表记录的字段，用于保持数据的一致性。比如，A表中的一个字段，是B表的主键，那他就可以是A表的外键。</li><li><strong>InnoDB是聚集索引，而MyISAM是非聚集索引</strong>。</li><li><strong>InnoDB不保存表的具体行数，而MyISAM用一个变量保存了整个表的行数</strong>。前者执行select count(*) from table时需要全表扫描。后者执行上述语句时只需要读出该变量即可，速度很快（注意不能加有任何WHERE条件）</li><li><strong>InnoDB支持表、行(默认)级锁，而MyISAM支持表级锁</strong></li><li><strong>InnoDB表必须有唯一索引（如主键），而Myisam可以没有</strong></li></ol><h2 id="持久型数据库怎么实现的"><a href="#持久型数据库怎么实现的" class="headerlink" title="持久型数据库怎么实现的"></a>持久型数据库怎么实现的</h2><h2 id="数据库有哪几种日志"><a href="#数据库有哪几种日志" class="headerlink" title="数据库有哪几种日志"></a>数据库有哪几种日志</h2><h2 id="分布式下还能满足ACID吗"><a href="#分布式下还能满足ACID吗" class="headerlink" title="分布式下还能满足ACID吗"></a>分布式下还能满足ACID吗</h2>]]></content>
    
    
      
      
    <summary type="html">&lt;h1 id=&quot;索引&quot;&gt;&lt;a href=&quot;#索引&quot; class=&quot;headerlink&quot; title=&quot;索引&quot;&gt;&lt;/a&gt;索引&lt;/h1&gt;&lt;h2 id=&quot;各种索引概念的区别&quot;&gt;&lt;a href=&quot;#各种索引概念的区别&quot; class=&quot;headerlink&quot; title=&quot;各种索引概念的</summary>
      
    
    
    
    
    <category term="学习" scheme="https://ifwant.tech/tags/%E5%AD%A6%E4%B9%A0/"/>
    
    <category term="数据库" scheme="https://ifwant.tech/tags/%E6%95%B0%E6%8D%AE%E5%BA%93/"/>
    
  </entry>
  
  <entry>
    <title>数据库语法</title>
    <link href="https://ifwant.tech/2021/01/09/sql/"/>
    <id>https://ifwant.tech/2021/01/09/sql/</id>
    <published>2021-01-09T10:26:39.000Z</published>
    <updated>2021-01-17T07:11:42.000Z</updated>
    
    <content type="html"><![CDATA[<p><img src="https://ifwant-pic-1303131123.cos.ap-nanjing.myqcloud.com/photo/20180718003208394.png"></p><h1 id="基础"><a href="#基础" class="headerlink" title="基础"></a>基础</h1><p>模式定义了数据如何存储、存储什么样的数据以及数据如何分解等信息，数据库和表都有模式。</p><p>主键的值不允许修改，也不允许复用（不能将已经删除的主键值赋给新数据行的主键）。</p><p>SQL（Structured Query Language)，标准 SQL 由 ANSI 标准委员会管理，从而称为 ANSI SQL。各个 DBMS 都有自己的实现，如 PL/SQL、Transact-SQL 等。</p><p>SQL 语句不区分大小写，但是数据库表名、列名和值是否区分依赖于具体的 DBMS 以及配置。</p><p>SQL 支持以下三种注释：</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># #  注释</span></span><br><span class="line"><span class="keyword">SELECT</span> *</span><br><span class="line"><span class="keyword">FROM</span> mytable; <span class="comment">-- 注释</span></span><br><span class="line"><span class="comment">/* 注释1</span></span><br><span class="line"><span class="comment">   注释2 */</span></span><br></pre></td></tr></table></figure><p>数据库创建与使用：</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">CREATE</span> <span class="keyword">DATABASE</span> <span class="keyword">test</span>;</span><br><span class="line"><span class="keyword">USE</span> <span class="keyword">test</span>;</span><br></pre></td></tr></table></figure><h1 id="创建表"><a href="#创建表" class="headerlink" title="创建表"></a>创建表</h1><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">CREATE</span> <span class="keyword">TABLE</span> table_name (column_name column_type);</span><br><span class="line"></span><br><span class="line"><span class="keyword">CREATE</span> <span class="keyword">TABLE</span> mytable (</span><br><span class="line">  <span class="comment">#  int 类型，不为空，自增</span></span><br><span class="line">  <span class="keyword">id</span> <span class="built_in">INT</span> <span class="keyword">NOT</span> <span class="literal">NULL</span> AUTO_INCREMENT,</span><br><span class="line">  <span class="comment">#  int 类型，不可为空，默认值为 1，不为空</span></span><br><span class="line">  col1 <span class="built_in">INT</span> <span class="keyword">NOT</span> <span class="literal">NULL</span> <span class="keyword">DEFAULT</span> <span class="number">1</span>,</span><br><span class="line">  <span class="comment">#  变长字符串类型，最长为 45 个字符，可以为空</span></span><br><span class="line">  col2 <span class="built_in">VARCHAR</span>(<span class="number">45</span>) <span class="literal">NULL</span>,</span><br><span class="line">  <span class="comment">#  日期类型，可为空</span></span><br><span class="line">  col3 <span class="built_in">DATE</span> <span class="literal">NULL</span>,</span><br><span class="line">  <span class="comment">#  设置主键为 id</span></span><br><span class="line">  PRIMARY <span class="keyword">KEY</span> (<span class="string">`id`</span>));</span><br></pre></td></tr></table></figure><h1 id="修改表"><a href="#修改表" class="headerlink" title="修改表"></a>修改表</h1><p>添加列</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">ALTER</span> <span class="keyword">TABLE</span> mytable</span><br><span class="line"><span class="keyword">ADD</span> <span class="keyword">col</span> <span class="built_in">CHAR</span>(<span class="number">20</span>);</span><br><span class="line"></span><br><span class="line"><span class="keyword">ALTER</span> <span class="keyword">TABLE</span> mytable <span class="comment">--添加到第一列</span></span><br><span class="line"><span class="keyword">ADD</span> i <span class="built_in">INT</span> <span class="keyword">FIRST</span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">ALTER</span> <span class="keyword">TABLE</span> mytable <span class="comment">--添加到c列之后</span></span><br><span class="line"><span class="keyword">ADD</span> i <span class="built_in">INT</span> <span class="keyword">AFTER</span> c;</span><br></pre></td></tr></table></figure><p>删除列</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">ALTER</span> <span class="keyword">TABLE</span> mytable</span><br><span class="line"><span class="keyword">DROP</span> <span class="keyword">COLUMN</span> <span class="keyword">col</span>;</span><br></pre></td></tr></table></figure><p>删除表</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">DROP</span> <span class="keyword">TABLE</span> mytable;</span><br></pre></td></tr></table></figure><p>修改表</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">ALTER</span> <span class="keyword">TABLE</span> mytable <span class="comment">--c修改为CHAR(10)类型</span></span><br><span class="line"><span class="keyword">MODIFY</span> c <span class="built_in">CHAR</span>(<span class="number">10</span>);</span><br><span class="line"></span><br><span class="line"><span class="keyword">ALTER</span> <span class="keyword">TABLE</span> mytable <span class="comment">--修改i字段名为j，类型改为BIGINT</span></span><br><span class="line"><span class="keyword">CHANGE</span> i j <span class="built_in">BIGINT</span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">ALTER</span> <span class="keyword">TABLE</span> mytable <span class="comment">--修改字段默认值</span></span><br><span class="line"><span class="keyword">ALTER</span> i <span class="keyword">SET</span> <span class="keyword">DEFAULT</span> <span class="number">1000</span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">ALTER</span> <span class="keyword">TABLE</span> mytable <span class="comment">--删除字段默认值</span></span><br><span class="line"><span class="keyword">ALTER</span> i <span class="keyword">DROP</span> <span class="keyword">DEFAULT</span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">ALTER</span> <span class="keyword">TABLE</span> mytable <span class="comment">--修改表名</span></span><br><span class="line"><span class="keyword">RENAME</span> <span class="keyword">TO</span> alter_tbl;</span><br><span class="line"></span><br><span class="line"><span class="keyword">ALTER</span> <span class="keyword">TABLE</span> tableName <span class="comment">--修改列的位置</span></span><br><span class="line"><span class="keyword">MODIFY</span> name1 type1 <span class="keyword">first</span>|<span class="keyword">after</span> name2;</span><br></pre></td></tr></table></figure><h1 id="插入"><a href="#插入" class="headerlink" title="插入"></a>插入</h1><p>普通插入</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">INSERT</span> <span class="keyword">INTO</span> mytable(col1, col2)</span><br><span class="line"><span class="keyword">VALUES</span>(val1, val2);</span><br></pre></td></tr></table></figure><p>插入检索出来的数据</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">INSERT</span> <span class="keyword">INTO</span> mytable1(col1, col2)</span><br><span class="line"><span class="keyword">SELECT</span> col1, col2</span><br><span class="line"><span class="keyword">FROM</span> mytable2;</span><br></pre></td></tr></table></figure><p>将一个表的内容插入到一个新表</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">CREATE</span> <span class="keyword">TABLE</span> newtable <span class="keyword">AS</span></span><br><span class="line"><span class="keyword">SELECT</span> * <span class="keyword">FROM</span> mytable;</span><br></pre></td></tr></table></figure><h1 id="更新"><a href="#更新" class="headerlink" title="更新"></a>更新</h1><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">UPDATE</span> mytable</span><br><span class="line"><span class="keyword">SET</span> <span class="keyword">col</span> = val</span><br><span class="line"><span class="keyword">WHERE</span> <span class="keyword">id</span> = <span class="number">1</span>;</span><br></pre></td></tr></table></figure><h1 id="删除"><a href="#删除" class="headerlink" title="删除"></a>删除</h1><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">DELETE</span> <span class="keyword">FROM</span> mytable</span><br><span class="line"><span class="keyword">WHERE</span> <span class="keyword">id</span> = <span class="number">1</span>;</span><br></pre></td></tr></table></figure><p><strong>TRUNCATE TABLE</strong> 可以清空表，也就是删除所有行。</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">TRUNCATE</span> <span class="keyword">TABLE</span> mytable;</span><br></pre></td></tr></table></figure><p>使用更新和删除操作时一定要用 WHERE 子句，不然会把整张表的数据都破坏。可以先用 SELECT 语句进行测试，防止错误删除。</p><h1 id="查询"><a href="#查询" class="headerlink" title="查询"></a>查询</h1><h2 id="DISTINCT"><a href="#DISTINCT" class="headerlink" title="DISTINCT"></a>DISTINCT</h2><p>相同值只会出现一次。它作用于所有列，也就是说所有列的值都相同才算相同。</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> <span class="keyword">DISTINCT</span> col1, col2</span><br><span class="line"><span class="keyword">FROM</span> mytable;</span><br></pre></td></tr></table></figure><h2 id="LIMIT"><a href="#LIMIT" class="headerlink" title="LIMIT"></a>LIMIT</h2><p>限制返回的行数。可以有两个参数，第一个参数为起始行，从 0 开始；第二个参数为返回的总行数。</p><p>返回前 5 行：</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> *</span><br><span class="line"><span class="keyword">FROM</span> mytable</span><br><span class="line"><span class="keyword">LIMIT</span> <span class="number">5</span>;</span><br><span class="line"><span class="keyword">SELECT</span> *</span><br><span class="line"><span class="keyword">FROM</span> mytable</span><br><span class="line"><span class="keyword">LIMIT</span> <span class="number">0</span>, <span class="number">5</span>;</span><br></pre></td></tr></table></figure><p>返回第 3 ~ 5 行：</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> *</span><br><span class="line"><span class="keyword">FROM</span> mytable</span><br><span class="line"><span class="keyword">LIMIT</span> <span class="number">2</span>, <span class="number">3</span>;</span><br></pre></td></tr></table></figure><h1 id="排序"><a href="#排序" class="headerlink" title="排序"></a>排序</h1><ul><li><strong>ASC</strong> ：升序（默认）</li><li><strong>DESC</strong> ：降序</li></ul><p>可以按多个列进行排序，并且为每个列指定不同的排序方式：</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> *</span><br><span class="line"><span class="keyword">FROM</span> mytable</span><br><span class="line"><span class="keyword">ORDER</span> <span class="keyword">BY</span> col1 <span class="keyword">DESC</span>, col2 <span class="keyword">ASC</span>;</span><br></pre></td></tr></table></figure><h1 id="过滤"><a href="#过滤" class="headerlink" title="过滤"></a>过滤</h1><p>不进行过滤的数据非常大，导致通过网络传输了多余的数据，从而浪费了网络带宽。因此尽量使用 SQL 语句来过滤不必要的数据，而不是传输所有的数据到客户端中然后由客户端进行过滤。</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> *</span><br><span class="line"><span class="keyword">FROM</span> mytable</span><br><span class="line"><span class="keyword">WHERE</span> <span class="keyword">col</span> <span class="keyword">IS</span> <span class="literal">NULL</span>;</span><br></pre></td></tr></table></figure><p>下表显示了 WHERE 子句可用的操作符</p><table><thead><tr><th align="center">操作符</th><th align="center">说明</th></tr></thead><tbody><tr><td align="center">=</td><td align="center">等于</td></tr><tr><td align="center">&lt;</td><td align="center">小于</td></tr><tr><td align="center">&gt;</td><td align="center">大于</td></tr><tr><td align="center">&lt;&gt; !=</td><td align="center">不等于</td></tr><tr><td align="center">&lt;= !&gt;</td><td align="center">小于等于</td></tr><tr><td align="center">&gt;= !&lt;</td><td align="center">大于等于</td></tr><tr><td align="center">BETWEEN</td><td align="center">在两个值之间</td></tr><tr><td align="center">IS NULL</td><td align="center">为 NULL 值</td></tr></tbody></table><p>应该注意到，NULL 与 0、空字符串都不同。</p><p><strong>AND 和 OR</strong> 用于连接多个过滤条件。优先处理 AND，当一个过滤表达式涉及到多个 AND 和 OR 时，可以使用 () 来决定优先级，使得优先级关系更清晰。</p><p><strong>IN</strong> 操作符用于匹配一组值，其后也可以接一个 SELECT 子句，从而匹配子查询得到的一组值。</p><p><strong>NOT</strong> 操作符用于否定一个条件。</p><h1 id="通配符"><a href="#通配符" class="headerlink" title="通配符"></a>通配符</h1><p>通配符也是用在过滤语句中，但它只能用于文本字段。</p><ul><li><strong>%</strong> 匹配 &gt;=0 个任意字符；</li><li><strong>_</strong> 匹配 ==1 个任意字符；</li><li><strong>[ ]</strong> 可以匹配集合内的字符，例如 [ab] 将匹配字符 a 或者 b。用脱字符 ^ 可以对其进行否定，也就是不匹配集合内的字符。</li><li>查询内容包含通配符时，把特殊字符用 **[ ]**括起便可正常查询</li></ul><p>使用 Like 来进行通配符匹配。</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> *</span><br><span class="line"><span class="keyword">FROM</span> mytable</span><br><span class="line"><span class="keyword">WHERE</span> <span class="keyword">col</span> <span class="keyword">LIKE</span> <span class="string">&#x27;[^AB]%&#x27;</span>; <span class="comment">-- 不以 A 和 B 开头的任意文本</span></span><br></pre></td></tr></table></figure><p>不要滥用通配符，通配符位于开头处匹配会非常慢。</p><h1 id="计算字段"><a href="#计算字段" class="headerlink" title="计算字段"></a>计算字段</h1><p>在数据库服务器上完成数据的转换和格式化的工作往往比客户端上快得多，并且转换和格式化后的数据量更少的话可以减少网络通信量。</p><p>计算字段通常需要使用 <strong>AS</strong> 来取别名，否则输出的时候字段名为计算表达式。</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> col1 * col2 <span class="keyword">AS</span> <span class="keyword">alias</span></span><br><span class="line"><span class="keyword">FROM</span> mytable;</span><br></pre></td></tr></table></figure><p><strong>CONCAT()</strong> 用于连接两个字段。许多数据库会使用空格把一个值填充为列宽，因此连接的结果会出现一些不必要的空格，使用 <strong>TRIM()</strong> 可以去除首尾空格。</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> <span class="keyword">CONCAT</span>(<span class="keyword">TRIM</span>(col1), <span class="string">&#x27;(&#x27;</span>, <span class="keyword">TRIM</span>(col2), <span class="string">&#x27;)&#x27;</span>) <span class="keyword">AS</span> concat_col</span><br><span class="line"><span class="keyword">FROM</span> mytable;</span><br></pre></td></tr></table></figure><h1 id="函数"><a href="#函数" class="headerlink" title="函数"></a>函数</h1><p>各个 DBMS 的函数都是不相同的，因此不可移植，以下主要是 MySQL 的函数。</p><h2 id="汇总"><a href="#汇总" class="headerlink" title="汇总"></a>汇总</h2><table><thead><tr><th align="center">函 数</th><th align="center">说 明</th></tr></thead><tbody><tr><td align="center">AVG()</td><td align="center">返回某列的平均值</td></tr><tr><td align="center">COUNT()</td><td align="center">返回某列的行数</td></tr><tr><td align="center">MAX()</td><td align="center">返回某列的最大值</td></tr><tr><td align="center">MIN()</td><td align="center">返回某列的最小值</td></tr><tr><td align="center">SUM()</td><td align="center">返回某列值之和</td></tr></tbody></table><p>AVG() 会忽略 NULL 行。</p><p>使用 DISTINCT 可以汇总不同的值。</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> <span class="keyword">AVG</span>(<span class="keyword">DISTINCT</span> col1) <span class="keyword">AS</span> avg_col</span><br><span class="line"><span class="keyword">FROM</span> mytable;</span><br></pre></td></tr></table></figure><h2 id="文本处理"><a href="#文本处理" class="headerlink" title="文本处理"></a>文本处理</h2><table><thead><tr><th align="center">函数</th><th align="center">说明</th></tr></thead><tbody><tr><td align="center">LEFT()</td><td align="center">左边的字符</td></tr><tr><td align="center">RIGHT()</td><td align="center">右边的字符</td></tr><tr><td align="center">LOWER()</td><td align="center">转换为小写字符</td></tr><tr><td align="center">UPPER()</td><td align="center">转换为大写字符</td></tr><tr><td align="center">LTRIM()</td><td align="center">去除左边的空格</td></tr><tr><td align="center">RTRIM()</td><td align="center">去除右边的空格</td></tr><tr><td align="center">LENGTH()</td><td align="center">长度</td></tr><tr><td align="center">SOUNDEX()</td><td align="center">转换为语音值</td></tr></tbody></table><p>其中， <strong>SOUNDEX()</strong> 可以将一个字符串转换为描述其语音表示的字母数字模式。</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> *</span><br><span class="line"><span class="keyword">FROM</span> mytable</span><br><span class="line"><span class="keyword">WHERE</span> <span class="keyword">SOUNDEX</span>(col1) = <span class="keyword">SOUNDEX</span>(<span class="string">&#x27;apple&#x27;</span>)</span><br></pre></td></tr></table></figure><h2 id="日期和时间处理"><a href="#日期和时间处理" class="headerlink" title="日期和时间处理"></a>日期和时间处理</h2><ul><li>日期格式：YYYY-MM-DD</li><li>时间格式：HH:<zero-width space>MM:SS</li></ul><table><thead><tr><th align="center">函 数</th><th align="center">说 明</th></tr></thead><tbody><tr><td align="center">ADDDATE()</td><td align="center">增加一个日期（天、周等）</td></tr><tr><td align="center">ADDTIME()</td><td align="center">增加一个时间（时、分等）</td></tr><tr><td align="center">CURDATE()</td><td align="center">返回当前日期</td></tr><tr><td align="center">CURTIME()</td><td align="center">返回当前时间</td></tr><tr><td align="center">DATE()</td><td align="center">返回日期时间的日期部分</td></tr><tr><td align="center">DATEDIFF()</td><td align="center">计算两个日期之差</td></tr><tr><td align="center">DATE_ADD()</td><td align="center">高度灵活的日期运算函数</td></tr><tr><td align="center">DATE_FORMAT()</td><td align="center">返回一个格式化的日期或时间串</td></tr><tr><td align="center">DAY()</td><td align="center">返回一个日期的天数部分</td></tr><tr><td align="center">DAYOFWEEK()</td><td align="center">对于一个日期，返回对应的星期几</td></tr><tr><td align="center">HOUR()</td><td align="center">返回一个时间的小时部分</td></tr><tr><td align="center">MINUTE()</td><td align="center">返回一个时间的分钟部分</td></tr><tr><td align="center">MONTH()</td><td align="center">返回一个日期的月份部分</td></tr><tr><td align="center">NOW()</td><td align="center">返回当前日期和时间</td></tr><tr><td align="center">SECOND()</td><td align="center">返回一个时间的秒部分</td></tr><tr><td align="center">TIME()</td><td align="center">返回一个日期时间的时间部分</td></tr><tr><td align="center">YEAR()</td><td align="center">返回一个日期的年份部分</td></tr></tbody></table><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">mysql&gt; SELECT NOW();</span><br><span class="line">2018-4-14 20:25:11</span><br></pre></td></tr></table></figure><h2 id="数值处理"><a href="#数值处理" class="headerlink" title="数值处理"></a>数值处理</h2><table><thead><tr><th align="center">函数</th><th align="center">说明</th></tr></thead><tbody><tr><td align="center">SIN()</td><td align="center">正弦</td></tr><tr><td align="center">COS()</td><td align="center">余弦</td></tr><tr><td align="center">TAN()</td><td align="center">正切</td></tr><tr><td align="center">ABS()</td><td align="center">绝对值</td></tr><tr><td align="center">SQRT()</td><td align="center">平方根</td></tr><tr><td align="center">MOD()</td><td align="center">余数</td></tr><tr><td align="center">EXP()</td><td align="center">指数</td></tr><tr><td align="center">PI()</td><td align="center">圆周率</td></tr><tr><td align="center">RAND()</td><td align="center">随机数</td></tr></tbody></table><h1 id="分组"><a href="#分组" class="headerlink" title="分组"></a>分组</h1><p>把具有相同的数据值的行放在同一组中。</p><p>可以对同一分组数据使用汇总函数进行处理，例如求分组数据的平均值等。</p><p>指定的分组字段除了能按该字段进行分组，也会自动按该字段进行排序。</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> <span class="keyword">col</span>, <span class="keyword">COUNT</span>(*) <span class="keyword">AS</span> <span class="keyword">num</span></span><br><span class="line"><span class="keyword">FROM</span> mytable</span><br><span class="line"><span class="keyword">GROUP</span> <span class="keyword">BY</span> <span class="keyword">col</span>;</span><br></pre></td></tr></table></figure><p>GROUP BY 自动按分组字段进行排序，ORDER BY 也可以按汇总字段来进行排序。</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> <span class="keyword">col</span>, <span class="keyword">COUNT</span>(*) <span class="keyword">AS</span> <span class="keyword">num</span></span><br><span class="line"><span class="keyword">FROM</span> mytable</span><br><span class="line"><span class="keyword">GROUP</span> <span class="keyword">BY</span> <span class="keyword">col</span></span><br><span class="line"><span class="keyword">ORDER</span> <span class="keyword">BY</span> <span class="keyword">num</span>;</span><br></pre></td></tr></table></figure><p>WHERE 过滤行，HAVING 过滤分组，行过滤应当先于分组过滤。</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> <span class="keyword">col</span>, <span class="keyword">COUNT</span>(*) <span class="keyword">AS</span> <span class="keyword">num</span></span><br><span class="line"><span class="keyword">FROM</span> mytable</span><br><span class="line"><span class="keyword">WHERE</span> <span class="keyword">col</span> &gt; <span class="number">2</span></span><br><span class="line"><span class="keyword">GROUP</span> <span class="keyword">BY</span> <span class="keyword">col</span></span><br><span class="line"><span class="keyword">HAVING</span> <span class="keyword">num</span> &gt;= <span class="number">2</span>;</span><br></pre></td></tr></table></figure><p>分组规定：</p><ul><li>GROUP BY 子句出现在 WHERE 子句之后，ORDER BY 子句之前；</li><li>除了汇总字段外，SELECT 语句中的每一字段都必须在 GROUP BY 子句中给出；</li><li>NULL 的行会单独分为一组；</li><li>大多数 SQL 实现不支持 GROUP BY 列具有可变长度的数据类型。</li></ul><h1 id="子查询"><a href="#子查询" class="headerlink" title="子查询"></a>子查询</h1><p>子查询中只能返回一个字段的数据。</p><p>可以将子查询的结果作为 WHRER 语句的过滤条件：</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> *</span><br><span class="line"><span class="keyword">FROM</span> mytable1</span><br><span class="line"><span class="keyword">WHERE</span> col1 <span class="keyword">IN</span> (<span class="keyword">SELECT</span> col2</span><br><span class="line">               <span class="keyword">FROM</span> mytable2);</span><br></pre></td></tr></table></figure><p>下面的语句可以检索出客户的订单数量，子查询语句会对第一个查询检索出的每个客户执行一次：</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> cust_name, (<span class="keyword">SELECT</span> <span class="keyword">COUNT</span>(*)</span><br><span class="line">                   <span class="keyword">FROM</span> Orders</span><br><span class="line">                   <span class="keyword">WHERE</span> Orders.cust_id = Customers.cust_id)</span><br><span class="line">                   <span class="keyword">AS</span> orders_num</span><br><span class="line"><span class="keyword">FROM</span> Customers</span><br><span class="line"><span class="keyword">ORDER</span> <span class="keyword">BY</span> cust_name;</span><br></pre></td></tr></table></figure><h1 id="连接"><a href="#连接" class="headerlink" title="连接"></a>连接</h1><p>连接用于连接多个表，使用 JOIN 关键字，并且条件语句使用 ON 而不是 WHERE。</p><p>连接可以替换子查询，并且比子查询的效率一般会更快。</p><p>可以用 AS 给列名、计算字段和表名取别名，给表名取别名是为了简化 SQL 语句以及连接相同表。</p><h2 id="内连接"><a href="#内连接" class="headerlink" title="内连接"></a>内连接</h2><p>内连接又称等值连接，使用 INNER JOIN 关键字。</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> A.value, B.value</span><br><span class="line"><span class="keyword">FROM</span> tablea <span class="keyword">AS</span> A <span class="keyword">INNER</span> <span class="keyword">JOIN</span> tableb <span class="keyword">AS</span> B</span><br><span class="line"><span class="keyword">ON</span> A.key = B.key;</span><br></pre></td></tr></table></figure><p>可以不明确使用 INNER JOIN，而使用普通查询并在 WHERE 中将两个表中要连接的列用等值方法连接起来。</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> A.value, B.value</span><br><span class="line"><span class="keyword">FROM</span> tablea <span class="keyword">AS</span> A, tableb <span class="keyword">AS</span> B</span><br><span class="line"><span class="keyword">WHERE</span> A.key = B.key;</span><br></pre></td></tr></table></figure><h2 id="自连接"><a href="#自连接" class="headerlink" title="自连接"></a>自连接</h2><p>自连接可以看成内连接的一种，只是连接的表是自身而已。</p><p>一张员工表，包含员工姓名和员工所属部门，要找出与 Jim 处在同一部门的所有员工姓名。</p><p>子查询版本</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> <span class="keyword">name</span></span><br><span class="line"><span class="keyword">FROM</span> employee</span><br><span class="line"><span class="keyword">WHERE</span> department = (</span><br><span class="line">      <span class="keyword">SELECT</span> department</span><br><span class="line">      <span class="keyword">FROM</span> employee</span><br><span class="line">      <span class="keyword">WHERE</span> <span class="keyword">name</span> = <span class="string">&quot;Jim&quot;</span>);</span><br></pre></td></tr></table></figure><p>自连接版本</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> e1.name</span><br><span class="line"><span class="keyword">FROM</span> employee <span class="keyword">AS</span> e1 <span class="keyword">INNER</span> <span class="keyword">JOIN</span> employee <span class="keyword">AS</span> e2</span><br><span class="line"><span class="keyword">ON</span> e1.department = e2.department</span><br><span class="line">      <span class="keyword">AND</span> e2.name = <span class="string">&quot;Jim&quot;</span>;</span><br></pre></td></tr></table></figure><h2 id="自然连接"><a href="#自然连接" class="headerlink" title="自然连接"></a>自然连接</h2><p>自然连接是把同名列通过等值测试连接起来的，同名列可以有多个。</p><p>内连接和自然连接的区别：内连接提供连接的列，而自然连接自动连接所有同名列。</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> A.value, B.value</span><br><span class="line"><span class="keyword">FROM</span> tablea <span class="keyword">AS</span> A <span class="keyword">NATURAL</span> <span class="keyword">JOIN</span> tableb <span class="keyword">AS</span> B;</span><br></pre></td></tr></table></figure><h2 id="外连接"><a href="#外连接" class="headerlink" title="外连接"></a>外连接</h2><p>外连接保留了没有关联的那些行。分为左外连接，右外连接以及全外连接，左外连接就是保留左表没有关联的行。</p><p>检索所有顾客的订单信息，包括还没有订单信息的顾客。</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> Customers.cust_id, Customer.cust_name, Orders.order_id</span><br><span class="line"><span class="keyword">FROM</span> Customers <span class="keyword">LEFT</span> <span class="keyword">OUTER</span> <span class="keyword">JOIN</span> Orders</span><br><span class="line"><span class="keyword">ON</span> Customers.cust_id = Orders.cust_id;</span><br></pre></td></tr></table></figure><p>customers 表：</p><table><thead><tr><th align="center">cust_id</th><th align="center">cust_name</th></tr></thead><tbody><tr><td align="center">1</td><td align="center">a</td></tr><tr><td align="center">2</td><td align="center">b</td></tr><tr><td align="center">3</td><td align="center">c</td></tr></tbody></table><p>orders 表：</p><table><thead><tr><th align="center">order_id</th><th align="center">cust_id</th></tr></thead><tbody><tr><td align="center">1</td><td align="center">1</td></tr><tr><td align="center">2</td><td align="center">1</td></tr><tr><td align="center">3</td><td align="center">3</td></tr><tr><td align="center">4</td><td align="center">3</td></tr></tbody></table><p>结果：</p><table><thead><tr><th align="center">cust_id</th><th align="center">cust_name</th><th align="center">order_id</th></tr></thead><tbody><tr><td align="center">1</td><td align="center">a</td><td align="center">1</td></tr><tr><td align="center">1</td><td align="center">a</td><td align="center">2</td></tr><tr><td align="center">3</td><td align="center">c</td><td align="center">3</td></tr><tr><td align="center">3</td><td align="center">c</td><td align="center">4</td></tr><tr><td align="center">2</td><td align="center">b</td><td align="center">Null</td></tr></tbody></table><h1 id="组合查询"><a href="#组合查询" class="headerlink" title="组合查询"></a>组合查询</h1><p>使用 <strong>UNION</strong> 来组合两个查询，如果第一个查询返回 M 行，第二个查询返回 N 行，那么组合查询的结果一般为 M+N 行。</p><p>每个查询必须包含相同的列、表达式和聚集函数。</p><p>默认会去除相同行，如果需要保留相同行，使用 UNION ALL。</p><p>只能包含一个 ORDER BY 子句，并且必须位于语句的最后。</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> <span class="keyword">col</span></span><br><span class="line"><span class="keyword">FROM</span> mytable</span><br><span class="line"><span class="keyword">WHERE</span> <span class="keyword">col</span> = <span class="number">1</span></span><br><span class="line"><span class="keyword">UNION</span></span><br><span class="line"><span class="keyword">SELECT</span> <span class="keyword">col</span></span><br><span class="line"><span class="keyword">FROM</span> mytable</span><br><span class="line"><span class="keyword">WHERE</span> <span class="keyword">col</span> =<span class="number">2</span>;</span><br></pre></td></tr></table></figure><h1 id="视图"><a href="#视图" class="headerlink" title="视图"></a>视图</h1><p>视图是虚拟的表，本身不包含数据，也就不能对其进行索引操作。</p><p>对视图的操作和对普通表的操作一样。</p><p>视图具有如下好处：</p><ul><li>简化复杂的 SQL 操作，比如复杂的连接；</li><li>只使用实际表的一部分数据；</li><li>通过只给用户访问视图的权限，保证数据的安全性；</li><li>更改数据格式和表示。</li></ul><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">CREATE</span> <span class="keyword">VIEW</span> myview <span class="keyword">AS</span></span><br><span class="line"><span class="keyword">SELECT</span> <span class="keyword">Concat</span>(col1, col2) <span class="keyword">AS</span> concat_col, col3*col4 <span class="keyword">AS</span> compute_col</span><br><span class="line"><span class="keyword">FROM</span> mytable</span><br><span class="line"><span class="keyword">WHERE</span> col5 = val;</span><br></pre></td></tr></table></figure><h1 id="存储过程"><a href="#存储过程" class="headerlink" title="存储过程"></a>存储过程</h1><p>存储过程可以看成是对一系列 SQL 操作的批处理。</p><p>使用存储过程的好处：</p><ul><li>代码封装，保证了一定的安全性；</li><li>代码复用；</li><li>由于是预先编译，因此具有很高的性能。</li></ul><p>命令行中创建存储过程需要自定义分隔符，因为命令行是以 ; 为结束符，而存储过程中也包含了分号，因此会错误把这部分分号当成是结束符，造成语法错误。</p><p>包含 in、out 和 inout 三种参数。</p><p>给变量赋值都需要用 select into 语句。</p><p>每次只能给一个变量赋值，不支持集合的操作。</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line">delimiter //</span><br><span class="line"></span><br><span class="line"><span class="keyword">create</span> <span class="keyword">procedure</span> myprocedure( <span class="keyword">out</span> ret <span class="built_in">int</span> )</span><br><span class="line">    <span class="keyword">begin</span></span><br><span class="line">        <span class="keyword">declare</span> y <span class="built_in">int</span>;</span><br><span class="line">        <span class="keyword">select</span> <span class="keyword">sum</span>(col1)</span><br><span class="line">        <span class="keyword">from</span> mytable</span><br><span class="line">        <span class="keyword">into</span> y;</span><br><span class="line">        <span class="keyword">select</span> y*y <span class="keyword">into</span> ret;</span><br><span class="line">    <span class="keyword">end</span> //</span><br><span class="line"></span><br><span class="line">delimiter ;</span><br><span class="line"><span class="keyword">call</span> myprocedure(@ret);</span><br><span class="line"><span class="keyword">select</span> @ret;</span><br></pre></td></tr></table></figure><h1 id="游标"><a href="#游标" class="headerlink" title="游标"></a>游标</h1><p>在存储过程中使用游标可以对一个结果集进行移动遍历。</p><p>游标主要用于交互式应用，其中用户需要对数据集中的任意行进行浏览和修改。</p><p>使用游标的四个步骤：</p><ol><li>声明游标，这个过程没有实际检索出数据；</li><li>打开游标；</li><li>取出数据；</li><li>关闭游标；</li></ol><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br></pre></td><td class="code"><pre><span class="line">delimiter //</span><br><span class="line"><span class="keyword">create</span> <span class="keyword">procedure</span> myprocedure(<span class="keyword">out</span> ret <span class="built_in">int</span>)</span><br><span class="line">    <span class="keyword">begin</span></span><br><span class="line">        <span class="keyword">declare</span> done <span class="built_in">boolean</span> <span class="keyword">default</span> <span class="number">0</span>;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">declare</span> mycursor <span class="keyword">cursor</span> <span class="keyword">for</span></span><br><span class="line">        <span class="keyword">select</span> col1 <span class="keyword">from</span> mytable;</span><br><span class="line">        <span class="comment">#  定义了一个 continue handler，当 sqlstate &#x27;02000&#x27; 这个条件出现时，会执行 set done = 1</span></span><br><span class="line">        <span class="keyword">declare</span> continue <span class="keyword">handler</span> <span class="keyword">for</span> <span class="keyword">sqlstate</span> <span class="string">&#x27;02000&#x27;</span> <span class="keyword">set</span> done = <span class="number">1</span>;</span><br><span class="line"></span><br><span class="line">        open mycursor;</span><br><span class="line"></span><br><span class="line">        repeat</span><br><span class="line">            fetch mycursor into ret;</span><br><span class="line">            <span class="keyword">select</span> ret;</span><br><span class="line">        until done <span class="keyword">end</span> <span class="keyword">repeat</span>;</span><br><span class="line"></span><br><span class="line">        close mycursor;</span><br><span class="line">    <span class="keyword">end</span> //</span><br><span class="line"> delimiter ;</span><br></pre></td></tr></table></figure><h1 id="触发器"><a href="#触发器" class="headerlink" title="触发器"></a>触发器</h1><p>触发器会在某个表执行以下语句时而自动执行：DELETE、INSERT、UPDATE。</p><p>触发器必须指定在语句执行之前还是之后自动执行，之前执行使用 BEFORE 关键字，之后执行使用 AFTER 关键字。BEFORE 用于数据验证和净化，AFTER 用于审计跟踪，将修改记录到另外一张表中。</p><p>INSERT 触发器包含一个名为 NEW 的虚拟表。</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">CREATE</span> <span class="keyword">TRIGGER</span> mytrigger <span class="keyword">AFTER</span> <span class="keyword">INSERT</span> <span class="keyword">ON</span> mytable</span><br><span class="line"><span class="keyword">FOR</span> <span class="keyword">EACH</span> <span class="keyword">ROW</span> <span class="keyword">SELECT</span> NEW.col <span class="keyword">into</span> @<span class="keyword">result</span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">SELECT</span> @<span class="keyword">result</span>; <span class="comment">-- 获取结果</span></span><br></pre></td></tr></table></figure><p>DELETE 触发器包含一个名为 OLD 的虚拟表，并且是只读的。</p><p>UPDATE 触发器包含一个名为 NEW 和一个名为 OLD 的虚拟表，其中 NEW 是可以被修改的，而 OLD 是只读的。</p><p>MySQL 不允许在触发器中使用 CALL 语句，也就是不能调用存储过程。</p><h1 id="事务管理"><a href="#事务管理" class="headerlink" title="事务管理"></a>事务管理</h1><p>基本术语：</p><ul><li>事务（transaction）指一组 SQL 语句；</li><li>回退（rollback）指撤销指定 SQL 语句的过程；</li><li>提交（commit）指将未存储的 SQL 语句结果写入数据库表；</li><li>保留点（savepoint）指事务处理中设置的临时占位符（placeholder），你可以对它发布回退（与回退整个事务处理不同）。</li></ul><p>不能回退 SELECT 语句，回退 SELECT 语句也没意义；也不能回退 CREATE 和 DROP 语句。</p><p>MySQL 的事务提交默认是隐式提交，每执行一条语句就把这条语句当成一个事务然后进行提交。当出现 START TRANSACTION 语句时，会关闭隐式提交；当 COMMIT 或 ROLLBACK 语句执行后，事务会自动关闭，重新恢复隐式提交。</p><p>设置 autocommit 为 0 可以取消自动提交；autocommit 标记是针对每个连接而不是针对服务器的。</p><p>如果没有设置保留点，ROLLBACK 会回退到 START TRANSACTION 语句处；如果设置了保留点，并且在 ROLLBACK 中指定该保留点，则会回退到该保留点。</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">START</span> <span class="keyword">TRANSACTION</span></span><br><span class="line">// ...</span><br><span class="line"><span class="keyword">SAVEPOINT</span> delete1</span><br><span class="line">// ...</span><br><span class="line"><span class="keyword">ROLLBACK</span> <span class="keyword">TO</span> delete1</span><br><span class="line">// ...</span><br><span class="line"><span class="keyword">COMMIT</span></span><br></pre></td></tr></table></figure><h1 id="字符集"><a href="#字符集" class="headerlink" title="字符集"></a>字符集</h1><p>基本术语：</p><ul><li>字符集为字母和符号的集合；</li><li>编码为某个字符集成员的内部表示；</li><li>校对字符指定如何比较，主要用于排序和分组。</li></ul><p>除了给表指定字符集和校对外，也可以给列指定：</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">CREATE</span> <span class="keyword">TABLE</span> mytable</span><br><span class="line">(<span class="keyword">col</span> <span class="built_in">VARCHAR</span>(<span class="number">10</span>) <span class="built_in">CHARACTER</span> <span class="keyword">SET</span> latin <span class="keyword">COLLATE</span> latin1_general_ci )</span><br><span class="line"><span class="keyword">DEFAULT</span> <span class="built_in">CHARACTER</span> <span class="keyword">SET</span> hebrew <span class="keyword">COLLATE</span> hebrew_general_ci;</span><br></pre></td></tr></table></figure><p>可以在排序、分组时指定校对：</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> *</span><br><span class="line"><span class="keyword">FROM</span> mytable</span><br><span class="line"><span class="keyword">ORDER</span> <span class="keyword">BY</span> <span class="keyword">col</span> <span class="keyword">COLLATE</span> latin1_general_ci;</span><br></pre></td></tr></table></figure><h1 id="权限管理"><a href="#权限管理" class="headerlink" title="权限管理"></a>权限管理</h1><p>MySQL 的账户信息保存在 mysql 这个数据库中。</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">USE</span> mysql;</span><br><span class="line"><span class="keyword">SELECT</span> <span class="keyword">user</span> <span class="keyword">FROM</span> <span class="keyword">user</span>;</span><br></pre></td></tr></table></figure><p><strong>创建账户</strong></p><p>新创建的账户没有任何权限。</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">CREATE</span> <span class="keyword">USER</span> myuser <span class="keyword">IDENTIFIED</span> <span class="keyword">BY</span> <span class="string">&#x27;mypassword&#x27;</span>;</span><br></pre></td></tr></table></figure><p><strong>修改账户名</strong></p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">RENAME</span> <span class="keyword">USER</span> myuser <span class="keyword">TO</span> newuser;</span><br></pre></td></tr></table></figure><p><strong>删除账户</strong></p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">DROP</span> <span class="keyword">USER</span> myuser;</span><br></pre></td></tr></table></figure><p><strong>查看权限</strong></p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">SHOW</span> <span class="keyword">GRANTS</span> <span class="keyword">FOR</span> myuser;</span><br></pre></td></tr></table></figure><p><strong>授予权限</strong></p><p>账户用 username@host 的形式定义，username@% 使用的是默认主机名。</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">GRANT</span> <span class="keyword">SELECT</span>, <span class="keyword">INSERT</span> <span class="keyword">ON</span> mydatabase.* <span class="keyword">TO</span> myuser;</span><br></pre></td></tr></table></figure><p><strong>删除权限</strong></p><p>GRANT 和 REVOKE 可在几个层次上控制访问权限：</p><ul><li>整个服务器，使用 GRANT ALL 和 REVOKE ALL；</li><li>整个数据库，使用 ON database.*；</li><li>特定的表，使用 ON database.table；</li><li>特定的列；</li><li>特定的存储过程。</li></ul><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">REVOKE</span> <span class="keyword">SELECT</span>, <span class="keyword">INSERT</span> <span class="keyword">ON</span> mydatabase.* <span class="keyword">FROM</span> myuser;</span><br></pre></td></tr></table></figure><p><strong>更改密码</strong></p><p>必须使用 Password() 函数进行加密。</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">SET</span> PASSWROD <span class="keyword">FOR</span> myuser = <span class="keyword">Password</span>(<span class="string">&#x27;new_password&#x27;</span>);</span><br></pre></td></tr></table></figure>]]></content>
    
    
      
      
    <summary type="html">&lt;p&gt;&lt;img src=&quot;https://ifwant-pic-1303131123.cos.ap-nanjing.myqcloud.com/photo/20180718003208394.png&quot;&gt;&lt;/p&gt;
&lt;h1 id=&quot;基础&quot;&gt;&lt;a href=&quot;#基础&quot; class=&quot;he</summary>
      
    
    
    
    
    <category term="学习" scheme="https://ifwant.tech/tags/%E5%AD%A6%E4%B9%A0/"/>
    
    <category term="数据库" scheme="https://ifwant.tech/tags/%E6%95%B0%E6%8D%AE%E5%BA%93/"/>
    
  </entry>
  
  <entry>
    <title>计算机网络</title>
    <link href="https://ifwant.tech/2021/01/07/mysql/"/>
    <id>https://ifwant.tech/2021/01/07/mysql/</id>
    <published>2021-01-07T08:48:39.000Z</published>
    <updated>2021-01-09T07:19:48.000Z</updated>
    
    <content type="html"><![CDATA[<p><img src="https://ifwant-pic-1303131123.cos.ap-nanjing.myqcloud.com/photo/640.png"></p><h2 id="HTTP"><a href="#HTTP" class="headerlink" title="HTTP"></a>HTTP</h2><h3 id="HTTP的报文结构"><a href="#HTTP的报文结构" class="headerlink" title="HTTP的报文结构"></a>HTTP的报文结构</h3><p><img src="https://ifwant-pic-1303131123.cos.ap-nanjing.myqcloud.com/photo/01083.jpg" alt="img"></p><h3 id="HTTP请求种类"><a href="#HTTP请求种类" class="headerlink" title="HTTP请求种类"></a>HTTP请求种类</h3><ol><li><strong>get：</strong>获取资源(多媒体，程序执行结果)</li><li><strong>post：</strong>传输实体主体，相比于 <code>get</code> 传输，<code>post</code> 请求的数据会放置在内容实体中而不是 <code>uri(Universal Resource Identifier)</code> 之后，安全性大大增强</li><li><strong>put：</strong>传输文件，由于 <code>http1.1</code> 本身不自带验证机制，所以安全性欠佳。一般 <code>web</code> 不使用此方法</li><li><strong>head：</strong>获得报文首部，与 <code>get</code> 一样，只是不获取报文主体，用来验证 <code>uri</code> 的有效性</li><li><strong>delete：</strong>删除文件 类似 <code>put</code></li><li><strong>options：</strong>询问支持的方法</li><li><strong>trace：</strong>追踪路径，让 <code>web</code> 服务器端将之前的请求通信环回给客户端，用来确认连接过程中，代理中转的加工/篡改。</li><li><strong>connect：</strong>要求用隧道协议连接代理，主要使用 <code>ssl</code> 和 <code>tsl</code> 把通信内容加密后经网络隧道传输</li></ol><hr><h3 id="HTTP状态码"><a href="#HTTP状态码" class="headerlink" title="HTTP状态码"></a>HTTP状态码</h3><p>HTTP状态码由三个十进制数字组成，第一个十进制数字定义了状态码的类型，后两个数字没有分类的作用。HTTP状态码共分为5种类型：</p><p><strong>1信息通知，2成功，3暂时没成功，但可以重定向，45是失败，4是客户端错误，5是服务器错误</strong></p><table><thead><tr><th align="left">分类</th><th align="left">分类描述</th></tr></thead><tbody><tr><td align="left">1**</td><td align="left">信息，服务器收到请求，需要请求者继续执行操作</td></tr><tr><td align="left">2**</td><td align="left">成功，操作被成功接收并处理</td></tr><tr><td align="left">3**</td><td align="left">重定向，需要进一步的操作以完成请求</td></tr><tr><td align="left">4**</td><td align="left">客户端错误，请求包含语法错误或无法完成请求</td></tr><tr><td align="left">5**</td><td align="left">服务器错误，服务器在处理请求的过程中发生了错误</td></tr></tbody></table><p><strong>HTTP状态码列表:</strong></p><table><thead><tr><th align="left">状态码</th><th align="left">状态码英文名称</th><th align="left">中文描述</th></tr></thead><tbody><tr><td align="left">100</td><td align="left">Continue</td><td align="left">继续。<a href="http://www.dreamdu.com/webbuild/client_vs_server/">客户端</a>应继续其请求</td></tr><tr><td align="left">101</td><td align="left">Switching Protocols</td><td align="left">切换协议。服务器根据客户端的请求切换协议。只能切换到更高级的协议，例如，切换到HTTP的新版本协议</td></tr><tr><td align="left"></td><td align="left"></td><td align="left"></td></tr><tr><td align="left">200</td><td align="left">OK</td><td align="left">请求成功。一般用于GET与POST请求</td></tr><tr><td align="left">201</td><td align="left">Created</td><td align="left">已创建。成功请求并创建了新的资源</td></tr><tr><td align="left">202</td><td align="left">Accepted</td><td align="left">已接受。已经接受请求，但未处理完成</td></tr><tr><td align="left">203</td><td align="left">Non-Authoritative Information</td><td align="left">非授权信息。请求成功。但返回的meta信息不在原始的服务器，而是一个副本</td></tr><tr><td align="left">204</td><td align="left">No Content</td><td align="left">无内容。服务器成功处理，但未返回内容。在未更新网页的情况下，可确保浏览器继续显示当前文档</td></tr><tr><td align="left">205</td><td align="left">Reset Content</td><td align="left">重置内容。服务器处理成功，用户终端（例如：浏览器）应重置文档视图。可通过此返回码清除浏览器的表单域</td></tr><tr><td align="left">206</td><td align="left">Partial Content</td><td align="left">部分内容。服务器成功处理了部分GET请求</td></tr><tr><td align="left"></td><td align="left"></td><td align="left"></td></tr><tr><td align="left">300</td><td align="left">Multiple Choices</td><td align="left">多种选择。请求的资源可包括多个位置，相应可返回一个资源特征与地址的列表用于用户终端（例如：浏览器）选择</td></tr><tr><td align="left">301</td><td align="left">Moved Permanently</td><td align="left">永久移动。请求的资源已被永久的移动到新URI，返回信息会包括新的URI，浏览器会自动定向到新URI。今后任何新的请求都应使用新的URI代替</td></tr><tr><td align="left">302</td><td align="left">Found</td><td align="left">临时移动。与301类似。但资源只是临时被移动。客户端应继续使用原有URI</td></tr><tr><td align="left">303</td><td align="left">See Other</td><td align="left">查看其它地址。与301类似。使用GET和POST请求查看</td></tr><tr><td align="left">304</td><td align="left">Not Modified</td><td align="left">未修改。所请求的资源未修改，服务器返回此状态码时，不会返回任何资源。客户端通常会缓存访问过的资源，通过提供一个头信息指出客户端希望只返回在指定日期之后修改的资源</td></tr><tr><td align="left">305</td><td align="left">Use Proxy</td><td align="left">使用代理。所请求的资源必须通过代理访问</td></tr><tr><td align="left">306</td><td align="left">Unused</td><td align="left">已经被废弃的HTTP状态码</td></tr><tr><td align="left">307</td><td align="left">Temporary Redirect</td><td align="left">临时重定向。与302类似。使用GET请求重定向</td></tr><tr><td align="left"></td><td align="left"></td><td align="left"></td></tr><tr><td align="left">400</td><td align="left">Bad Request</td><td align="left">客户端请求的语法错误，服务器无法理解</td></tr><tr><td align="left">401</td><td align="left">Unauthorized</td><td align="left">请求要求用户的身份认证</td></tr><tr><td align="left">402</td><td align="left">Payment Required</td><td align="left">保留，将来使用</td></tr><tr><td align="left">403</td><td align="left">Forbidden</td><td align="left">服务器理解请求客户端的请求，但是拒绝执行此请求</td></tr><tr><td align="left">404</td><td align="left">Not Found</td><td align="left">服务器无法根据客户端的请求找到资源（网页）。通过此代码，网站设计人员可设置”您所请求的资源无法找到”的个性页面</td></tr><tr><td align="left">405</td><td align="left">Method Not Allowed</td><td align="left">客户端请求中的方法被禁止</td></tr><tr><td align="left">406</td><td align="left">Not Acceptable</td><td align="left">服务器无法根据客户端请求的内容特性完成请求</td></tr><tr><td align="left">407</td><td align="left">Proxy Authentication Required</td><td align="left">请求要求代理的身份认证，与401类似，但请求者应当使用代理进行授权</td></tr><tr><td align="left">408</td><td align="left">Request Time-out</td><td align="left">服务器等待客户端发送的请求时间过长，超时</td></tr><tr><td align="left">409</td><td align="left">Conflict</td><td align="left">服务器完成客户端的 PUT 请求时可能返回此代码，服务器处理请求时发生了冲突</td></tr><tr><td align="left">410</td><td align="left">Gone</td><td align="left">客户端请求的资源已经不存在。410不同于404，如果资源以前有现在被永久删除了可使用410代码，网站设计人员可通过301代码指定资源的新位置</td></tr><tr><td align="left">411</td><td align="left">Length Required</td><td align="left">服务器无法处理客户端发送的不带Content-Length的请求信息</td></tr><tr><td align="left">412</td><td align="left">Precondition Failed</td><td align="left">客户端请求信息的先决条件错误</td></tr><tr><td align="left">413</td><td align="left">Request Entity Too Large</td><td align="left">由于请求的实体过大，服务器无法处理，因此拒绝请求。为防止客户端的连续请求，服务器可能会关闭连接。如果只是服务器暂时无法处理，则会包含一个Retry-After的响应信息</td></tr><tr><td align="left">414</td><td align="left">Request-URI Too Large</td><td align="left">请求的URI过长（URI通常为网址），服务器无法处理</td></tr><tr><td align="left">415</td><td align="left">Unsupported Media Type</td><td align="left">服务器无法处理请求附带的媒体格式</td></tr><tr><td align="left">416</td><td align="left">Requested range not satisfiable</td><td align="left">客户端请求的范围无效</td></tr><tr><td align="left">417</td><td align="left">Expectation Failed</td><td align="left">服务器无法满足Expect的请求头信息</td></tr><tr><td align="left"></td><td align="left"></td><td align="left"></td></tr><tr><td align="left">500</td><td align="left">Internal Server Error</td><td align="left">服务器内部错误，无法完成请求</td></tr><tr><td align="left">501</td><td align="left">Not Implemented</td><td align="left">服务器不支持请求的功能，无法完成请求</td></tr><tr><td align="left">502</td><td align="left">Bad Gateway</td><td align="left">作为网关或者代理工作的服务器尝试执行请求时，从远程服务器接收到了一个无效的响应</td></tr><tr><td align="left">503</td><td align="left">Service Unavailable</td><td align="left">由于超载或系统维护，服务器暂时的无法处理客户端的请求。延时的长度可包含在服务器的Retry-After头信息中</td></tr><tr><td align="left">504</td><td align="left">Gateway Time-out</td><td align="left">充当网关或代理的服务器，未及时从远端服务器获取请求</td></tr><tr><td align="left">505</td><td align="left">HTTP Version not supported</td><td align="left">服务器不支持请求的HTTP协议的版本，无法完成处理</td></tr></tbody></table><hr><h3 id="HTTP的首部字段"><a href="#HTTP的首部字段" class="headerlink" title="HTTP的首部字段"></a>HTTP的首部字段</h3><ul><li><p>通用字段：在请求头和响应头里都可以出现； </p></li><li><p>请求字段：仅能出现在请求头里，进一步说明请求信息或者额外的附加条件； </p></li><li><p>响应字段：仅能出现在响应头里，补充说明响应报文的信息； </p></li><li><p>实体字段：它实际上属于通用字段，但专门描述 body 的额外信息。</p></li></ul><h4 id="首部字段：缓存"><a href="#首部字段：缓存" class="headerlink" title="首部字段：缓存"></a>首部字段：缓存</h4><ul><li><p>Cache-Control：</p><ul><li><p>max-age：资源存储的最长时间</p></li><li><p>no-store：不允许缓存</p></li><li><p>no-cache：可以缓存，使用之前必须要去服务器验证是否过期</p></li><li><p>must-revalidate：资源过期（比如已经超过max-age），在成功向原始服务器验证之前，缓存不能用该资源响应后续请求</p><img src="https://ifwant-pic-1303131123.cos.ap-nanjing.myqcloud.com/photo/01084.jpg" style="zoom: 80%;" /></li></ul></li><li><p>条件缓存</p><ul><li><p>if-Modified-Since：指的是文件最后修改时间。在给定的日期时间之后，如果请求的资源没有改变，就返回304响应；如果有改变，返回资源</p><p><img src="https://ifwant-pic-1303131123.cos.ap-nanjing.myqcloud.com/photo/01085.jpg"></p></li><li><p>If-None-Match：对于 GET 和 HEAD请求方法来说，当且仅当服务器上没有任何资源的 ETag 属性值与这个首部中列出的相匹配的时候，服务器端会才返回所请求的资源，响应码为 200</p></li></ul></li></ul><hr><h3 id="HTTPS的通信过程⭐"><a href="#HTTPS的通信过程⭐" class="headerlink" title="HTTPS的通信过程⭐"></a>HTTPS的通信过程⭐</h3><p><code>HTTPS</code> 是一种通过计算机网络进行安全通信的传输协议。<code>HTTPS</code> 经由 <code>HTTP</code> 进行通信，但利用 <strong>SSL/TLS</strong> 来加密数据包。 <code>HTTPS</code> 的通信过程，充分利用了各种加密算法及其特性。<code>HTTPS</code> 只是在原有的协议接口替换成了<strong>SSL(Secure Socket Layer)**和</strong>TLS(Transport Layer Security)**协议</p><p><img src="https://ifwant-pic-1303131123.cos.ap-nanjing.myqcloud.com/photo/1627759-20190615112708629-638195014.jpg" alt="img"></p><ol><li><p>客户端或者浏览器向服务器发送 <code>https</code> 请求</p></li><li><p>服务器将数字证书(公钥+数字签名)返回给客户端</p></li><li><p>浏览器接收到服务器公钥</p><p>浏览器收到公钥后会对公钥进行一系列的验证，如：颁发机构，过期时间等等。认证失败会有警告，成功的话，<strong>会生成一个随机的对阵密钥作为对称加密的密钥，用服务器返回的公钥对这个随机密钥进行加密</strong></p></li><li><p>加密后的随机密钥发送到服务器</p><p>服务器收到公钥加密后的随机密钥，会拿自己的私钥去解，解开之后得到随机密钥</p></li><li><p>客户端与服务器进行加密通信</p><p>客户端和服务器端信息数据通信，以这个随机密钥进行对称加密。</p></li></ol><h4 id="非对称加密算法-对称加密算法"><a href="#非对称加密算法-对称加密算法" class="headerlink" title="非对称加密算法+对称加密算法"></a>非对称加密算法+对称加密算法</h4><p>HTTPS 采用的是<strong>对称加密</strong>和<strong>非对称加密</strong>结合的「混合加密」方式：</p><ul><li>在通信建立前采用<strong>非对称加密</strong>的方式交换「会话秘钥」，后续就不再使用非对称加密。</li><li>在通信过程中全部使用<strong>对称加密</strong>的「会话秘钥」的方式加密明文数据。</li></ul><p>非对称加密算法，它有两个密钥：一个是公钥，可以明文传输的。一个是私钥，由发送放保存，其它人都不知道。非对称加密算法的加密和解密算法不同。通过公钥加密的信息只有用私钥才能解开。</p><p>主要是由于非对称加密算法运算量大，用非对称加密算法进行通信，其加解密过程特别慢。因此可以第一次用非对称加密算法传递对称加密算法的密钥，后续通信就用对称加密算法。除了第一次用非对称加密算法加解密密钥的时候比较慢外，其它时间都不会慢了。而且也可以保证对称加密算法密钥的安全性。这样可以充分利用非对称加密算法和对称加密算法的各自特点。</p><h4 id="数字证书—防止中间人攻击"><a href="#数字证书—防止中间人攻击" class="headerlink" title="数字证书—防止中间人攻击"></a>数字证书—防止中间人攻击</h4><p>为了防止在通信过程中有中间人截获两端的非对称密钥，导致两端收到的公钥都是中间人的，可以获取两端的信息然后中转。找个可靠的人做公证人，两端各自把公钥交给他。由他颁布一个证书，证书包含公钥以及两端的身份信息，来证明两端各自的身份。但是这个证书也可以在传递过程中被篡改。</p><h4 id="数字摘要算法"><a href="#数字摘要算法" class="headerlink" title="数字摘要算法"></a>数字摘要算法</h4><p><strong>摘要算法</strong>用来实现<strong>完整性</strong>，能够为数据生成独一无二的「指纹」，用于校验数据的完整性，解决了篡改的风险。</p><p>为了解决数字证书中中间人攻击的问题，可以让公证人用<strong>数字摘要算法</strong>，把<strong>公钥和身份信息</strong>生成一个摘要。同时用非对称加密算法对摘要进行加密，生成数字签名。然后把【公钥和身份信息+数字签名】合并，形成数字证书。在获得数字证书的时候，就可以用公钥进行解密生成摘要信息，再用数字摘要算法对公钥和身份信息生成摘要信息，两者比对，如果能匹配，就说明没有被篡改 。</p><p>数字证书通常来说是由受信任的数字证书颁发机构<strong>CA</strong>，在验证服务器身份后颁发。</p><h4 id="证书内置"><a href="#证书内置" class="headerlink" title="证书内置"></a>证书内置</h4><p>为了防止公证人公钥传输过程也会出现中间人攻击问题，可以把公证人公钥预先加载在操作系统中。微软等公司会根据一些权威安全机构的评估选取一些信誉很好并且通过一定的安全认证的证书发布机构，把这些证书发布机构的证书默认安装在操作系统中，并且设置为操作系统信任的数字证书。</p><hr><h3 id="get-post-区别"><a href="#get-post-区别" class="headerlink" title="get/post 区别"></a>get/post 区别</h3><ul><li><code>GET</code> 用于信息获取，不会增删改服务器上的内容；<code>POST</code> 请求可能修改服务器上资源；</li><li><code>GET</code> 请求数据附在 <code>URL</code> 后面，而 <code>POST</code> 在 <code>HTTP</code> 包体内；</li><li><code>GET</code>方法产生一个TCP数据包，把首部和数据一起发送;<code>POST</code>方法产生两个TCP数据包，先发首部，服务器响应后再发数据 </li></ul><hr><h3 id="http-https-0-9、1-0、1-1、2-0、3-0的特点和区别"><a href="#http-https-0-9、1-0、1-1、2-0、3-0的特点和区别" class="headerlink" title="http/https 0.9、1.0、1.1、2.0、3.0的特点和区别"></a>http/https 0.9、1.0、1.1、2.0、3.0的特点和区别</h3><h4 id="Http0-9"><a href="#Http0-9" class="headerlink" title="Http0.9"></a>Http0.9</h4><ul><li><strong>请求方法支持有限</strong><br>只支持 <code>GET</code> 请求方式，不支持其他请求方式，因此客户端向服务端传输信息的量非常有限，也就是现在常用的 <code>Post</code> 请求无法使用</li><li><strong>不支持请求头header</strong><br>不能在请求中指定版本号，服务端只具有返回 <code>HTML</code> 字符串的能力</li><li><strong>响应即关闭</strong><br>服务端相响应之后，立即关闭 <code>TCP</code> 连接</li></ul><hr><h4 id="Http1-0"><a href="#Http1-0" class="headerlink" title="Http1.0"></a>Http1.0</h4><p>1.0版本主要是对0.9版本的强化，效果也比较明显，主要特性和缺点包括：</p><ul><li><strong>丰富请求方法</strong><br>请求方式新增了 <code>POST</code> ，<code>DELETE</code> ，<code>PUT</code> ，<code>HEADER</code> 等方式，提高了客户端向服务端发送信息的量级</li><li><strong>增加请求头和响应头</strong><br>增添了<strong>请求头</strong>和<strong>响应头</strong>的概念，可以在通信中指定了 <code>HTTP</code> 协议版本号，以及其他 <code>header</code> 信息，使得C/S交互更加灵活方便</li><li><strong>丰富数据传输内容</strong><br>扩充了传输内容格式包括：<strong>图片、音视频资源、二进制</strong>等都可以进行传输，相比0.9的只能传输 <code>html</code> 内容让<code>http</code> 的应用场景更多。</li></ul><p><strong>缺点如下：</strong></p><ul><li><strong>链接复用性差</strong><br>1.0版本中<strong>每个TCP连接只能发送一个请求</strong>，<strong>数据发送完毕连接就关闭</strong>，如果还要请求其他资源，就必须重新建立连接。TCP为了保证正确性和可靠性需要客户端和服务器三次握手和四次挥手，因此建立连接成本很高，基于拥塞控制开始时发送速率较慢，所以1.0版本的<strong>性能并不理想</strong>。</li><li><strong>无状态无连接的弊端</strong><br>1.0版本是<strong>无状态且无连接</strong>的，换句话说就是服务器不跟踪不记录请求过的状态，客户端<strong>每次请求都需要建立tcp连接不能复用</strong>，并且1.0规定在前一个请求响应到达之后下一个请求才能发送，如果前一个阻塞后面的请求就会被阻塞。 <strong>丢包和乱序问题</strong>和高成本的链接过程让复用和<strong>队头阻塞</strong>产生很多问题，所以<strong>无连接无状态</strong>是1.0版本的一个<strong>弱肋</strong>。</li></ul><h4 id="Http1-1"><a href="#Http1-1" class="headerlink" title="Http1.1"></a>Http1.1</h4><p>1.1版本在1.0版本发布后大约1年就推出了，是<strong>对1.0版本的优化和完善</strong>，1.1版本的主要特点包括：</p><ul><li><strong>增加长连接</strong><br>新增 <code>Connection</code> 字段，可以设置 <code>keep-alive</code> 值保持连接不断开，即 <code>TCP</code> 连接默认不关闭，可以被多个请求<strong>复用</strong>，这也是1.1版本很重要的优化，在请求首部字段中提出keepalive之后，只要任意一段没有明确提出断开连接，tcp就一直会保持连接状态。但是在S端服务器只有处理完一个回应，才会进行下一个回应。要是前面的回应特别慢，后面就会有许多请求排队等着，仍然存在队头阻塞问题。</li><li><strong>管道化</strong><br>在长连接的基础上，管道化可以不等第一个请求响应继续发送后面的请求，但响应的顺序还是按照请求的顺序返回，即在<strong>同一个TCP连接中，客户端可以同时发送多个请求</strong>，进一步改进了HTTP协议的传输效率。</li><li><strong>更多的请求方法</strong><br>增加了 <code>PUT</code>、 <code>PATCH</code>、<code>OPTIONS</code>、<code>DELETE</code> 等请求方式。</li><li><strong>host字段</strong><br><code>Host</code> 字段用来指定服务器的域名，这样就可以将多种请求发往同一台服务器上的不同网站，提高了机器的复用，这个也是重要的优化</li></ul><p><strong>缺点：</strong></p><ul><li>仍然存在着<strong>队首阻塞</strong>的问题。也就是虽然请求可以同时发送了，但是要是前面的回应特别慢，后面就会有许多请求排队等着，仍然存在队头阻塞问题。</li></ul><hr><h4 id="Http2-0"><a href="#Http2-0" class="headerlink" title="Http2.0"></a>Http2.0</h4><p><strong>HTTP2.0多路复用+TLS安全加密+TCP拥塞控制</strong></p><p>2.0版本是个里程碑式的版本，相比1.x版本有了非常多的优化去适应当前的网络场景，其中几个重要功能点包括：</p><ul><li><p><strong>二进制格式</strong><br><code>1.x</code> 是文本协议，然而2.0是<strong>以二进制帧为基本单位</strong>，可以说是一个二进制协议，将所有传输的信息分割为<strong>消息和帧</strong>，并采用二进制格式的编码，一帧中包含数据和标识符，使得网络传输变得高效而灵活。</p><ul><li><p>二进制分帧层binary framing layer在不修改请求方法和语义的基础上，重新设计了编码机制。二进制编码机制使得通信可以在单个TCP连接上进行，该连接在整个对话期间一直处于活跃状态。</p><ul><li><strong>链接Link</strong><br>就是指一条C/S之间的TCP链接，这是个基础的链路数据的高速公路</li><li><strong>数据流Stream</strong><br>已建立的TCP连接内的双向字节流，TCP链接中可以承载一条或多条消息</li><li><strong>消息Message</strong><br>消息属于一个数据流，消息就是逻辑请求或响应消息对应的完整的一系列帧，也就是帧组成了消息</li><li><strong>帧Frame</strong><br>帧是通信的最小单位，每个帧都包含帧头和消息体，标识出当前帧所属的数据流</li></ul></li></ul></li><li><p><strong>多路复用</strong><br>这是一个非常重要的改进，1.x中建立多个连接的消耗以及效率都存在问题，2.0版本的多路复用<strong>多个请求共用一个连接，多个请求可以同时在一个TCP连接上并发，主要借助于二进制帧中的标识进行区分实现链路的复用。</strong></p><ul><li><p>客户端和服务器将交互数据分解为<strong>相互独立的帧</strong>，互不影响地<strong>交错传输</strong>，最后再在对端根据<strong>帧头中的流标识符</strong>把它们<strong>重新组装</strong>起来，从而实现了TCP链接的多路复用。</p><p><img src="https://ifwant-pic-1303131123.cos.ap-nanjing.myqcloud.com/photo/a.jpg" alt="图片"></p></li></ul></li><li><p><strong>头部压缩</strong><br>2.0版本使用使用<strong>HPACK算法</strong>对头部 <code>header</code> 数据进行压缩，从而减少请求的大小提高效率，这个非常好理解，之前每次发送都要带相同的 <code>header</code> ，显得很冗余，<code>2.0</code> 版本对头部信息进行增量更新有效减少了头部数据的传输。</p><ul><li><p>2.0版本中HPACK算法在C/S中使用<strong>首部表</strong>来存储之前发送的键值对，对于相同的数据通信期间几乎不会改变的通用键值对只需发送一次即可。</p></li><li><p>极端情况如果请求头每次没有变化，那么传输中则不包含首部，也就是首部开销就是<strong>零字节</strong>。如果首部键值对发生变化了，也<strong>只需要发送变化的数据</strong>，并且将<strong>新增或修改的首部帧会被追加到首部表</strong>，首部表在链接存活期始终存在, 并且由客户端和服务器<strong>共同更新和维护</strong>。</p></li><li><p>简单说就是客户端和服务端共同维护了一个<strong>key-value</strong>的结构，发生变化时则更新传输，否则就不传输，这样相当于<strong>首次全量传输之后增量更新传输</strong>即可，这个思想在日常开发中也非常普遍，不用想的太复杂。</p><p><img src="https://ifwant-pic-1303131123.cos.ap-nanjing.myqcloud.com/photo/01082.jpg" alt="图片"></p></li></ul></li><li><p><strong>服务端推送</strong><br>这个功能有点意思，之前 <code>1.x</code> 版本服务端都是收到请求后被动执行，在2.0版本允许服务器主动向客户端发送资源，这样在客户端可以起到加速的作用。</p><ul><li>在实际的C/S交互中这种<strong>主动推送额外资源</strong>的方法很有效，因为几乎每个网络应用都会包含多种资源，客户端需要全部逐个获取它们，此时如果让服务器提前推送这些资源，从而可以<strong>有效减少额外的延迟时间</strong>，因为服务器可以知道客户端下一步要请求什么资源。</li></ul></li><li><p><strong>HTTP2.0存在的不足(主要是TCP的问题)</strong></p><ul><li>建立连接时间长</li><li>队头阻塞问题(HTTP2.0协议的<strong>多路复用机制</strong>解决了HTTP层的队头阻塞问题，但是在<strong>TCP层仍然存在队头阻塞问题</strong>。TCP协议在收到数据包之后，这部分数据可能是乱序到达的，但是TCP必须将所有数据收集排序整合后给上层使用，<strong>如果其中某个包丢失了，就必须等待重传，从而出现某个丢包数据阻塞整个连接的数据使用</strong>。)</li><li>移动互联网领域表现不佳(弱网环境)</li></ul></li></ul><hr><h4 id="Http3-0"><a href="#Http3-0" class="headerlink" title="Http3.0"></a>Http3.0</h4><p>HTTP3.0又称为<strong>HTTP Over QUIC</strong>，其<strong>弃用TCP协议</strong>，改为使用<strong>基于UDP协议</strong>的QUIC协议来实现。</p><ul><li><strong>QUIC</strong>协议(<strong>Quick UDP Internet Connections</strong>)</li></ul><blockquote><p>QUIC提高了当前正在使用TCP的面向连接的Web应用程序的性能。它在两个端点之间使用用户数据报协议（UDP）建立多个复用连接来实现此目的。</p></blockquote><blockquote><p>QUIC的次要目标包括减少连接和传输延迟，在每个方向进行带宽估计以避免拥塞。它还将拥塞控制算法移动到用户空间，而不是内核空间，此外使用前向纠错（FEC）进行扩展，以在出现错误时进一步提高性能。</p></blockquote><ul><li><strong>基于TCP协议和TLS协议的HTTP2.0在真正发送数据包之前需要花费一些时间来完成握手和加密协商</strong>，完成之后才可以真正传输业务数据。但是QUIC则第一个数据包就可以发业务数据，从而在连接延时有很大优势，可以节约数百毫秒的时间。</li><li><strong>使用QUIC协议的客户端和服务端要使用1RTT进行密钥交换，使用的交换算法是DH(Diffie-Hellman)迪菲-赫尔曼算法。首次连接之后传递了config包，里面包含了服务端公钥和两个随机数，客户端会将config存储下来，后续再连接时可以直接使用，从而跳过这个1RTT，实现0RTT的业务数据交互。</strong></li><li>QUIC协议基于UDP实现摒弃了五元组(SIP+SPort+DIP+DPort+协议号)的概念，使用64位的随机数作为连接的ID，并使用该ID表示连接。基于QUIC协议之下，我们在日常wifi和4G切换时，或者不同基站之间切换都不会重连，从而提高业务层的体验。</li><li>QUIC协议的核心思想是将TCP协议在内核实现的诸如可靠传输、流量控制、拥塞控制等功能转移到用户态来实现，同时在加密传输方向的尝试也推动了TLS1.3的发展。</li></ul><hr><h3 id="HTTP的无状态性"><a href="#HTTP的无状态性" class="headerlink" title="HTTP的无状态性"></a>HTTP的无状态性</h3><p><code>http</code> 是一种无状态协议，即<strong>不会保存请求和响应之间的通信状态</strong>。好处是<strong>可以加快处理速度，确保协议的可伸缩性</strong>。<code>http</code> 最初的设计是无状态的(stateless)，但是无状态的 <code>http</code> 无法满足互联网日益发展的需求，于是业界扩展了 <code>http</code> 协议，增加了有状态(stateful)协议头，使之变成一个有状态协议。</p><p>两种用于保持HTTP连接状态的技术就应运而生了，一个是Cookie，而另一个则是Session。HTTP本身是一个无状态的连接协议，为了支持客户端与服务器之间的交互，我们就需要通过不同的技术为交互存储状态，而这些不同的技术就是Cookie和Session了。</p><h4 id="Cookie"><a href="#Cookie" class="headerlink" title="Cookie"></a>Cookie</h4><p>由于HTTP是一种无状态的协议，服务器<strong>单纯从网络连接上</strong>无从知道客户身份。怎么办呢？就给客户端们颁发一个通行证吧，每人一个，无论谁访问都必须携带自己通行证。这样服务器就能从通行证上确认客户身份了。这就是Cookie的工作原理。</p><p>Cookie实际上是<strong>一小段的文本信息</strong>。客户端请求服务器，如果服务器需要记录该用户状态，就使用 <code>response</code> 向客户端浏览器颁发一个Cookie。客户端浏览器会把Cookie保存起来。当浏览器再请求该网站时，<strong>浏览器把请求的网址连同该Cookie一同提交给服务器</strong>。<strong>服务器检查该Cookie，以此来辨认用户状态。服务器还可以根据需要修改Cookie的内容。</strong></p><p><em>Cookie具有不可跨域名性</em></p><p>很多网站都会使用Cookie。例如，Google会向客户端颁发Cookie，Baidu也会向客户端颁发Cookie。那浏览器访问Google会不会也携带上Baidu颁发的Cookie呢？或者Google能不能修改Baidu颁发的Cookie呢？</p><p>答案是否定的。Cookie具有<strong>不可跨域名性</strong>。根据Cookie规范，浏览器访问Google只会携带Google的Cookie，而不会携带Baidu的Cookie。Google也只能操作Google的Cookie，而不能操作Baidu的Cookie。</p><p><strong>Cookie在客户端是由浏览器来管理的</strong>。浏览器能够保证Google只会操作Google的Cookie而不会操作Baidu的Cookie，从而保证用户的隐私安全。浏览器判断一个网站是否能操作另一个网站Cookie的依据是域名。Google与Baidu的域名不一样，因此Google不能操作Baidu的Cookie。</p><h4 id="Session"><a href="#Session" class="headerlink" title="Session"></a>Session</h4><p>由于网页是一种无状态的连接程序，因此无法得知用户的浏览状态。在网上购物的时，把很多商品加入了购物车，而在结账时网站却不知道你购物车有哪些物品。为了解决这个问题，服务器端就为特定用户创建了特定的<code>session</code> ，用于标示并跟踪这个用户，这样才知道购物车里有哪些商品。</p><ul><li><code>Session</code> 是另一种<strong>记录客户状态的机制</strong>，不同的是 <code>Cookie</code> 保存在客户端浏览器中，而 <code>Session</code> 保存在服务器上。</li><li>客户端浏览器访问服务器的时候，服务器把客户端信息以某种形式记录在服务器上。这就是 <code>Session</code> 。客户端浏览器再次访问时只需要从该 <code>Session</code> 中查找该客户的状态就可以了。</li><li>如果说 <code>Cookie</code> 机制是通过检查客户身上的“通行证”来确定客户身份的话，那么 <code>Session</code> 机制就是通过检查服务器上的“客户明细表”来确认客户身份。</li><li><code>Session</code> 相当于程序在服务器上建立的一份客户档案，客户来访的时候只需要查询客户档案表就可以了。</li></ul><h4 id="Session和Cookie的关系"><a href="#Session和Cookie的关系" class="headerlink" title="Session和Cookie的关系"></a>Session和Cookie的关系</h4><ul><li><code>cookie</code> 是一个实际存在的、具体的东西，<code>http</code> 协议中定义在 <code>header</code> 中的字段。</li><li><code>session</code> 是一个抽象概念、开发者为了实现中断和继续等操作，将 <code>client</code> 和 <code>server</code> 之间一对一的交互，抽象为“会话”，进而衍生出“会话状态”，也就是 <code>session</code> 的概念。</li><li>即 <code>session</code> 描述的是一种通讯会话机制，而 <code>cookie</code> 只是目前实现这种机制的主流方案里面的一个参与者，它一般是用于保存<strong>session ID</strong>。</li></ul><hr><h3 id="从浏览器输入URL到出现页面经过了哪些阶段"><a href="#从浏览器输入URL到出现页面经过了哪些阶段" class="headerlink" title="从浏览器输入URL到出现页面经过了哪些阶段"></a>从浏览器输入URL到出现页面经过了哪些阶段</h3><h4 id="输入网址"><a href="#输入网址" class="headerlink" title="输入网址"></a>输入网址</h4><p>URL的形式： <code>schema://host:port/path?query#fragment</code></p><p>　　<code>-schema</code> : 协议，例如 <code>http</code> , <code>https</code> , <code>ftp</code> 等</p><p>　　<code>-host</code> : 域名或者 <code>ip</code> 地址</p><p>　　<code>-port</code> ： 端口，<code>http</code> 默认端口 <code>80</code> ，可以省略</p><p>　　<code>-path</code> ： 路径，例如 <code>/abc</code> <code>/a/b/c</code></p><p>　　<code>-query</code> : 查询参数，例如 <code>token=sdfs2223fds2&amp;name=sdffaf</code></p><p>　　<code>-fragment</code> : 锚点（哈希 <code>Hash</code> ），用于定位页面的某个位置。</p><p>符合规则的URL</p><p>　　<a href="http://www.baidu.com/java/web?flag=1#function">http://www.baidu.com/java/web?flag=1#function</a></p><h4 id="缓存解析"><a href="#缓存解析" class="headerlink" title="缓存解析"></a>缓存解析</h4><p>浏览器获取了这个 <code>url</code> ，它先去缓存当中看看有没有，依次从浏览器缓存-&gt;系统缓存-&gt;路由器缓存当中查看，如果有从缓存当中显示页面，这样是不需要发送 <code>http</code> 请求的。如果没有那就进行步骤3。<br>缓存就是把之前访问的web资源，比如一些js，css，图片什么的保存在本机的内存或者磁盘当中。</p><h4 id="域名DNS解析"><a href="#域名DNS解析" class="headerlink" title="域名DNS解析"></a>域名DNS解析</h4><p>当缓存中没有这个 <code>url</code> 的时候，就需要发送 <code>http</code> 请求了，那么在发送 <code>http</code> 请求之前，需要先进行<strong>DNS解析</strong>将域名解析成 <code>ip</code> 地址。<strong>DNS解析</strong>存在着多级缓存：</p><ul><li>浏览器缓存、系统缓存（hosts)、路由器缓存</li><li>本地DNS服务器（ISP提供）<ul><li>有映射，解析完成</li><li>有缓存，解析完成，不权威</li></ul></li><li>上述都失败<ul><li>未采用转发模式（迭代）：<ul><li>发给根DNS服务器</li><li>返回负责该顶级域名服务器的IP （如.com)</li><li>向该顶级域名服务器IP发送请求</li><li>如果失败，找下一级DNS服务器（如qq.com）</li><li>循环往复，直到得到结果，发给本地DNS服务器，再发给客户机</li></ul></li><li>采用转发模式（递归）：<ul><li>请求转发至上一级DNS服务器</li><li>不断重复</li></ul></li><li><strong>递归查询</strong>：本机向本地域名服务器发出一次查询请求，就静待最终的结果。如果本地域名服务器无法解析，自己会以 <code>DNS</code> 客户机的身份向其它域名服务器查询，直到得到最终的 <code>IP</code> 地址告诉本机</li><li><strong>迭代查询</strong>：本地域名服务器向根域名服务器查询，根域名服务器告诉它下一步到哪里去查询，然后它再去查，每次它都是以客户机的身份去各个服务器查询。</li></ul></li></ul><p><strong>DNS优化</strong></p><ul><li><strong>DNS缓存</strong><br>DNS存在着多级缓存，从离浏览器的距离排序的话，有以下几种: 浏览器缓存，系统缓存，路由器缓存，IPS服务器缓存，根域名服务器缓存，顶级域名服务器缓存，主域名服务器缓存。</li><li><strong>DNS负载均衡</strong><br>DNS可以返回一个合适的机器的 IP 给用户，例如可以根据每台机器的负载量，该机器离用户地理位置的距离等等。</li></ul><h4 id="tcp链接，三次握手"><a href="#tcp链接，三次握手" class="headerlink" title="tcp链接，三次握手"></a>tcp链接，三次握手</h4><p>在域名解析之后，浏览器向服务器发起了http请求，tcp连接，三次握手建立tcp连接。TCP协议是面向连接的，所以在传输数据前必须建立连接。</p><p>（1）客户端向服务器发送连接请求报文；<br>（2）服务器端接受客户端发送的连接请求后后回复ACK报文，并为这次连接分配资源。<br>（3）客户端接收到ACK报文后也向服务器端发生ACK报文，并分配资源。</p><p>这样TCP连接就建立了。<br>在此之后，浏览器开始向服务器发送http请求，请求数据包。请求信息包含一个<strong>请求头部</strong>和一个<strong>请求体</strong>。</p><h4 id="服务器收到请求"><a href="#服务器收到请求" class="headerlink" title="服务器收到请求"></a>服务器收到请求</h4><p>服务器收到浏览器发送的请求信息，返回一个<strong>响应头</strong>和一个<strong>响应体</strong>。</p><h4 id="浏览器页面渲染"><a href="#浏览器页面渲染" class="headerlink" title="浏览器页面渲染"></a>浏览器页面渲染</h4><ul><li>处理HTML标记并构建DOM树；</li><li>处理CSS标记并构建CSSOM树；</li><li>将DOM和CSSOM合并为一颗渲染树，通常为了更好的用户体验，渲染会与解析同时进行，将已经解析好的树渲染出来。</li><li>根据渲染树来布局，以计算每个节点的几何信息(重排)</li><li>将各个结点绘制到屏幕上(重绘)</li><li>如果 DOM 或 CSSOM 被修改，以上过程需要重复执行，这样才能计算出哪些像素需要在屏幕上进行重新渲染。</li></ul><h4 id="断开连接，四次挥手"><a href="#断开连接，四次挥手" class="headerlink" title="断开连接，四次挥手"></a>断开连接，四次挥手</h4><hr><h2 id="TCP-UDP-运输层"><a href="#TCP-UDP-运输层" class="headerlink" title="TCP/UDP/运输层"></a>TCP/UDP/运输层</h2><h3 id="UDP和TCP的差别⭐"><a href="#UDP和TCP的差别⭐" class="headerlink" title="UDP和TCP的差别⭐"></a>UDP和TCP的差别⭐</h3><p><strong>UDP：</strong></p><ul><li><strong>无连接</strong>：减少了开销和发送前时延；</li><li><strong>尽最大努力交付</strong>：不保证可靠交付；</li><li><strong>面向报文</strong>：对应用层交下来的报文既不合并，也不拆分，保留这些报文的边界。</li><li><strong>没有拥塞控制</strong>：网络拥塞不会使源主机发送速率降低，允许在网络拥塞时丢失一些数据，却不允许数据有太大的时延。</li><li><strong>支持一对一、一对多、多对一、多对多的交互通信</strong></li><li><strong>首部开销小：</strong> 首部长度只有 <strong>8</strong> 字节，比 TCP 的 <strong>20</strong> 字节短。</li></ul><p><strong>TCP:</strong></p><ul><li><strong>面向连接：</strong> 传输前需建立连接，传输完成需释放连接；TCP 连接是一条<strong>逻辑连接</strong>。</li><li><strong>提供可靠交付：</strong> 无差错、不丢失、不重复、不失序；</li><li><strong>面向字节流：</strong> TCP 把应用进程交下来的数据看作<strong>一连串的无结构字节流</strong>；</li><li><strong>有拥塞控制</strong></li><li><strong>点对点：</strong> 每一条 TCP 连接只能有两个端点；</li><li><strong>首部开销大：</strong>20字节</li></ul><hr><h3 id="TCP-UDP数据发送和接收过程"><a href="#TCP-UDP数据发送和接收过程" class="headerlink" title="TCP/UDP数据发送和接收过程"></a>TCP/UDP数据发送和接收过程</h3><h4 id="发送端"><a href="#发送端" class="headerlink" title="发送端"></a><strong>发送端</strong></h4><p>数据发送是发送到物理链路层的，从<strong>应用层</strong>开始，经过一层层的数据报文的封装，最终封装成<strong>以太网封装格式</strong> 和 <strong>802.x 封装格式的数据包</strong>，通过有线网络或无线网络在以太网上传输<strong>数据帧</strong>（bit流）。</p><h4 id="接收端"><a href="#接收端" class="headerlink" title="接收端"></a><strong>接收端</strong></h4><p><img src="https://ifwant-pic-1303131123.cos.ap-nanjing.myqcloud.com/photo/1568283339430.91a72ae3.png" alt="1568283339430"></p><p>接收端和发送端的顺序刚好相反。即分别经过</p><ul><li>经过数据链路层去掉<strong>以太网的头部</strong>得到<strong>以太网MAC帧</strong></li><li>经过网络层去掉<strong>IP的头部</strong>得到<strong>IP数据包</strong></li><li>经过传输层去掉 <code>TCP/UDP</code> 的头部得到<strong>报文段/用户数据报</strong></li><li>经过应用层得到数据流</li></ul><p>就完成了整个的数据传输过程。</p><h4 id="复用与分用"><a href="#复用与分用" class="headerlink" title="复用与分用"></a>复用与分用</h4><p>应用层所有应用进程都可以通过运输层传输到 <code>IP</code> 层，称为<strong>复用</strong>；运输层从 <code>IP</code> 层收到发送给各应用进程的数据后，必须分别交付给指明的各应用进程，称为<strong>分用</strong>。</p><hr><h3 id="TCP协议的可靠性体现在哪里"><a href="#TCP协议的可靠性体现在哪里" class="headerlink" title="TCP协议的可靠性体现在哪里"></a>TCP协议的可靠性体现在哪里</h3><ol><li><strong>应用数据被分割成TCP认为最适合发送的数据块</strong>。(这和UDP完全不同，应用程序产生的数据报长度将保持不变)。</li><li><strong>超时重传:</strong> 当TCP发出一个段后，它启动一个定时器，等待目的端确认收到这个报文段。如果不能及时收到一个确认,将重发这个报文段。</li><li><strong>有序编号：</strong>TCP给发送的每一一个包进行编号 ,接收方对数据包进行排序,把有序数据传送给应用层。（TCP报文段作为IP数据报来传输，IP数据报的大刀可能会失序，因此TCP报文段的到达也可能会失序，如果必要，TCP将对收到的数据进行<strong>重新排序</strong>）。</li><li><strong>校验和</strong>: TCP将保持它首部和数据的检验和。这是一个端到端的检验和，目的是检测数据在传输过程中的任何变化。如果收到段的检验和有差错, TCP将丢弃这个报文段和不确认收到此报文段。</li><li>IP数据报会发生重复，TCP的接收端会<strong>丢弃重复</strong>的数据。</li><li><strong>流量控制:</strong> TCP连接的每一方都有固定大小的缓冲空间 , TCP的接收端只允许发送端发送接收端缓冲区能接纳的我数据。当接收方来不及处理发送方的数据,能提示发送方降低发送的速率,防止包丢失。TCP使用的流量控制协议是可变大小的滑动窗口协议。</li><li><strong>拥塞控制</strong>：当网络拥塞时,减少数据的发送。</li><li><strong>确认响应</strong>：对于一个收到的请求，将发送一个确认。这个确认通常要延迟几分之一秒。</li></ol><p>TCP协议保证数据传输可靠性的方式主要有：</p><h4 id="校验和"><a href="#校验和" class="headerlink" title="校验和"></a>校验和</h4><p>计算方式：在数据传输的过程中，将发送的数据段都当做一个16位的整数。将这些整数加起来。并且前面的进位不能丢弃，补在后面，最后取反，得到校验和。<br><strong>发送方</strong>：在发送数据之前计算检验和，并进行校验和的填充。<br><strong>接收方</strong>：收到数据后，对数据以同样的方式进行计算，求出校验和，与发送方的进行比对。</p><p><img src="https://ifwant-pic-1303131123.cos.ap-nanjing.myqcloud.com/photo/01091.jpg" alt="这里写图片描述"></p><h4 id="序列号-ACK-和确认应答"><a href="#序列号-ACK-和确认应答" class="headerlink" title="序列号(ACK)和确认应答"></a>序列号(ACK)和确认应答</h4><p><code>TCP</code> 传输时将每个字节的数据都进行了编号，这就是序列号。确认应答：<code>TCP</code> 传输的过程中，每次接收方收到数据后，都会对传输方进行确认应答。也就是发送 <code>ACK</code> 报文。这个 <code>ACK</code> 报文当中带有对应的确认序列号，告诉发送方，接收到了哪些数据，下一次的数据从哪里发。序列号的作用不仅仅是应答的作用，有了序列号能够将接收到的数据根据序列号排序，并且去掉重复序列号的数据。这也是 <code>TCP</code> 传输可靠性的保证之一。</p><h4 id="超时重传"><a href="#超时重传" class="headerlink" title="超时重传"></a>超时重传</h4><p>在进行TCP传输时，由于确认应答与序列号机制，也就是说发送方发送一部分数据后，都会等待接收方发送的 <code>ACK</code> 报文，并解析 <code>ACK</code> 报文，判断数据是否传输成功。如果发送方发送完数据后，迟迟没有等到接收方的 <code>ACK</code> 报文，这该怎么办呢？而没有收到 <code>ACK</code> 报文的原因可能是什么呢？</p><p>首先，发送方没有介绍到响应的 <code>ACK</code> 报文原因可能有两点：</p><ol><li>数据在传输过程中由于网络原因等直接全体丢包，接收方根本<strong>没有接收</strong>到。</li><li>接收方接收到了响应的数据，但是发送的 <code>ACK</code> 报文响应却由于网络原因<strong>丢包</strong>了。</li></ol><p>TCP在解决这个问题的时候引入了一个新的机制，叫做<strong>超时重传机制</strong>。简单理解就是发送方在发送完数据后等待一个时间(<strong>500ms的整数倍</strong>)，时间到达没有接收到 <code>ACK</code> 报文，那么对刚才发送的数据进行重新发送。如果是刚才第一个原因，接收方收到二次重发的数据后，便进行 <code>ACK</code> 应答。如果是第二个原因，接收方发现接收的数据已存在（判断存在的根据就是序列号，所以上面说序列号还有去除重复数据的作用），那么直接丢弃，仍旧发送 <code>ACK</code> 应答。</p><h4 id="流量控制"><a href="#流量控制" class="headerlink" title="流量控制"></a>流量控制</h4><p>接收端在接收到数据后，对其进行处理。如果发送端的发送速度太快，导致接收端的结束缓冲区很快的填充满了。此时如果发送端仍旧发送数据，那么接下来发送的数据都会丢包，继而导致丢包的一系列连锁反应，超时重传呀什么的。而<strong>TCP根据接收端对数据的处理能力，决定发送端的发送速度，这个机制就是流量控制</strong>。在TCP协议的报头信息当中，有一个16位字段的窗口大小。在介绍这个窗口大小时我们知道，窗口大小的内容实际上是接收端接收数据缓冲区的剩余大小。这个数字越大，证明接收端接收缓冲区的剩余空间越大，网络的吞吐量越大。接收端会在确认应答发送 <code>ACK</code> 报文时，将自己的即时窗口大小填入，并跟随 <code>ACK</code> 报文一起发送过去。而发送方根据 <code>ACK</code> 报文里的窗口大小的值的改变进而改变自己的发送速度。如果接收到窗口大小的值为0，那么发送方将停止发送数据。并定期的向接收端发送窗口探测数据段，让接收端把窗口大小告诉发送端。</p><p><img src="https://ifwant-pic-1303131123.cos.ap-nanjing.myqcloud.com/photo/01092.jpg" alt="这里写图片描述"></p><h4 id="滑动窗口-以字节为单位"><a href="#滑动窗口-以字节为单位" class="headerlink" title="滑动窗口(以字节为单位)"></a>滑动窗口(以字节为单位)</h4><ul><li>假定 <code>A</code> 收到来自 <code>B</code> 的确认报文段，其中窗口是 20 字节，确认号是 31 (表明 <code>B</code> 期望收到的下一个序号是 31)。根据这两个数据， <code>A</code> 构造出自己的发送窗口。</li></ul><p><img src="https://ifwant-pic-1303131123.cos.ap-nanjing.myqcloud.com/photo/01093.jpg" alt="1568790013316"></p><ul><li><p>发送窗口由<strong>前沿</strong>和<strong>后沿</strong>共同确定。发送窗口通常<strong>不断向前移动</strong>，也有<strong>可能不动</strong>（一是没有收到新的确认；二是收到了新的确认但对方通知窗口缩小了，因此只有后沿缩小，前沿正好不动）。</p></li><li><p>描述一个发送窗口的状态需要<strong>三个指针</strong>：$P_1,P_2,P_3$，指针指向字节序号。</p><ul><li>小于 $P_1$ 是已经发送并<strong>收到确认</strong>的部分</li><li>$P_1 - P_2$ 是已经发送但<strong>未收到确认</strong>的部分</li><li>$P_2 - P_3$ 是<strong>窗口内允许发送</strong>但<strong>尚未发送</strong>的部分</li><li>大于 $P_3$ 是<strong>窗口外不允许发送</strong>的部分</li></ul><p><img src="https://ifwant-pic-1303131123.cos.ap-nanjing.myqcloud.com/photo/1568790430010.9e25f59d.png" alt="1568790430010"></p></li></ul><h4 id="拥塞控制"><a href="#拥塞控制" class="headerlink" title="拥塞控制"></a>拥塞控制</h4><ul><li><p>拥塞控制是TCP在传输时尽可能快的将数据传输，并且避免拥塞造成的一系列问题。是可靠性的保证，同时也是维护了传输的高效性。</p><p><img src="https://ifwant-pic-1303131123.cos.ap-nanjing.myqcloud.com/photo/1568796886603.87101025.png" alt="1568796886603"></p></li><li><p>发送方窗口的上限值应取<strong>接收方窗口 rwnd 和拥塞窗口 cwnd 两个变量中的较小值</strong>：</p></li><li><p>TCP传输的过程中，发送端开始发送数据的时候，如果刚开始就发送大量的数据，那么就可能造成一些问题。网络可能在开始的时候就很拥堵，如果给网络中在扔出大量数据，那么这个拥堵就会加剧。拥堵的加剧就会产生大量的丢包，就对大量的超时重传，严重影响传输。</p></li><li><p>TCP拥塞控制由四个部分组成：<strong>慢开始，拥塞避免，快重传，快恢复</strong></p><p><img src="https://ifwant-pic-1303131123.cos.ap-nanjing.myqcloud.com/photo/1568796161225.0a85de7d.png" alt="1568796161225"></p></li></ul><h5 id="慢开始"><a href="#慢开始" class="headerlink" title="慢开始"></a><strong>慢开始</strong></h5><ul><li>在开始发送数据时，先发送少量的数据探路。探清当前的网络状态如何，再决定多大的速度进行传输。这时候就引入一个叫做<strong>拥塞窗口(cwnd)**的概念。发送刚开始定义拥塞窗口为 <code>1</code>，在传输的过程中以</strong>2的倍数<strong>指数增大 <code>cwnd</code> 直至其大于 <code>ssthreshold</code> ，之后转为</strong>拥塞避免<strong>，</strong>每次收到ACK应答，拥塞窗口加 1**。在发送数据之前，首先将拥塞窗口与接收端反馈的窗口大小比对，取较小的值作为实际发送的窗口。</li></ul><h5 id="拥塞避免"><a href="#拥塞避免" class="headerlink" title="拥塞避免"></a><strong>拥塞避免</strong></h5><ul><li>当出现报文丢失时（既发生重传时），认定网络出现拥塞。将 <code>ssthreshold/=2</code> ，并初始化 <code>cwnd=1</code> 重新开始算法。</li></ul><h5 id="快重传"><a href="#快重传" class="headerlink" title="快重传"></a><strong>快重传</strong></h5><p>让发送方尽早<strong>知道个别报文段的丢失</strong>，以至于<strong>不需超时重传</strong>。使用快重传可以使整个网络吞吐量提升 20% 。</p><ul><li>快重传要求接收方不要等待自己发送数据时才捎带确认，而是<strong>立即发送确认</strong>；即使收到了失序的报文段也要立即发出对已收到的报文段的<strong>重复确认</strong>。</li><li>如下图所示，发送方只要<strong>一连收到 3 个重复确认ACK</strong>，就知道接收方没有收到 <code>M3</code> ，而<strong>立即重传 M3</strong> 。</li></ul><p><img src="https://ifwant-pic-1303131123.cos.ap-nanjing.myqcloud.com/photo/1568796172902.50a3f779.png" alt="1568796172902"></p><h5 id="快恢复"><a href="#快恢复" class="headerlink" title="快恢复"></a><strong>快恢复</strong></h5><p>当接收到连续 3 个重复确认时，知道只是丢失了个别报文段，于是不启动慢开始，而是执行快恢复。</p><ul><li>发送方调整门限值 <strong>ssthresh = cwnd / 2</strong>；同时设置 <strong>cwnd = ssthresh</strong> ，立即进入<strong>拥塞避免</strong>。</li><li>这样做的好处是<strong>避免个别丢失的报文段被误认为是网络拥塞造成。</strong></li></ul><hr><h3 id="TCP-和UDP报文段首部"><a href="#TCP-和UDP报文段首部" class="headerlink" title="TCP 和UDP报文段首部"></a>TCP 和UDP报文段首部</h3><p><strong>TCP</strong></p><ul><li>TCP 报文段首部前 <code>20</code> 字节固定，后面有 <code>4n</code> 字节可根据需要加选项。</li></ul><p><a href="https://img-blog.csdnimg.cn/20181130165347546.png?x-oss-process=image/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L3FxXzQxMzA4MDI3,size_16,color_FFFFFF,t_70"><img src="https://img-blog.csdnimg.cn/20181130165347546.png?x-oss-process=image/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L3FxXzQxMzA4MDI3,size_16,color_FFFFFF,t_70" alt="图片来源于网络"></a></p><ul><li><strong>源端口和目的端口：</strong> 各占 <strong>2</strong> 字节；TCP 的分用功能通过端口实现。</li><li><strong>序号(seq)：</strong> 占 <strong>4</strong> 字节（序号范围 $[0,2^{32}-1]$）；TCP 传送的<strong>字节流</strong>中每一个字节都<strong>按顺序编号</strong>；序号字段代表本报文段发送数据的<strong>第一个字节的序号</strong>，例如一报文段序号 <code>301</code> ，最后一个字节序号 <code>400</code> ，那么下一个报文段的数据序号应当从 <code>401</code> 开始。</li><li><strong>确认号(ACK)**：占 **4</strong> 字节；代表<strong>期望收到对方下一个报文段第一个数据字节的序号</strong>；例如 <code>B</code> 正确收到一个报文段，序号字段是 <code>501</code>，数据长度是 <code>200</code> 字节，则 <code>B</code> 期望收到下一个数据序号是 <code>701</code> ，即将发送给 <code>A</code> 的确认报文段中的确认号置为 <code>701</code> 。若确认号 = <code>N</code>，代表直到序号 <code>N-1</code> 的所有数据都已正确收到。</li><li><strong>数据偏移</strong>：占 <strong>4</strong> 字节；指出 TCP 报文段<strong>数据起始处</strong>距离 TCP 报文段起始处的距离；能表示的最大十进制数字为 15，因此数据偏移最大值是 60 字节，也是 TCP 首部的最大长度。</li><li><strong>保留：</strong> 占 <strong>6</strong> 位，保留今后使用，应设置为 0；</li><li><strong>紧急 URG (urgent)：</strong> 当 <strong>URG = 1</strong> 时，表明紧急指针字段有效。发送方 TCP 把紧急数据插入到本报文段数据的<strong>最前面</strong>。</li><li><strong>确认 ACK (acknowledgment)**：仅当 **ACK = 1</strong> 时<strong>确认号</strong>字段有效。TCP 规定，在连接建立后所有传送的报文段应将 <code>ACK</code> 置 1 。</li><li><strong>推送 PSH (push)：</strong> 当两个应用进程交互式通信时，一段应用进程希望键入一个命令后立即收到对方响应；推送操作很少使用。</li><li><strong>复位 RST (reset)：</strong> <code>RST = 1</code> 代表 TCP 出现严重差错（例如主机崩溃），必须释放连接； <code>RST</code> 还用来拒绝一个非法的报文段或拒绝打开一个连接。</li><li><strong>同步 SYN (synchronization)：</strong> 连接建立时用来同步序号。当 <code>SYN = 1</code> 而 <code>ACK = 0</code> 时，表明这是一个<strong>连接请求</strong>报文段。<code>SYN = 1</code> 和 <code>ACK = 1</code> 就表示<strong>连接接受</strong>。</li><li><strong>终止 FIN (finish)：</strong> 用来释放一个连接。当 <code>FIN = 1</code> 时，表明发送方数据已经发送完毕，并要求<strong>释放连接</strong>。</li><li><strong>窗口：</strong> 占 <strong>2</strong> 字节；指发送本报文段的一方的<strong>接收窗口</strong>（窗口大小是由接收方定的，作为发送方设置其发送窗口的依据）。窗口字段明确指出了现在允许对方发送的数据量。窗口值是动态变化的。</li><li><strong>检验和：</strong> 占 <strong>2</strong> 字节；检验范围包括<strong>首部和数据</strong>两部分。计算检验和前需要在 TCP 报文段前面加上 12 字节的伪首部，格式与 UDP 伪首部一样，但应把第 4 个字段中的 17 改为 6 （TCP 协议号为 6 ），把第 5 个字段中 UDP 长度改为 TCP 长度。</li><li><strong>紧急指针：</strong>占 <strong>2</strong> 字节；仅在 <code>URG = 1</code> 时才有意义，指出本报文段中紧急数据的字节数（紧急数据后都是普通数据），紧急指针指出了紧急数据的末尾所在报文段中的位置；窗口为 0 时也可发紧急数据。</li><li><strong>选项：</strong> 长度可变，<strong>最长 40 字节</strong>。<strong>无选项时， TCP 首部长度为 20 字节。</strong></li></ul><p><strong>UDP</strong></p><p>相应的，<code>UDP</code> 的报文段只有<strong>源端口+目的端口+长度+检验和：</strong> 各占 <strong>2</strong> 字节，<strong>首部长度为8字节。</strong></p><p><strong>长度字段：</strong>占<code>16</code>比特。标明 <code>UDP</code> 头部和 <code>UDP</code> 数据的总长度字节，<code>UDP</code> 首部的长度为固定的 <code>8</code> 个字节，加上 <code>UDP</code> 数据长度就是长度大小，<code>UDP</code> 数据长度可以通过 <code>wireshark</code> 抓包看到。</p><p><a href="https://krahets.gitee.io/assets/img/1568704529477.08a8d8a2.png"><img src="https://krahets.gitee.io/assets/img/1568704529477.08a8d8a2.png" alt="1568704529477"></a></p><hr><h3 id="停止等待-ARQ-协议"><a href="#停止等待-ARQ-协议" class="headerlink" title="停止等待 ARQ 协议"></a>停止等待 ARQ 协议</h3><hr><h4 id="三次握手四次挥手"><a href="#三次握手四次挥手" class="headerlink" title="三次握手四次挥手"></a>三次握手四次挥手</h4><h5 id="三次握手"><a href="#三次握手" class="headerlink" title="三次握手"></a>三次握手</h5><ul><li><p>第一次握手：客户端给服务端发一个 SYN 报文，并指明客户端的初始化序列号 ISN©。此时客户端处于 SYN_SEND 状态。</p><p>首部的同步位SYN=1，初始序号seq=x，SYN=1的报文段不能携带数据，但要消耗掉一个序号。</p></li><li><p>第二次握手：服务器收到客户端的 SYN 报文之后，会以自己的 SYN 报文作为应答，并且也是指定了自己的初始化序列号 ISN(s)。同时会把客户端的 ISN + 1 作为ACK 的值，表示自己已经收到了客户端的 SYN，此时服务器处于 SYN_REVD 的状态。</p><p>在确认报文段中SYN=1，ACK=1，确认号ack=x+1，初始序号seq=y。</p></li><li><p>第三次握手：客户端收到 SYN 报文之后，会发送一个 ACK 报文，当然，也是一样把服务器的 ISN + 1 作为 ACK 的值，表示已经收到了服务端的 SYN 报文，此时客户端处于 ESTABLISHED 状态。服务器收到 ACK 报文之后，也处于 ESTABLISHED 状态，此时，双方已建立起了连接。</p><p>确认报文段ACK=1，确认号ack=y+1，序号seq=x+1（初始为seq=x，第二个报文段所以要+1），ACK报文段可以携带数据，不携带数据则不消耗序号。</p></li></ul><p>发送第一个SYN的一端将执行主动打开（active open），接收这个SYN并发回下一个SYN的另一端执行被动打开（passive open）。</p><p><img src="https://ifwant-pic-1303131123.cos.ap-nanjing.myqcloud.com/photo/1568797056445.aa1412a3.png" alt="1568797056445"></p><h5 id="四次挥手"><a href="#四次挥手" class="headerlink" title="四次挥手"></a>四次挥手</h5><p><img src="https://ifwant-pic-1303131123.cos.ap-nanjing.myqcloud.com/photo/1568797480363.f8ec5647.png" alt="1568797480363"></p><ol><li><p>A 发送 TCP 连接释放报文，进入 <strong>FIN-WAIT-1</strong> 状态，<strong>等待 B 的确认</strong>；</p></li><li><p>B 收到报文段后立即发出确认，并进入 <strong>CLOSE-WAIT</strong> 状态；此时 A 到 B 的单向连接已经被释放， TCP 连接处于<strong>半关闭状态</strong>；B 继续向 A 传输未完成的数据。</p></li><li><p>A收到 B 的确认后，进入 <strong>FIN-WAIT-2</strong> 状态，<strong>等待 B 的连接释放报文段</strong>；</p></li><li><p>若 B已经将所有数据向 A 发送完毕，则向 A发送 TCP 连接释放报文，并进入 <strong>LAST-ACK</strong> 状态，<strong>等待 A 的确认</strong>；</p></li><li><p>A收到B的连接释放报文段后，立即发出确认，然后进入TIME-WAIT状态，必须经过时间等待计时器 设置的时间2MSL(4 min)后，A才进入CLOSED状态，这是因为：</p><ul><li><p><strong>第一：</strong> 由于 A 到 B的最后的 <code>ACK</code> 确认报文段有可能丢失，B 可能会超时重传释放连接报文段，而 <code>A</code> 就能在 <code>2MSL</code> 时间内收到这个重传的连接释放报文段，并重传确认且重新启动 <code>2MSL</code> 计时器。</p></li><li><p><strong>第二：</strong> 防止<strong>“已失效的连接请求报文段”</strong>出现在本连接中， <code>A</code> 等待 <code>2MSL</code> 就可以使本链接持续时间内所产生的所有报文段在网络中消失。MSL是任何报文段被丢弃前在网络内的最长时间</p></li></ul></li></ol><p><strong>其实可以理解为：seq就是发送者id，ack就是接受者id</strong></p><hr><h3 id="为什么需要三次握手，两次不行吗？"><a href="#为什么需要三次握手，两次不行吗？" class="headerlink" title="为什么需要三次握手，两次不行吗？"></a>为什么需要三次握手，两次不行吗？</h3><p><strong>防止已失效的连接请求又传送到服务器端，因而产生错误：</strong>假设 <code>A</code> 发出的第一个连接请求报文段没有丢失，而是在网络某处长时间滞留了，本来这是一个失效的连接请求；但后续 <code>B</code> 收到了它，误认为是 <code>A</code> 的连接请求，就向 <code>A</code> 发出确认报文段。若不采用三报文握手，此时一个已经失效的连接已经被建立，网络资源被白白浪费了。</p><p>第二次握手结束时，不能确定客户端的接收能力正常</p><hr><h3 id="TCP三次握手，如果第三次客户端发出的信息服务器没收到怎么办"><a href="#TCP三次握手，如果第三次客户端发出的信息服务器没收到怎么办" class="headerlink" title="TCP三次握手，如果第三次客户端发出的信息服务器没收到怎么办"></a>TCP三次握手，如果第三次客户端发出的信息服务器没收到怎么办</h3><p>服务器第一次收到客户端的 SYN 之后，就会处于 SYN_RCVD 状态，此时双方还没有完全建立其连接，服务器会把此种状态下请求连接放在一个队列里，我们把这种队列称之为半连接队列。</p><p>服务器发送完SYN-ACK包，如果未收到客户确认包，服务器进行首次重传，等待一段时间仍未收到客户确认包，进行第二次重传。如果重传次数超过系统规定的最大重传次数，系统将该连接信息从半连接队列中删除。</p><hr><h3 id="ISN-Initial-Sequence-Number-是固定的吗"><a href="#ISN-Initial-Sequence-Number-是固定的吗" class="headerlink" title="ISN(Initial Sequence Number)是固定的吗"></a>ISN(Initial Sequence Number)是固定的吗</h3><p>就是图中的seq，不是固定的，ISN随时间而变化，因此每个连接都将具有不同的ISN。固定的容易被攻击。</p><hr><h3 id="三次握手过程中可以携带数据么"><a href="#三次握手过程中可以携带数据么" class="headerlink" title="三次握手过程中可以携带数据么"></a>三次握手过程中可以携带数据么</h3><p>第三次握手的时候，可以携带。前两次握手不能携带数据。</p><p>如果前两次握手能够携带数据，那么一旦有人想攻击服务器，那么他只需要在第一次握手中的 SYN 报文中放大量数据，那么服务器势必会消耗更多的时间和内存空间去处理这些数据，增大了服务器被攻击的风险。</p><p>而对于第三次握手，此时客户端已经建立了连接，已经能够确认服务器的接收、发送能力正常，这个时候相对安全了，可以携带数据。</p><hr><h3 id="挥手为什么需要四次？"><a href="#挥手为什么需要四次？" class="headerlink" title="挥手为什么需要四次？"></a>挥手为什么需要四次？</h3><p>因为服务端收到关闭请求的时候可能还有数据要传送，所以需要先把数据传送完，才发FIN请求，所以多了一次。</p><h3 id="什么是Socket"><a href="#什么是Socket" class="headerlink" title="什么是Socket"></a>什么是Socket</h3><p><a href="https://img-blog.csdnimg.cn/20190718154523875.png?x-oss-process=image/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L3Bhc2hhbmh1NjQwMg==,size_16,color_FFFFFF,t_70"><img src="https://img-blog.csdnimg.cn/20190718154523875.png?x-oss-process=image/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L3Bhc2hhbmh1NjQwMg==,size_16,color_FFFFFF,t_70" alt="img"></a></p><p><code>Socket</code> 是应用层与 <code>TCP/IP</code> 协议族通信的中间软件抽象层，它是一组接口。在设计模式中，<code>Socket</code> 其实就是一个门面模式，它把复杂的 <code>TCP/IP</code> 协议族隐藏在 <code>Socket</code> 接口后面，对用户来说，一组简单的接口就是全部，让<code>Socket</code> 去组织数据，以符合指定的协议。</p><p><a href="https://img-blog.csdnimg.cn/20190718154556909.png?x-oss-process=image/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L3Bhc2hhbmh1NjQwMg==,size_16,color_FFFFFF,t_70"><img src="https://img-blog.csdnimg.cn/20190718154556909.png?x-oss-process=image/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L3Bhc2hhbmh1NjQwMg==,size_16,color_FFFFFF,t_70" alt="img"></a></p><p>先从服务器端说起。服务器端先初始化 <code>Socket</code> ，然后与端口绑定( <code>bind</code> )，对端口进行监听( <code>listen</code> )，调用 <code>accept</code> 阻塞，等待客户端连接。在这时如果有个客户端初始化一个 <code>Socket</code> ，然后连接服务器( <code>connect</code> )，如果连接成功，这时客户端与服务器端的连接就建立了。客户端发送数据请求，服务器端接收请求并处理请求，然后把回应数据发送给客户端，客户端读取数据，最后关闭连接，一次交互结束。</p><h5 id="socket函数"><a href="#socket函数" class="headerlink" title="socket函数"></a>socket函数</h5><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">int socket(int domain, int type, int protocol);</span><br></pre></td></tr></table></figure><p>socket函数对应于普通文件的打开操作。普通文件的打开操作返回一个文件描述字，而<strong>socket()**用于创建一个</strong>socket描述符（socket descriptor）**，它唯一标识一个socket。这个socket描述字跟文件描述字一样，后续的操作都有用到它，把它作为参数，通过它来进行一些读写操作。</p><ul><li><strong>domain</strong>：即<strong>协议域</strong>，又称为协议族（family）。常用的协议族有，AF_INET、AF_INET6、AF_LOCAL（或称AF_UNIX，Unix域socket）、AF_ROUTE等等。协议族决定了socket的地址类型，在通信中必须采用对应的地址，如AF_INET决定了要用ipv4地址（32位的）与端口号（16位的）的组合、AF_UNIX决定了要用一个绝对路径名作为地址。</li><li><strong>type</strong>：<strong>指定socket类型</strong>。常用的socket类型有，SOCK_STREAM、SOCK_DGRAM、SOCK_RAW、SOCK_PACKET、SOCK_SEQPACKET等等（socket的类型有哪些？）。</li><li><strong>protocol</strong>：<strong>指定协议</strong>。常用的协议有，IPPROTO_TCP、IPPTOTO_UDP、IPPROTO_SCTP、IPPROTO_TIPC等，它们分别对应TCP传输协议、UDP传输协议、STCP传输协议、TIPC传输协议等等。</li></ul><h5 id="bind函数"><a href="#bind函数" class="headerlink" title="bind函数"></a>bind函数</h5><p>正如上面所说 <code>bind()</code> 函数把一个地址族中的特定地址赋给 <code>socket</code> 。例如对应 <code>AF_INET</code> 、<code>AF_INET6</code> 就是把一个 <code>ipv4</code> 或 <code>ipv6</code> 地址和端口号组合赋给 <code>socket</code> 。</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">int bind(int sockfd, const struct sockaddr *addr, socklen_t addrlen);</span><br></pre></td></tr></table></figure><h5 id="listen-connect函数"><a href="#listen-connect函数" class="headerlink" title="listen/connect函数"></a>listen/connect函数</h5><p>如果作为一个服务器，在调用socket()、bind()之后就会调用listen()来监听这个socket，如果客户端这时调用connect()发出连接请求，服务器端就会接收到这个请求。</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">int listen(int sockfd, int backlog);</span><br><span class="line">int connect(int sockfd, const struct sockaddr *addr, socklen_t addrlen);</span><br></pre></td></tr></table></figure><p>listen函数的第一个参数即为要监听的socket描述字，第二个参数为相应socket可以排队的最大连接个数。socket()函数创建的socket默认是一个主动类型的，listen函数将socket变为被动类型的，等待客户的连接请求。</p><p>connect函数的第一个参数即为客户端的socket描述字，第二参数为服务器的socket地址，第三个参数为socket地址的长度。客户端通过调用connect函数来建立与TCP服务器的连接。</p><h5 id="accept函数"><a href="#accept函数" class="headerlink" title="accept函数"></a>accept函数</h5><p>TCP客户端依次调用socket()、connect()之后就想TCP服务器发送了一个连接请求。TCP服务器监听到这个请求之后，就会调用accept()函数取接收请求，这样连接就建立好了。之后就可以开始网络I/O操作了，即类同于普通文件的读写I/O操作。如果accpet成功，那么其返回值是由内核自动生成的一个全新的描述字，代表与返回客户的TCP连接。</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">int accept(int sockfd, struct sockaddr *addr, socklen_t *addrlen);</span><br></pre></td></tr></table></figure><h4 id="read-write函数"><a href="#read-write函数" class="headerlink" title="read/write函数"></a>read/write函数</h4><p>至此服务器与客户已经建立好连接了。可以调用网络I/O进行读写操作了，即实现了网咯中不同进程之间的通信！网络I/O操作有下面几组：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">read()&#x2F;write()</span><br><span class="line">recv()&#x2F;send()</span><br><span class="line">readv()&#x2F;writev()</span><br><span class="line">recvmsg()&#x2F;sendmsg()</span><br><span class="line">recvfrom()&#x2F;sendto()</span><br></pre></td></tr></table></figure><hr><h3 id="SOCKET通信中TCP、UDP数据包大小的确定"><a href="#SOCKET通信中TCP、UDP数据包大小的确定" class="headerlink" title="SOCKET通信中TCP、UDP数据包大小的确定"></a>SOCKET通信中TCP、UDP数据包大小的确定</h3><p><code>UDP</code> 和 <code>TCP</code> 协议利用端口号实现多项应用同时发送和接收数据。数据通过源端口发送出去，通过目标端口接收。有的网络应用只能使用预留或注册的静态端口；而另外一些网络应用则可以使用未被注册的动态端口。因为 <code>UDP</code> 和 <code>TCP</code> 报头使用两个字节存放端口号，所以端口号的有效范围是从 <code>0</code> 到 <code>65535</code> 。动态端口的范围是从 <code>1024</code> 到 <code>65535</code> 。</p><h4 id="MTU最大传输单元"><a href="#MTU最大传输单元" class="headerlink" title="MTU最大传输单元"></a>MTU最大传输单元</h4><ul><li>由于以太网最大的数据帧是 <code>1518Bytes</code> ，刨去以太网帧的帧头（DMAC目的MAC地址<strong>6Bytes</strong>+SMAC源MAC地址<strong>6Bytes</strong>+Type域<strong>2bytes</strong>）<strong>14Bytes</strong>和<strong>帧尾CRC校验部分4Bytes</strong> 那么剩下承载上层协议的地方也就是Data域最大就只能有<strong>1500Bytes</strong>这个值我们就把它称之为<strong>MTU</strong>。</li><li>数据链路层规定了<strong>数据帧中数据字段的最大长度 MTU</strong> ，最常用的以太网规定 <strong>MTU = 1500</strong> 字节，若长度超过 <code>MTU</code> 值，就必须把数据报分片处理。</li><li>虽然尽可能长的 <code>IP</code> 数据报会使传输效率变高（首部长度占比小），但数据报短路由器转发速度也更快。</li></ul><p>因此：</p><p><strong>UDP</strong> 包的大小就应该是 <strong>1500 - IP头(20) - UDP头(8) = 1472(BYTES)</strong><br><strong>TCP</strong> 包的大小就应该是 <strong>1500 - IP头(20) - TCP头(20) = 1460 (BYTES)</strong></p><p>在用 <code>Socket</code> 编程时， <code>UDP</code>协议要求包小于 <code>64K</code> ，<code>TCP</code>没有限定。</p><p>不过鉴于Internet上的<strong>标准MTU</strong>值为<strong>576字节</strong>，所以建议在进行Internet的UDP编程时，最好将UDP的数据长度控制在**548字节 (576-8-20)**以内。</p><hr><h2 id="IP-网络层"><a href="#IP-网络层" class="headerlink" title="IP/网络层"></a>IP/网络层</h2><h4 id="地址解析协议-ARP"><a href="#地址解析协议-ARP" class="headerlink" title="地址解析协议 ARP"></a>地址解析协议 ARP</h4><h5 id="计算机网络是如何根据IP地址定位到一台主机的"><a href="#计算机网络是如何根据IP地址定位到一台主机的" class="headerlink" title="计算机网络是如何根据IP地址定位到一台主机的"></a>计算机网络是如何根据IP地址定位到一台主机的</h5><p><img src="https://ifwant-pic-1303131123.cos.ap-nanjing.myqcloud.com/photo/1568551356084.394e4465.png" alt="1568551356084"></p><ul><li><p>APR 协议作用：已经知道一个机器的IP 地址，需要找出对应的硬件地址。</p><ul><li><strong>RARP 协议</strong>：现在的 <code>DHCP</code> 协议包含逆地址解析协议功能，即从<strong>硬件地址</strong>找出其 <strong>IP 地址</strong>。</li></ul></li><li><p>ARP 协议工作原理：网络层使用IP地址，但实际网络链路上传送数据帧时，最终必须使用该网络的硬件地址(MAC)。ARP解决方法是在主机 ARP 高速缓存中存放一个从IP地址到硬件地址的映射表，并实时动态更新（新增或超时删除）。</p><ul><li>当主机 A 要向主机 B 发送 <code>IP</code> 数据报时，就在其 <code>ARP</code> 高速缓存中查看有无主机 B 的 <code>IP</code> 地址，如有就查找并<strong>写入 MAC 帧</strong>，并传输。</li></ul></li><li><p>有可能查不到主机 B 的 <code>IP</code> 地址，可能主机 B 刚入网，可能主机 A 刚加电（高速缓存为空）。在这种情况下，主机 A 就自动运行 <code>ARP</code>，通过广播请求分组找出主机 B 的硬件地址。</p></li><li><p>ARP 协议工作流程：</p><ol><li>主机 A 在本局域网上发送一个 <code>ARP</code> 请求分组，在本局域网上所有主机上运行的 <code>ARP</code> 进程都收到此请求分组。</li><li>主机 B 收下这个 <code>ARP</code> 请求，并向主机 A 发送响应分组（普通的单播，发送给主机 A ）。同时，把请求分组里 A 的 <code>IP</code> 地址到硬件地址的映射写入主机 B 的 <code>ARP</code> 高速缓存中。</li><li>主机 A 收到主机 B 的响应分组后，就在 <code>ARP</code> 高速缓存中写入主机 B 的 <code>IP</code> 地址到硬件地址的映射。</li></ol></li><li><p><strong>ARP生存时间：</strong> 超过生存时间（例如 10 ~ 20 分钟）的项目从高速缓存中删掉。这样如果 B 更换了设备， A 无法找到原主机 B ，但过了一段时间， A 的 <code>ARP</code> 高速缓存中已经删除了 B 原先硬件地址，于是 A 重新广播发送 <code>ARP</code> 请求分组，找到新的 B 硬件地址。</p></li></ul><hr><h4 id="ICMP-IGMP"><a href="#ICMP-IGMP" class="headerlink" title="ICMP/IGMP"></a>ICMP/IGMP</h4><p><img src="https://ifwant-pic-1303131123.cos.ap-nanjing.myqcloud.com/photo/1568288257688.197541c0.png" alt="1568288257688"></p><h5 id="网际控制报文协议-ICMP"><a href="#网际控制报文协议-ICMP" class="headerlink" title="网际控制报文协议 ICMP"></a>网际控制报文协议 <strong>ICMP</strong></h5><ul><li><h4 id="ICMP-报文种类"><a href="#ICMP-报文种类" class="headerlink" title="ICMP 报文种类"></a>ICMP 报文种类</h4><ul><li><p><strong>ICMP 差错报告报文和 ICMP 询问报文</strong></p><p><img src="https://ifwant-pic-1303131123.cos.ap-nanjing.myqcloud.com/photo/1568635687315.e67e9b59.png" alt="1568635687315"></p></li></ul></li><li><p><strong>ICMP应用：</strong></p><ul><li><strong>分组网间探测 PING (Packet Internet Groper)**：使用了 **ICMP</strong> 的回送请求和回送回答报文。</li></ul></li></ul><h5 id="网际组管理协议-IGMP"><a href="#网际组管理协议-IGMP" class="headerlink" title="网际组管理协议 IGMP"></a>网际组管理协议 <strong>IGMP</strong></h5><h4 id="IP-数据报的格式"><a href="#IP-数据报的格式" class="headerlink" title="IP 数据报的格式"></a>IP 数据报的格式</h4><p><img src="https://ifwant-pic-1303131123.cos.ap-nanjing.myqcloud.com/photo/01094.jpg" alt="img"></p><h5 id="首部固定字段"><a href="#首部固定字段" class="headerlink" title="首部固定字段"></a>首部固定字段</h5><ol><li><p><strong>版本：</strong> 占 4 位；指 IP 协议版本。通信双方使用的 IP 协议版本需一致。</p></li><li><p><strong>首部长度：</strong> 占 4 位；单位是 32 位字（4 字节）。</p></li><li><p><strong>区分服务：</strong> 占 8 位；只有在使用区分服务时才使用，一般情况不使用。</p></li><li><p>总长度：</p></li></ol><p>   占 16 位；指首部和数据长度之和；单位为字节，因此数据报最大长度为 $2^{16}-1=65536$字节</p><ul><li>数据链路层规定了<strong>数据帧中数据字段的最大长度 MTU</strong> ，最常用的以太网规定 MTU = 1500 字节，若长度超过 MTU 值，就必须把数据报分片处理。</li><li>虽然尽可能长的 IP 数据报会使传输效率变高（首部长度占比小），但数据报短路由器转发速度也更快。</li></ul><ol start="5"><li><p>标识</p><p>：占 16 位。 每产生一个 IP 数据报，计数器 +1。</p><ul><li>这个标识并不是序号（网络层不负责按序接收）；</li><li>在由于长度超过 MTU 而被分片时，标识字段值被复制到所有分片的标识字段，使各数据报片最后能正确地组装成原来的数据报。</li></ul></li><li><p>标志</p><p>：占 3 位，目前仅前 2 位有意义。</p><ul><li>最低位记为 MF (More Fragment) ，MF = 1 表示后面“还有分片”数据报，MF = 0 表示这是若干数据报片中的最后一个。</li><li>中间一位记为 DF (Don’t Fragment) ， DF = 1 表示不能分片，DF = 0 才允许分片。</li></ul></li><li><p>片偏移：</p></li></ol><p>   占 13 位；表示数据报在分片后，某片在原分组中的相对位置；单位是 8 字节。</p><ul><li>每个分片的长度一定是 8 字节（64 位）的整数倍。</li></ul><ol start="8"><li><p>生存时间（TTL）</p><p>：占 8 位；表示数据报在网络中的寿命。防止数据报在互联网中无限制兜圈子。</p><ul><li>最初设计 TTL 以秒为单位，消耗时间小于 1s 则将 TTL 减 1 。</li><li>目前路由器处理数据报时间一般远远小于 1s ，后来 TTL 字段功能改为<strong>“跳数限制”</strong>，路由器每次转发数据之前把 TTL 减 1 ，当 TTL = 0 就丢失这个数据报。</li><li>数据报能在互联网中经过路由器的最大数值是 $255=(2^8 - 1)$。若设置 TTL = 1 ，则表示此数据报只能在本局域网中传送。</li></ul></li><li><p><strong>协议：</strong> 占 8 位；表示此数据报中携带的数据使用何种协议，以便接收方主机 IP 层直到应将数据上交给哪个协议处理。</p></li><li><p><strong>首部校验和：</strong> 占 16 位；只检验数据报的首部，不包括数据部分（减少计算量）。</p></li><li><p><strong>源地址：</strong> 占 32 位；</p></li><li><p><strong>目的地址：</strong> 占 32 位。</p></li></ol><h5 id="首部可变部分"><a href="#首部可变部分" class="headerlink" title="首部可变部分"></a>首部可变部分</h5><ul><li>IP 数据报首部的可变部分就是一个<strong>选项字段</strong>，用来支持<strong>排错、测量、安全措施</strong>等。</li><li>长度 <strong>1 - 40</strong> 字节，取决于选择的项目。</li><li>可变部分是用来增加 IP 数据报的功能，实际上这些选项很少被使用。很多路由器都不考虑 IP 首部的选项字段，因此新的 IP 版本 <strong>IPv6 就将 IP 数据报的首部长度做成固定的</strong>。</li></ul><hr><h3 id="交换机和路由器的区别"><a href="#交换机和路由器的区别" class="headerlink" title="交换机和路由器的区别"></a>交换机和路由器的区别</h3><ol><li>工作层次：交换机主要工作在数据链路层，路由器主要工作在网络层 </li><li>转发依据：交换机转发依据的对象是MAC地址，路由器转发依据的对象是IP地址 </li><li>功能：交换机功能较简单，只是将主机连接起来组件局域网，路由器可以将局域网连接起来，还能分割广播域，还能提供防火墙 </li></ol><h3 id="MAC表、路由表、ARP表的字段"><a href="#MAC表、路由表、ARP表的字段" class="headerlink" title="MAC表、路由表、ARP表的字段"></a>MAC表、路由表、ARP表的字段</h3><ul><li><p>MAC表 </p><p><img src="https://ifwant-pic-1303131123.cos.ap-nanjing.myqcloud.com/photo/01096.jpg" alt="img">  </p></li><li><p>路由表 </p><img src="https://ifwant-pic-1303131123.cos.ap-nanjing.myqcloud.com/photo/01097.jpg" alt="img" style="zoom:80%;" />  <img src="https://ifwant-pic-1303131123.cos.ap-nanjing.myqcloud.com/photo/01098.jpg" alt="img" style="zoom:80%;" />  </li><li><p>ARP表 </p><p><img src="https://ifwant-pic-1303131123.cos.ap-nanjing.myqcloud.com/photo/01099.jpg" alt="img">  </p></li></ul>]]></content>
    
    
      
      
    <summary type="html">&lt;p&gt;&lt;img src=&quot;https://ifwant-pic-1303131123.cos.ap-nanjing.myqcloud.com/photo/640.png&quot;&gt;&lt;/p&gt;
&lt;h2 id=&quot;HTTP&quot;&gt;&lt;a href=&quot;#HTTP&quot; class=&quot;headerlink&quot; </summary>
      
    
    
    
    
    <category term="学习" scheme="https://ifwant.tech/tags/%E5%AD%A6%E4%B9%A0/"/>
    
  </entry>
  
  <entry>
    <title>捐献</title>
    <link href="https://ifwant.tech/2021/01/03/suixiang/"/>
    <id>https://ifwant.tech/2021/01/03/suixiang/</id>
    <published>2021-01-02T16:28:39.000Z</published>
    <updated>2021-01-02T16:33:25.741Z</updated>
    
    <content type="html"><![CDATA[<p>无他</p><p>登记了器官和眼角膜捐献</p><p>以上</p><p><img src="https://ifwant-pic-1303131123.cos.ap-nanjing.myqcloud.com/photo/juanxian.jpg" alt="电子卡"></p>]]></content>
    
    
      
      
    <summary type="html">&lt;p&gt;无他&lt;/p&gt;
&lt;p&gt;登记了器官和眼角膜捐献&lt;/p&gt;
&lt;p&gt;以上&lt;/p&gt;
&lt;p&gt;&lt;img src=&quot;https://ifwant-pic-1303131123.cos.ap-nanjing.myqcloud.com/photo/juanxian.jpg&quot; alt=&quot;电子卡&quot;&gt;</summary>
      
    
    
    
    
    <category term="随笔" scheme="https://ifwant.tech/tags/%E9%9A%8F%E7%AC%94/"/>
    
  </entry>
  
  <entry>
    <title>零散的Java语法和类笔记</title>
    <link href="https://ifwant.tech/2021/01/01/other/"/>
    <id>https://ifwant.tech/2021/01/01/other/</id>
    <published>2021-01-01T02:37:39.000Z</published>
    <updated>2021-01-01T07:04:15.629Z</updated>
    
    <content type="html"><![CDATA[<h1 id="String-StringBuilder和StringBuffer"><a href="#String-StringBuilder和StringBuffer" class="headerlink" title="String, StringBuilder和StringBuffer"></a>String, StringBuilder和StringBuffer</h1><p>String ：不可改变的，<br>StringBuilder ：可改变，速度快，线程不安全<br>StringBuffer：可改变，线程安全</p><h2 id="String"><a href="#String" class="headerlink" title="String"></a>String</h2><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">String str=<span class="string">&quot;abc&quot;</span>;<span class="comment">//创建在方法区的常量池里，相同值则共用</span></span><br><span class="line">String str=<span class="keyword">new</span> String(<span class="string">&quot;abc&quot;</span>);<span class="comment">//创建在堆上，每个对象单独分配</span></span><br><span class="line">str.length();<span class="comment">//字符串长度</span></span><br><span class="line">str.concat(str);<span class="comment">//拼接字符串</span></span><br><span class="line">str.substring(<span class="number">0</span>,n);<span class="comment">//子字符串，左闭右开</span></span><br><span class="line">str.charAt();<span class="comment">//索引处char值</span></span><br></pre></td></tr></table></figure><h2 id="StringBuilder"><a href="#StringBuilder" class="headerlink" title="StringBuilder"></a>StringBuilder</h2><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line">StringBuilder str=<span class="keyword">new</span> StringBuilder(<span class="string">&quot;abc&quot;</span>);</span><br><span class="line">StringBuilder str=<span class="keyword">new</span> StringBuilder(<span class="number">12</span>);<span class="comment">//创建时也可以先开辟空间，再append进去字符</span></span><br><span class="line">str.length();<span class="comment">//字符串长度</span></span><br><span class="line">str.append(<span class="string">&quot;efg&quot;</span>);<span class="comment">//追加字符</span></span><br><span class="line">str.delete(<span class="number">0</span>,n);<span class="comment">//删除字符，左闭右开</span></span><br><span class="line">str.insert(<span class="number">0</span>,<span class="string">&quot;aaa&quot;</span>);<span class="comment">//插入字符</span></span><br><span class="line">str.toString();<span class="comment">//转String</span></span><br><span class="line">str.reverse();<span class="comment">//反转</span></span><br><span class="line">str.substring(<span class="number">0</span>,n);<span class="comment">//子字符串，左闭右开</span></span><br></pre></td></tr></table></figure><h1 id="Arrays"><a href="#Arrays" class="headerlink" title="Arrays"></a>Arrays</h1><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">Arrays.sort();<span class="comment">//数组排序</span></span><br><span class="line">Arrays.copyOfRange (array,start,end);<span class="comment">//复制数组</span></span><br><span class="line">Arrays.toString();<span class="comment">//转String</span></span><br></pre></td></tr></table></figure>]]></content>
    
    
      
      
    <summary type="html">&lt;h1 id=&quot;String-StringBuilder和StringBuffer&quot;&gt;&lt;a href=&quot;#String-StringBuilder和StringBuffer&quot; class=&quot;headerlink&quot; title=&quot;String, StringBuilder和Stri</summary>
      
    
    
    
    
    <category term="学习" scheme="https://ifwant.tech/tags/%E5%AD%A6%E4%B9%A0/"/>
    
    <category term="JAVA" scheme="https://ifwant.tech/tags/JAVA/"/>
    
  </entry>
  
  <entry>
    <title>Java集合总结</title>
    <link href="https://ifwant.tech/2020/12/28/collection/"/>
    <id>https://ifwant.tech/2020/12/28/collection/</id>
    <published>2020-12-28T14:25:39.000Z</published>
    <updated>2021-01-01T03:12:18.033Z</updated>
    
    <content type="html"><![CDATA[<table><thead><tr><th></th><th>Stack</th><th>ArrayList</th><th>LinkedList</th><th>HashMap</th><th>HashSet</th><th>Queue</th></tr></thead><tbody><tr><td>添加元素</td><td>push</td><td>add, addAll</td><td>add, addAll</td><td>put</td><td>add</td><td>offer</td></tr><tr><td>删除元素</td><td>pop</td><td>remove</td><td>remove</td><td></td><td>remove</td><td>poll</td></tr><tr><td>查看第一个元素</td><td>peek</td><td></td><td></td><td></td><td></td><td>peek</td></tr><tr><td>查看指定元素</td><td></td><td>get</td><td>get</td><td></td><td></td><td></td></tr><tr><td>修改元素</td><td></td><td>set</td><td>set</td><td></td><td></td><td></td></tr><tr><td>大小</td><td>size</td><td>size</td><td>size</td><td></td><td>size</td><td>size</td></tr><tr><td>判断元素是否存在</td><td>search</td><td>contains</td><td>contains</td><td></td><td>contains</td><td></td></tr><tr><td>是否为空</td><td>empty</td><td>isEmpty</td><td>isEmpty</td><td></td><td></td><td></td></tr><tr><td>线程安全</td><td>是</td><td>否</td><td>否</td><td>否</td><td>否</td><td></td></tr></tbody></table><p>初始化：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">Stack&lt;Object&gt; <span class="keyword">var</span>=<span class="keyword">new</span> Stack&lt;&gt;();</span><br><span class="line">ArrayList&lt;Object&gt; <span class="keyword">var</span>=<span class="keyword">new</span> ArrayList&lt;&gt;();</span><br><span class="line">LinkedList&lt;Object&gt; <span class="keyword">var</span>=<span class="keyword">new</span> LinkedList&lt;&gt;();</span><br><span class="line">HashSet&lt;Object&gt; <span class="keyword">var</span>=<span class="keyword">new</span> HashSet&lt;&gt;();</span><br><span class="line"></span><br><span class="line">Queue&lt;Object&gt; <span class="keyword">var</span>=<span class="keyword">new</span> LinkedList&lt;&gt;();</span><br><span class="line">HashMap&lt;Key,Value&gt; <span class="keyword">var</span>=<span class="keyword">new</span> HashMap&lt;&gt;();</span><br></pre></td></tr></table></figure><h1 id="Stack"><a href="#Stack" class="headerlink" title="Stack"></a>Stack</h1><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">search(Object)<span class="comment">//查找元素所在位置，栈顶为1，不存在则返回-1</span></span><br></pre></td></tr></table></figure><h1 id="ArrayList"><a href="#ArrayList" class="headerlink" title="ArrayList"></a>ArrayList</h1><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">add(value)<span class="comment">//末尾添加元素</span></span><br><span class="line">addAll(index,collection)<span class="comment">//添加集合的所有元素到index（可省略）之后</span></span><br><span class="line">remove(index)<span class="comment">//删除元素,参数可以是元素，也可以是索引</span></span><br><span class="line">get(index)<span class="comment">//访问元素</span></span><br><span class="line">set(index,value)<span class="comment">//修改元素</span></span><br><span class="line">size()<span class="comment">//大小</span></span><br></pre></td></tr></table></figure><h1 id="LinkedList"><a href="#LinkedList" class="headerlink" title="LinkedList"></a>LinkedList</h1><p>同ArrayList</p><h1 id="HashMap"><a href="#HashMap" class="headerlink" title="HashMap"></a>HashMap</h1><h1 id="HashSet"><a href="#HashSet" class="headerlink" title="HashSet"></a>HashSet</h1><h1 id="Queue"><a href="#Queue" class="headerlink" title="Queue"></a>Queue</h1><h2 id="PriorityQueue"><a href="#PriorityQueue" class="headerlink" title="PriorityQueue"></a>PriorityQueue</h2><h1 id="Collections"><a href="#Collections" class="headerlink" title="Collections"></a>Collections</h1><h2 id="reverse"><a href="#reverse" class="headerlink" title="reverse"></a>reverse</h2><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">Collections.reverse();<span class="comment">//反转</span></span><br></pre></td></tr></table></figure><h2 id="sort"><a href="#sort" class="headerlink" title="sort"></a>sort</h2><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">//重写sorts函数的比较函数</span></span><br><span class="line">Comparator&lt;Integer&gt; cmp=<span class="keyword">new</span> Comparator&lt;Integer&gt;()&#123;</span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">int</span> <span class="title">compare</span><span class="params">(Integer a, Integer b)</span></span>&#123;</span><br><span class="line">        <span class="keyword">return</span> b-a;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br><span class="line"><span class="comment">//返回值为负代表当前对象位置要排在前</span></span><br><span class="line"></span><br><span class="line"><span class="comment">//对于String</span></span><br><span class="line">Comparator&lt;String&gt; myCmp=<span class="keyword">new</span> Comparator&lt;String&gt;()&#123;</span><br><span class="line">        <span class="meta">@Override</span></span><br><span class="line">        <span class="function"><span class="keyword">public</span> <span class="keyword">int</span> <span class="title">compare</span><span class="params">(String a,String b)</span></span>&#123;</span><br><span class="line">            <span class="keyword">return</span> a.compareTo(b);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br><span class="line"><span class="comment">//compareTo函数，当前值小于参数值，返回负数</span></span><br></pre></td></tr></table></figure>]]></content>
    
    
      
      
    <summary type="html">&lt;table&gt;
&lt;thead&gt;
&lt;tr&gt;
&lt;th&gt;&lt;/th&gt;
&lt;th&gt;Stack&lt;/th&gt;
&lt;th&gt;ArrayList&lt;/th&gt;
&lt;th&gt;LinkedList&lt;/th&gt;
&lt;th&gt;HashMap&lt;/th&gt;
&lt;th&gt;HashSet&lt;/th&gt;
&lt;th&gt;Queue&lt;/th&gt;
&lt;/tr&gt;
</summary>
      
    
    
    
    
    <category term="学习" scheme="https://ifwant.tech/tags/%E5%AD%A6%E4%B9%A0/"/>
    
    <category term="JAVA" scheme="https://ifwant.tech/tags/JAVA/"/>
    
  </entry>
  
  <entry>
    <title>二叉树遍历</title>
    <link href="https://ifwant.tech/2020/12/28/binaryrteebianli/"/>
    <id>https://ifwant.tech/2020/12/28/binaryrteebianli/</id>
    <published>2020-12-28T04:27:39.000Z</published>
    <updated>2020-12-28T13:15:31.260Z</updated>
    
    <content type="html"><![CDATA[<p>二叉树结点结构</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">TreeNode</span> </span>&#123;</span><br><span class="line">    <span class="keyword">int</span> val;</span><br><span class="line">    TreeNode left;</span><br><span class="line">    TreeNode right;</span><br><span class="line">    TreeNode(<span class="keyword">int</span> x)&#123;</span><br><span class="line">        val=x;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> String <span class="title">toString</span><span class="params">()</span></span>&#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="string">&quot;val: &quot;</span>+val;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>访问函数</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">visit</span><span class="params">(TreeNode node)</span></span>&#123;</span><br><span class="line">    System.out.print(node.val+<span class="string">&quot; &quot;</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h1 id="前序遍历"><a href="#前序遍历" class="headerlink" title="前序遍历"></a>前序遍历</h1><h2 id="递归实现"><a href="#递归实现" class="headerlink" title="递归实现"></a>递归实现</h2><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">preOrderRecursion</span><span class="params">(TreeNode node)</span></span>&#123;</span><br><span class="line">    <span class="keyword">if</span>(node==<span class="keyword">null</span>)<span class="keyword">return</span>;</span><br><span class="line">    visit(node);</span><br><span class="line">    preOrderRecursion(node.left);</span><br><span class="line">    preOrderRecursion(node.right);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h2 id="非递归实现"><a href="#非递归实现" class="headerlink" title="非递归实现"></a>非递归实现</h2><p>使用栈:根节点出栈，右节点入栈，左节点入栈</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Solution</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> List&lt;Integer&gt; <span class="title">preorderTraversal</span><span class="params">(TreeNode root)</span> </span>&#123;</span><br><span class="line">        List&lt;Integer&gt; result=<span class="keyword">new</span> ArrayList&lt;Integer&gt;();</span><br><span class="line">        Stack&lt;TreeNode&gt; preStack=<span class="keyword">new</span> Stack&lt;TreeNode&gt;();</span><br><span class="line">        <span class="keyword">if</span>(root==<span class="keyword">null</span>)<span class="keyword">return</span> result;</span><br><span class="line">        preStack.push(root);</span><br><span class="line">        <span class="keyword">while</span>(!preStack.isEmpty())&#123;</span><br><span class="line">            TreeNode nodeNow=preStack.pop();</span><br><span class="line">            result.add(nodeNow.val);</span><br><span class="line">            <span class="keyword">if</span>(nodeNow.right!=<span class="keyword">null</span>)preStack.push(nodeNow.right);</span><br><span class="line">            <span class="keyword">if</span>(nodeNow.left!=<span class="keyword">null</span>)preStack.push(nodeNow.left);</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> result;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h1 id="中序遍历"><a href="#中序遍历" class="headerlink" title="中序遍历"></a>中序遍历</h1><h2 id="非递归实现-1"><a href="#非递归实现-1" class="headerlink" title="非递归实现"></a>非递归实现</h2><p>使用栈+双层循环：内层循环不停寻找左孩子并入栈，直到没有左孩子；外层循环每次pop出栈顶元素，并都把右孩子（如果有）入栈。</p><p>注意一点：外层循环的判断条件是或，即只要栈非空或者结点有右孩子，就继续循环。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br></pre></td><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Solution</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> List&lt;Integer&gt; <span class="title">inorderTraversal</span><span class="params">(TreeNode root)</span> </span>&#123;</span><br><span class="line">        List&lt;Integer&gt; result=<span class="keyword">new</span> ArrayList&lt;Integer&gt;();</span><br><span class="line">        Stack&lt;TreeNode&gt; inStack=<span class="keyword">new</span> Stack&lt;TreeNode&gt;();</span><br><span class="line">        TreeNode currentNode=root;</span><br><span class="line">        <span class="keyword">while</span>(!inStack.isEmpty()||currentNode!=<span class="keyword">null</span>)&#123;      </span><br><span class="line">            <span class="keyword">while</span>(currentNode!=<span class="keyword">null</span>)&#123;</span><br><span class="line">                inStack.push(currentNode);</span><br><span class="line">                currentNode=currentNode.left;</span><br><span class="line">            &#125;</span><br><span class="line">            currentNode=inStack.pop();</span><br><span class="line">            result.add(currentNode.val);</span><br><span class="line">            currentNode=currentNode.right;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> result;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h1 id="后序遍历"><a href="#后序遍历" class="headerlink" title="后序遍历"></a>后序遍历</h1><h2 id="非递归实现-2"><a href="#非递归实现-2" class="headerlink" title="非递归实现"></a>非递归实现</h2><p>使用一个栈和一个hashmap，hashmap记录节点是否被访问过。</p><p>四种情况访问这个节点：</p><ul><li><p>没有左右孩子</p></li><li><p>只有左孩子且左孩子已被访问</p></li><li><p>只有右孩子且右孩子已被访问</p></li><li><p>有左右孩子且左右孩子都被访问</p></li></ul><p>左孩子未被访问，则不断把左孩子入栈</p><p>右孩子未被访问，把这个右孩子入栈</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br></pre></td><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Solution</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> List&lt;Integer&gt; <span class="title">postorderTraversal</span><span class="params">(TreeNode root)</span> </span>&#123;</span><br><span class="line">        List&lt;Integer&gt; result=<span class="keyword">new</span> ArrayList&lt;Integer&gt;();</span><br><span class="line">        Stack&lt;TreeNode&gt; postStack=<span class="keyword">new</span> Stack&lt;TreeNode&gt;();</span><br><span class="line">        Map&lt;TreeNode,Integer&gt; visitedNode=<span class="keyword">new</span> HashMap&lt;TreeNode,Integer&gt;();</span><br><span class="line">        <span class="keyword">if</span>(root==<span class="keyword">null</span>)<span class="keyword">return</span> result;</span><br><span class="line">        postStack.push(root);</span><br><span class="line">        <span class="keyword">while</span>(!postStack.isEmpty())&#123;</span><br><span class="line">            TreeNode currentNode=postStack.peek();</span><br><span class="line">            <span class="keyword">if</span>( (currentNode.left==<span class="keyword">null</span>&amp;&amp;currentNode.right==<span class="keyword">null</span>)||</span><br><span class="line">                (currentNode.left==<span class="keyword">null</span>&amp;&amp;currentNode.right!=<span class="keyword">null</span>&amp;&amp;visitedNode.containsKey(currentNode.right))||</span><br><span class="line">                (currentNode.right==<span class="keyword">null</span>&amp;&amp;currentNode.left!=<span class="keyword">null</span>&amp;&amp;visitedNode.containsKey(currentNode.left))||</span><br><span class="line">                (currentNode.right!=<span class="keyword">null</span>&amp;&amp;currentNode.left!=<span class="keyword">null</span>&amp;&amp;visitedNode.containsKey(currentNode.left)&amp;&amp;visitedNode.containsKey(currentNode.right)))&#123;</span><br><span class="line">                result.add(currentNode.val);</span><br><span class="line">                visitedNode.put(currentNode,<span class="number">1</span>);</span><br><span class="line">                postStack.pop();</span><br><span class="line">                <span class="keyword">continue</span>;</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="keyword">if</span>(currentNode.left!=<span class="keyword">null</span>&amp;&amp;(!visitedNode.containsKey(currentNode.left)))&#123;</span><br><span class="line">                <span class="keyword">while</span>(currentNode.left!=<span class="keyword">null</span>)&#123;</span><br><span class="line">                    postStack.push(currentNode.left);</span><br><span class="line">                    currentNode=currentNode.left;</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="keyword">if</span>(currentNode.right!=<span class="keyword">null</span>&amp;&amp;(!visitedNode.containsKey(currentNode.right)))&#123;</span><br><span class="line">                postStack.push(currentNode.right);</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> result;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h1 id="层序遍历"><a href="#层序遍历" class="headerlink" title="层序遍历"></a>层序遍历</h1><h2 id="非递归实现-3"><a href="#非递归实现-3" class="headerlink" title="非递归实现"></a>非递归实现</h2><ul><li>使用队列，每次结点出栈后将左右结点依次入队</li><li>使用变量<code>levelNum</code>保存当前层的结点个数</li></ul><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br></pre></td><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Solution</span> </span>&#123;</span><br><span class="line">    <span class="keyword">public</span> List&lt;List&lt;Integer&gt;&gt; levelOrder(TreeNode root) &#123;</span><br><span class="line">        List&lt;List&lt;Integer&gt;&gt; result=<span class="keyword">new</span> ArrayList&lt;&gt;();</span><br><span class="line">        Queue&lt;TreeNode&gt; nodeQueue=<span class="keyword">new</span> LinkedList&lt;TreeNode&gt;();</span><br><span class="line">        <span class="keyword">if</span>(root==<span class="keyword">null</span>)<span class="keyword">return</span> result;</span><br><span class="line">        nodeQueue.offer(root);</span><br><span class="line">        <span class="keyword">int</span> levelNum=<span class="number">1</span>;</span><br><span class="line">        <span class="keyword">while</span>(!nodeQueue.isEmpty())&#123;</span><br><span class="line">            List&lt;Integer&gt; levelList=<span class="keyword">new</span> ArrayList&lt;&gt;();</span><br><span class="line">            <span class="keyword">while</span>(levelNum!=<span class="number">0</span>)&#123;</span><br><span class="line">                TreeNode tmp=nodeQueue.poll();</span><br><span class="line">                <span class="keyword">if</span>(tmp.left!=<span class="keyword">null</span>)&#123;</span><br><span class="line">                    nodeQueue.offer(tmp.left);</span><br><span class="line">                &#125;</span><br><span class="line">                <span class="keyword">if</span>(tmp.right!=<span class="keyword">null</span>)&#123;</span><br><span class="line">                    nodeQueue.offer(tmp.right);</span><br><span class="line">                &#125;</span><br><span class="line">                levelList.add(tmp.val);</span><br><span class="line">                levelNum--;</span><br><span class="line">            &#125;</span><br><span class="line">            result.add(levelList);</span><br><span class="line">            levelNum=nodeQueue.size();</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> result;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>]]></content>
    
    
      
      
    <summary type="html">&lt;p&gt;二叉树结点结构&lt;/p&gt;
&lt;figure class=&quot;highlight java&quot;&gt;&lt;table&gt;&lt;tr&gt;&lt;td class=&quot;gutter&quot;&gt;&lt;pre&gt;&lt;span class=&quot;line&quot;&gt;1&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;2&lt;/span&gt;&lt;</summary>
      
    
    
    
    
    <category term="学习" scheme="https://ifwant.tech/tags/%E5%AD%A6%E4%B9%A0/"/>
    
    <category term="JAVA" scheme="https://ifwant.tech/tags/JAVA/"/>
    
  </entry>
  
  <entry>
    <title>排序算法归纳</title>
    <link href="https://ifwant.tech/2020/12/25/sort/"/>
    <id>https://ifwant.tech/2020/12/25/sort/</id>
    <published>2020-12-25T01:28:39.000Z</published>
    <updated>2020-12-25T02:07:51.974Z</updated>
    
    <content type="html"><![CDATA[<h1 id="插入排序"><a href="#插入排序" class="headerlink" title="插入排序"></a>插入排序</h1><p>将一个记录插入到已经排好序的有序表中，从而得到一个新的、记录数增 1 的有序表。</p><p>使用双层循环</p><ul><li>外层循环对除了第一个元素之外的所有元素</li><li>内层循环将当前元素和前面的元素比较，不满足排序要求则交换这两个元素位置。</li></ul><p>时间复杂度 <strong>O(n^2)</strong>   空间复杂度<strong>O(1)</strong></p><p>伪代码：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">for</span> (<span class="keyword">int</span> i=<span class="number">1</span>; i&lt;length; ++i) &#123;</span><br><span class="line">    <span class="keyword">for</span> ( j = i-<span class="number">1</span>; j &gt;= <span class="number">0</span>; --j)&#123;</span><br><span class="line">        <span class="keyword">if</span>(array[j]和array[j+<span class="number">1</span>]不满足排序顺序)&#123;</span><br><span class="line">            swap(array,j,j+<span class="number">1</span>);</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">else</span>&#123;</span><br><span class="line">            <span class="keyword">break</span>;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125; </span><br></pre></td></tr></table></figure><p>过程图示：</p><p><img src="https://ifwant-pic-1303131123.cos.ap-nanjing.myqcloud.com/photo/InsertSort-01.png" alt="insert1"></p><p><img src="https://ifwant-pic-1303131123.cos.ap-nanjing.myqcloud.com/photo/InsertSort-02.png" alt="insert2"></p><p><img src="https://ifwant-pic-1303131123.cos.ap-nanjing.myqcloud.com/photo/InsertSort-03.png" alt="insert3"></p><p><img src="https://ifwant-pic-1303131123.cos.ap-nanjing.myqcloud.com/photo/InsertSort-04.png" alt="insert4"></p>]]></content>
    
    
      
      
    <summary type="html">&lt;h1 id=&quot;插入排序&quot;&gt;&lt;a href=&quot;#插入排序&quot; class=&quot;headerlink&quot; title=&quot;插入排序&quot;&gt;&lt;/a&gt;插入排序&lt;/h1&gt;&lt;p&gt;将一个记录插入到已经排好序的有序表中，从而得到一个新的、记录数增 1 的有序表。&lt;/p&gt;
&lt;p&gt;使用双层循环&lt;/p&gt;
&lt;ul</summary>
      
    
    
    
    
    <category term="学习" scheme="https://ifwant.tech/tags/%E5%AD%A6%E4%B9%A0/"/>
    
    <category term="算法" scheme="https://ifwant.tech/tags/%E7%AE%97%E6%B3%95/"/>
    
  </entry>
  
  <entry>
    <title>Java高并发程序设计（一）：类加载器</title>
    <link href="https://ifwant.tech/2020/12/24/gbf_1/"/>
    <id>https://ifwant.tech/2020/12/24/gbf_1/</id>
    <published>2020-12-24T07:24:39.000Z</published>
    <updated>2021-01-19T09:23:33.887Z</updated>
    
    <content type="html"><![CDATA[<h1 id="基本概念"><a href="#基本概念" class="headerlink" title="基本概念"></a>基本概念</h1><h2 id="同步和异步"><a href="#同步和异步" class="headerlink" title="同步和异步"></a>同步和异步</h2><p>用来形容方法调用 </p><ul><li>同步方法调用：方法调用开始后，调用者必须等到方法返回后才能继续后续的操作</li><li>异步方法调用：方法调用开始后，异步方法会在另一个线程中执行，调用者可以继续其他操作。当异步方法有返回值时，会在方法调用结束后返回。</li></ul><p>例子：<br>同步调用就像我们在实体店买东西，我们要自己亲自去实体店，买完东西后我们要亲自把东西带回来，而异步就像网购，我们在完成网上支付时买东西这个事件就已经结束了，我们可以去做其他的一些事情，而卖方会将东西给我们送过来。 </p><h2 id="并发和并行"><a href="#并发和并行" class="headerlink" title="并发和并行"></a>并发和并行</h2><ul><li>并发：多个任务交替执行</li><li>并行：多个任务同时执行</li></ul><h2 id="临界区"><a href="#临界区" class="headerlink" title="临界区"></a>临界区</h2><p>共享数据，可以被多个线程使用，但一次只能有一个线程用。</p><h2 id="阻塞和非阻塞"><a href="#阻塞和非阻塞" class="headerlink" title="阻塞和非阻塞"></a>阻塞和非阻塞</h2><p>形容多线程间的相互影响。</p><ul><li><p>阻塞：一个线程访问了临界区，其他需要这个临界区资源的线程必须等待。</p></li><li><p>非阻塞：没有一个线程可以妨碍其他线程的执行，所有线程都会不断的向前执行。</p></li></ul><h2 id="死锁、饥饿和活锁"><a href="#死锁、饥饿和活锁" class="headerlink" title="死锁、饥饿和活锁"></a>死锁、饥饿和活锁</h2><ul><li>死锁：两个或两个以上的进程在执行过程中，由于竞争资源或者由于彼此通信而造成的一种阻塞的现象 。</li><li>饥饿：一个或者多个线程因为种种原因（例如优先级）无法获得所需要的资源，导致一直无法执行。</li><li>活锁：获取不到资源就释放已有资源，一直尝试-失败-尝试-失败。<ul><li>例子：  走路时对面堵着一个人，我向左走想避开他，他向右走想避开我，这样我们就有堵在一起了。  </li></ul></li></ul><h2 id="并发级别"><a href="#并发级别" class="headerlink" title="并发级别"></a>并发级别</h2><ul><li>阻塞：其他线程释放资源前，当前线程无法执行</li><li>无饥饿：线程的调度是公平的，先来先调度，后来后调度，不存在饥饿的现象</li><li>无障碍：所有线程无需等待可以一起进入临界区，一旦临界区的数据遭到破坏，所有线程会回滚，保证数据的安全。</li><li>无锁：无锁是无障碍的一种，但是无锁保证必然有一个线程能够执行成功</li><li>无等待：所有的线程都必须在有限步内完成<ul><li>例子：RCU（Read-Copy-Update）对数据的读可以不加控制。因此，所有的读线程都是无等待的，它们既不会被锁定等待也不会引起任何冲突。 但在写数据的时候，先取得原始数据的副本，接着只修改副本数据（这就是为什么读可以不加控制），修改完成后，在合适的时机回写数据。 </li></ul></li></ul><h2 id="多线程的原子性、可见性和有序性"><a href="#多线程的原子性、可见性和有序性" class="headerlink" title="多线程的原子性、可见性和有序性"></a>多线程的原子性、可见性和有序性</h2><ul><li>原子性：一个操作不可中断</li><li>可见性：一个线程修改了一个共享变量的值，其他线程能否立即知道这个修改。<ul><li>缓存优化、硬件优化、指令重排等都可能导致可见性问题</li></ul></li><li>有序性：指令重排可能导致多线程下先写的代码后运行</li></ul><h2 id="Happen-Before原则"><a href="#Happen-Before原则" class="headerlink" title="Happen-Before原则"></a>Happen-Before原则</h2><p>指令不能重排的原则：</p><ul><li>程序顺序原则∶ 一个线程内保证语义的串行性 </li><li>volatile规则∶volatile变量的写，先发生于读，这保证了volatile变量的可见性</li><li>锁规则∶解锁（unlock）必然发生在随后的加锁（lock）前</li><li>传递性∶ A先于B，B先于C，那么 A 必然先于 C</li><li>线程的 start（）方法先于它的每一个动作 </li><li>线程的所有操作先于线程的终结（Thread.join（））</li><li>线程的中断（interrupt（））先于被中断线程的代码</li><li>对象的构造函数执行、结束先于 finalize（）方法</li></ul><h2 id="线程的状态"><a href="#线程的状态" class="headerlink" title="线程的状态"></a>线程的状态</h2><p>Thread中的 State 枚举中定义</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">enum</span> <span class="title">State</span></span>&#123;</span><br><span class="line">    NEW, </span><br><span class="line">    RUNNABLE, </span><br><span class="line">    BLOCKED, </span><br><span class="line">    WAITING, <span class="comment">//无限时间等待</span></span><br><span class="line">    TIMED_WAITING, <span class="comment">//有限时间等待</span></span><br><span class="line">    TERMINATED;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p><img src="https://ifwant-pic-1303131123.cos.ap-nanjing.myqcloud.com/photo/01184.png"></p><h2 id="线程的基本操作"><a href="#线程的基本操作" class="headerlink" title="线程的基本操作"></a>线程的基本操作</h2><h3 id="新建线程"><a href="#新建线程" class="headerlink" title="新建线程"></a>新建线程</h3><p>new一个Thread对象，然后start()就可以新建一个线程了。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">Thread t1=<span class="keyword">new</span> Thread();</span><br><span class="line">t1.start();</span><br></pre></td></tr></table></figure><p><code>start()</code>之后会调用<code>run()</code>函数，我们需要在run函数中实现自己想要的功能。</p><ul><li>继承Thread类（这里是匿名内部类）重载run</li></ul><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">Thread t1=<span class="keyword">new</span> Thread() &#123;</span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">run</span> <span class="params">()</span> </span>&#123;</span><br><span class="line">        System.out.println(<span class="string">&quot;Hello,I am t1&quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br><span class="line">t1.start ();</span><br></pre></td></tr></table></figure><ul><li>实现Runnable接口</li></ul><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">CreateThread3</span> <span class="keyword">implements</span> <span class="title">Runnable</span></span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span></span>&#123;</span><br><span class="line">        Thread t1=<span class="keyword">new</span> Thread(<span class="keyword">new</span> CreateThread3());</span><br><span class="line">        t1.start();</span><br><span class="line">    &#125;</span><br><span class="line">        </span><br><span class="line">        <span class="meta">@Override</span></span><br><span class="line">        <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">run</span> <span class="params">()</span></span>&#123;</span><br><span class="line">            System.out.println(<span class="string">&quot;Oh,I am Runnable&quot;</span>);</span><br><span class="line">        &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"></span><br></pre></td></tr></table></figure><h3 id="终止线程"><a href="#终止线程" class="headerlink" title="终止线程"></a>终止线程</h3><p>用标记变量来决定是否退出：设置个stopme的boolean，为true就break</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">static</span> <span class="class"><span class="keyword">class</span> <span class="title">ChangeObjectThread</span> <span class="keyword">extends</span> <span class="title">Thread</span></span>&#123;</span><br><span class="line">    <span class="keyword">volatile</span> <span class="keyword">boolean</span> stopme = <span class="keyword">false</span>;</span><br><span class="line">    </span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">stopMe</span><span class="params">()</span> </span>&#123; </span><br><span class="line">        stopme = <span class="keyword">true</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="meta">@override</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">run</span> <span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">while</span> (<span class="keyword">true</span>)&#123;</span><br><span class="line">            <span class="keyword">if</span> (stopme) &#123;</span><br><span class="line">                System.out.println(<span class="string">&quot;exit by stop me&quot;</span>);</span><br><span class="line">                <span class="keyword">break</span>;</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="keyword">synchronized</span> (u)&#123;</span><br><span class="line">                <span class="keyword">int</span> v=(<span class="keyword">int</span>)(System.currentTimeMillis()/ <span class="number">1000</span>); </span><br><span class="line">                u.setId(v);</span><br><span class="line">                <span class="comment">//oh,do sth. else </span></span><br><span class="line">                <span class="keyword">try</span>&#123;</span><br><span class="line">                    Thread.sleep(<span class="number">100</span>);</span><br><span class="line">                &#125;<span class="keyword">catch</span>(InterruptedException e)&#123;</span><br><span class="line">                    e.printStackTrace();</span><br><span class="line">                &#125;</span><br><span class="line">                u.setName(String.valueOf(v));</span><br><span class="line">            &#125;</span><br><span class="line">            Thread.yield();</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="中断线程"><a href="#中断线程" class="headerlink" title="中断线程"></a>中断线程</h3><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">void</span> Thread.interrupt() <span class="comment">//中断线程</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">boolean</span> Thread.isInterrupted() <span class="comment">//判断线程是否中断</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">boolean</span> Thread.interrupted() <span class="comment">//判断是否中断，并清除中断状态</span></span><br></pre></td></tr></table></figure><p>例子:通过<code>Thread.isInterrupted()</code>l来在<code>run</code>函数里写中断时做的操作，这样中断<code>Thread.interrupt()</code>调用时才有作用。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[]args)</span><span class="keyword">throws</span> InterruptedException </span>&#123;</span><br><span class="line">    Thread t1=<span class="keyword">new</span> Thread() &#123;</span><br><span class="line">        <span class="meta">@override</span></span><br><span class="line">        <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">run</span> <span class="params">()</span> </span>&#123; </span><br><span class="line">            <span class="keyword">if</span>(Thread.currentThread().isInterrupted())&#123;</span><br><span class="line">                <span class="comment">//do something</span></span><br><span class="line">            &#125;</span><br><span class="line">                Thread.yield();</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;;</span><br><span class="line">    t1.start();</span><br><span class="line">    Thread.sleep (<span class="number">2000</span>); </span><br><span class="line">    t1.interrupt ();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="wait和notify"><a href="#wait和notify" class="headerlink" title="wait和notify"></a>wait和notify</h3><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br></pre></td><td class="code"><pre><span class="line"></span><br><span class="line"><span class="keyword">package</span> ch2;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">SimpleWN</span> </span>&#123;</span><br><span class="line">    <span class="keyword">final</span> <span class="keyword">static</span> Object object = <span class="keyword">new</span> Object();</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="class"><span class="keyword">class</span> <span class="title">T1</span> <span class="keyword">extends</span> <span class="title">Thread</span> </span>&#123;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">        <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">run</span><span class="params">()</span> </span>&#123;</span><br><span class="line">            <span class="keyword">synchronized</span> (object) &#123;<span class="comment">//获取object锁</span></span><br><span class="line">                System.out.println(System.currentTimeMillis() + <span class="string">&quot;T1&quot;</span>);</span><br><span class="line">                <span class="keyword">try</span> &#123;</span><br><span class="line">                    System.out.println(System.currentTimeMillis() + <span class="string">&quot;T1 wait for object&quot;</span>);</span><br><span class="line">                    object.wait();<span class="comment">//等待并释放object锁</span></span><br><span class="line">                &#125; <span class="keyword">catch</span> (InterruptedException e) &#123;</span><br><span class="line">                    e.printStackTrace();</span><br><span class="line">                &#125;</span><br><span class="line">                System.out.println(System.currentTimeMillis() + <span class="string">&quot;T1 end&quot;</span>);</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="class"><span class="keyword">class</span> <span class="title">T2</span> <span class="keyword">extends</span> <span class="title">Thread</span> </span>&#123;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">        <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">run</span><span class="params">()</span> </span>&#123;</span><br><span class="line">            <span class="keyword">synchronized</span> (object) &#123;<span class="comment">//获取锁</span></span><br><span class="line">                System.out.println(System.currentTimeMillis() + <span class="string">&quot;T2&quot;</span>);</span><br><span class="line">                object.notify();<span class="comment">//通知T1</span></span><br><span class="line">                System.out.println(System.currentTimeMillis() + <span class="string">&quot;T2 end&quot;</span>);</span><br><span class="line">                <span class="keyword">try</span> &#123;</span><br><span class="line">                    Thread.sleep(<span class="number">2000</span>);<span class="comment">//休眠两秒，两秒后才释放锁，T1才能继续操作</span></span><br><span class="line">                &#125; <span class="keyword">catch</span> (InterruptedException e) &#123;</span><br><span class="line">                    e.printStackTrace();</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        Thread t1 = <span class="keyword">new</span> T1();</span><br><span class="line">        Thread t2 = <span class="keyword">new</span> T2();</span><br><span class="line">        t1.start();</span><br><span class="line">        t2.start();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>输出</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">T1 start!</span><br><span class="line">T1 wait for object</span><br><span class="line">T2 start! notify one thread</span><br><span class="line">T2 end!</span><br><span class="line">T1 end!</span><br></pre></td></tr></table></figure><p>wait会释放掉锁，sleep不会。notify之后T1线程要获得锁才能继续执行</p><p>wait是object的方法，sleep是Thread的方法</p><h3 id="join和yield"><a href="#join和yield" class="headerlink" title="join和yield"></a>join和yield</h3><p>线程a调用线程b的join函数，则a会等待b线程结束才继续执行</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">package</span> ch2;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">JoinMain</span> </span>&#123;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">volatile</span> <span class="keyword">int</span> i = <span class="number">0</span>;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="class"><span class="keyword">class</span> <span class="title">AddThread</span> <span class="keyword">extends</span> <span class="title">Thread</span></span>&#123;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">        <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">run</span><span class="params">()</span> </span>&#123;</span><br><span class="line">            <span class="keyword">for</span>(i=<span class="number">0</span>;i&lt;<span class="number">10000000</span>;i++)&#123;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> <span class="keyword">throws</span> InterruptedException </span>&#123;</span><br><span class="line">        AddThread at = <span class="keyword">new</span> AddThread();</span><br><span class="line">        at.start();</span><br><span class="line">        at.join();</span><br><span class="line">        System.out.println(i);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>yield声明了当前线程已经完成生命周期中最重要的部分，可以切换给其他线程来执行。让出CPU重新参与资源竞争。</p>]]></content>
    
    
      
      
    <summary type="html">&lt;h1 id=&quot;基本概念&quot;&gt;&lt;a href=&quot;#基本概念&quot; class=&quot;headerlink&quot; title=&quot;基本概念&quot;&gt;&lt;/a&gt;基本概念&lt;/h1&gt;&lt;h2 id=&quot;同步和异步&quot;&gt;&lt;a href=&quot;#同步和异步&quot; class=&quot;headerlink&quot; title=&quot;同步和异步&quot;&gt;</summary>
      
    
    
    
    
    <category term="学习" scheme="https://ifwant.tech/tags/%E5%AD%A6%E4%B9%A0/"/>
    
    <category term="JAVA" scheme="https://ifwant.tech/tags/JAVA/"/>
    
    <category term="高并发" scheme="https://ifwant.tech/tags/%E9%AB%98%E5%B9%B6%E5%8F%91/"/>
    
  </entry>
  
  <entry>
    <title>深入理解JAVA虚拟机（九）：类加载器</title>
    <link href="https://ifwant.tech/2020/12/23/jvm_9/"/>
    <id>https://ifwant.tech/2020/12/23/jvm_9/</id>
    <published>2020-12-23T12:52:39.000Z</published>
    <updated>2020-12-23T13:43:08.523Z</updated>
    
    <content type="html"><![CDATA[<h1 id="类与类加载器"><a href="#类与类加载器" class="headerlink" title="类与类加载器"></a>类与类加载器</h1><p>实现“通过一个类的全限定名来获取描述该类的二进制字节流”功能</p><h2 id="判断类是否“相等”"><a href="#判断类是否“相等”" class="headerlink" title="判断类是否“相等”"></a>判断类是否“相等”</h2><p>任意一个类，都由<strong>加载它的类加载器</strong>和这个<strong>类本身</strong>一同确立其在 Java 虚拟机中的唯一性，每一个类加载器，都有一个独立的类名称空间。即使两个类来源于同一个 Class 文件，被同一个虚拟机加载，只要加载它们的类加载器不同，那么这两个类就必定不相等。 </p><p>“相等”：包括代表类的 Class 对象的 equals() 方法、isInstance() 方法的返回结果，也包括使用 instanceof 关键字做对象所属关系判定等情况。 </p><h2 id="加载器种类"><a href="#加载器种类" class="headerlink" title="加载器种类"></a>加载器种类</h2><ul><li><p>启动类加载器（Bootstrap ClassLoader）： 负责将存放在 <code> &lt;JAVA_HOME&gt;\lib</code> 目录中的，并且能被虚拟机识别的（仅按照文件名识别，如 rt.jar，名字不符合的类库即使放在 lib 目录中也不会被加载）类库加载到虚拟机内存中。</p></li><li><p>扩展类加载器（Extension ClassLoader）： 负责加载 <code> &lt;JAVA_HOME&gt;\lib\ext</code> 目录中的所有类库，开发者可以直接使用扩展类加载器。</p></li><li><p>应用程序类加载器（Application ClassLoader）： 由于这个类加载器是 ClassLoader 中的 getSystemClassLoader() 方法的返回值，所以一般也称它为“系统类加载器”。它负责加载用户类路径（classpath）上所指定的类库，开发者可以直接使用这个类加载器，如果应用程序中没有自定义过自己的类加载器，一般情况下这个就是程序中默认的类加载器。</p></li></ul><p><img src="https://ifwant-pic-1303131123.cos.ap-nanjing.myqcloud.com/photo/classloader.png" alt="类加载器双亲委派模型"></p><h1 id="双亲委派模型"><a href="#双亲委派模型" class="headerlink" title="双亲委派模型"></a>双亲委派模型</h1><p>双亲委派模型是描述类加载器之间的层次关系。它要求除了顶层的启动类加载器外，其余的类加载器都应当有自己的父类加载器。（父子关系一般不会以继承的关系实现，而是以组合关系来复用父加载器的代码） </p><h2 id="工作过程"><a href="#工作过程" class="headerlink" title="工作过程"></a>工作过程</h2><p>如果一个类加载器收到了类加载的请求，它首先不会自己去尝试加载这个类，而是把这个请求委派给父类加载器去完成，每一个层次的类加载器都是如此，因此所有的加载请求最终都应该传送到顶层的启动类加载器中，只有当父加载器反馈自己无法完成这个加载请求（找不到所需的类）时，子加载器才会尝试自己去加载。 </p><p>在 java.lang.ClassLoader 中的 loadClass() 方法中实现该过程 </p><h2 id="为什么要双亲委派模型"><a href="#为什么要双亲委派模型" class="headerlink" title="为什么要双亲委派模型"></a>为什么要双亲委派模型</h2><p>为了保证 Java 类型体系中最基础的行为。 如果没有使用双亲委派模型，由各个类加载器自行去加载的话，如果用户自己编写了一个称为 java.lang.Object 的类，并放在 classpath 下，那么系统将会出现多个不同的 Object 类。</p><h2 id="打破双亲委派"><a href="#打破双亲委派" class="headerlink" title="打破双亲委派"></a>打破双亲委派</h2><p>针对java.*开头的类，jvm的实现中已经保证了必须由bootstrp来加载 </p><p>但是可以自定义一个类，包名不是java.*，可以放在用户目录下加载。</p>]]></content>
    
    
      
      
    <summary type="html">&lt;h1 id=&quot;类与类加载器&quot;&gt;&lt;a href=&quot;#类与类加载器&quot; class=&quot;headerlink&quot; title=&quot;类与类加载器&quot;&gt;&lt;/a&gt;类与类加载器&lt;/h1&gt;&lt;p&gt;实现“通过一个类的全限定名来获取描述该类的二进制字节流”功能&lt;/p&gt;
&lt;h2 id=&quot;判断类是否“相等”&quot;&gt;</summary>
      
    
    
    
    
    <category term="学习" scheme="https://ifwant.tech/tags/%E5%AD%A6%E4%B9%A0/"/>
    
    <category term="JAVA" scheme="https://ifwant.tech/tags/JAVA/"/>
    
    <category term="JVM" scheme="https://ifwant.tech/tags/JVM/"/>
    
  </entry>
  
  <entry>
    <title>深入理解JAVA虚拟机（八）：类加载时机和过程</title>
    <link href="https://ifwant.tech/2020/12/23/jvm_8/"/>
    <id>https://ifwant.tech/2020/12/23/jvm_8/</id>
    <published>2020-12-23T08:59:39.000Z</published>
    <updated>2020-12-23T12:51:41.248Z</updated>
    
    <content type="html"><![CDATA[<p>类加载：把类加载到内存中的过程</p><p>类的生命周期七个：加载、验证、准备、解析、初始化、使用、卸载。</p><p><img src="https://ifwant-pic-1303131123.cos.ap-nanjing.myqcloud.com/photo/loadclass.png" alt="类的生命周期"></p><p>除了解析以外，其他都要按顺序开始。</p><h1 id="加载"><a href="#加载" class="headerlink" title="加载"></a>加载</h1><ul><li><p>通过类的全限定名来获取类的二进制字节流</p></li><li><p>将字节流所代表的静态存储结构转化为方法区的运行时数据结构</p></li><li><p>创建一个表示该类的java.lang.Class对象，作为方法区这个类的各种数据的访问入口</p></li></ul><h1 id="验证"><a href="#验证" class="headerlink" title="验证"></a>验证</h1><p> 确保 Class 文件的字节流中包含的信息符合当前虚拟机的要求，并且不会危害虚拟机自身的安全 </p><ul><li><p>格式验证：字节流是否满足Class文件格式规范</p></li><li><p>元数据验证：字节码语义分析，符合java语言规范</p></li><li><p>字节码验证：对方法体验证</p></li><li><p>符号引用验证：在解析阶段发生</p></li></ul><h1 id="准备"><a href="#准备" class="headerlink" title="准备"></a>准备</h1><p>为类变量（或称“静态成员变量”）分配内存并设置初始值。这些变量（不包括实例变量）所使用的内存都在方法区中进行分配。 </p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">int</span> value = <span class="number">123</span>;</span><br></pre></td></tr></table></figure><p>初始化为0</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="keyword">int</span> value = <span class="number">123</span>;</span><br></pre></td></tr></table></figure><p>初始化为123</p><h1 id="解析"><a href="#解析" class="headerlink" title="解析"></a>解析</h1><p> 将常量池内的符号引用替换为直接引用</p><h1 id="初始化"><a href="#初始化" class="headerlink" title="初始化"></a>初始化</h1><p>执行类构造器 <code>&lt;clinit&gt;()</code> 方法 ， <code>&lt;clinit&gt;()</code>  方法是由编译器自动收集类中的所有<strong>类变量的赋值</strong>动作和<strong>静态语句块</strong>（<code>static &#123;&#125;</code> 块）中的语句合并产生的，先执行父类的。</p>]]></content>
    
    
      
      
    <summary type="html">&lt;p&gt;类加载：把类加载到内存中的过程&lt;/p&gt;
&lt;p&gt;类的生命周期七个：加载、验证、准备、解析、初始化、使用、卸载。&lt;/p&gt;
&lt;p&gt;&lt;img src=&quot;https://ifwant-pic-1303131123.cos.ap-nanjing.myqcloud.com/photo/l</summary>
      
    
    
    
    
    <category term="学习" scheme="https://ifwant.tech/tags/%E5%AD%A6%E4%B9%A0/"/>
    
    <category term="JAVA" scheme="https://ifwant.tech/tags/JAVA/"/>
    
    <category term="JVM" scheme="https://ifwant.tech/tags/JVM/"/>
    
  </entry>
  
  <entry>
    <title>深入理解JAVA虚拟机（七）：类文件结构</title>
    <link href="https://ifwant.tech/2020/12/23/jvm_7/"/>
    <id>https://ifwant.tech/2020/12/23/jvm_7/</id>
    <published>2020-12-23T08:08:39.000Z</published>
    <updated>2020-12-23T09:23:48.522Z</updated>
    
    <content type="html"><![CDATA[<p>Class 文件是二进制文件 ，有两种数据类型：</p><ul><li>无符号数： u1、u2、u4、u8 分别代表 1/2/4/8 字节的无符号数 </li><li>表： 多个无符号数或者其他表作为数据项构成的复合数据类型 </li></ul><p>在vim下输入<code>:%!xxd</code>可查看十六进制的Class文件内容。</p><p><img src="https://ifwant-pic-1303131123.cos.ap-nanjing.myqcloud.com/photo/class.png" alt="十六进制下class文件内容"></p><h1 id="魔数"><a href="#魔数" class="headerlink" title="魔数"></a>魔数</h1><p> Class 文件的头 4 个字节， 用来表示这个 Class 文件的类型， 相当于文件后缀名。(cafe babe)</p><h1 id="版本信息"><a href="#版本信息" class="headerlink" title="版本信息"></a>版本信息</h1><p>接下来4个字节是版本信息，java只向下兼容。<br>34对应jdk8</p><h1 id="常量池"><a href="#常量池" class="headerlink" title="常量池"></a>常量池</h1><p>常量池存放两种类型常量：</p><ul><li>字面量：java语言中定义的常量，如字符串、final修饰的值</li><li>符号引用：定义的各种名字，如类和接口的全限定名、字段的名称和描述符、方法的名字和描述符</li></ul><p>特点：</p><ul><li>常量池数量不固定，开头放置一个u2类型的无符号数，记录常量池容量</li><li>每个常量都是一个表。第一位是u1标志位，标志常量类型</li></ul><h1 id="访问标志"><a href="#访问标志" class="headerlink" title="访问标志"></a>访问标志</h1><p>常量池后的2个字节，用于识别一些类或接口的访问信息，如这个Class是类还是接口、是否是public等</p><h1 id="类索引、父类索引与接口索引集合"><a href="#类索引、父类索引与接口索引集合" class="headerlink" title="类索引、父类索引与接口索引集合"></a>类索引、父类索引与接口索引集合</h1><ul><li>类索引：u2数据，表示类名</li><li>父类索引：u2数据，表示父类名</li><li>接口索引集合：一组 u2 数据 ，表示实现的接口名</li></ul><h1 id="字段表集合"><a href="#字段表集合" class="headerlink" title="字段表集合"></a>字段表集合</h1><p>存储本类涉及到的成员变量，包括实例变量和类变量，但不包括方法中的局部变量<br>一个成员变量一个字段表<br>不会出现从父类（或接口）中继承而来的字段 </p><h1 id="方法表集合"><a href="#方法表集合" class="headerlink" title="方法表集合"></a>方法表集合</h1><p>描述方法的一些信息<br>方法表的属性表集合中有一张 Code 属性表，用于存储当前方法经编译器编译后的字节码指令。 </p><h1 id="属性表集合"><a href="#属性表集合" class="headerlink" title="属性表集合"></a>属性表集合</h1><p> 每个属性对应一张属性表 </p>]]></content>
    
    
      
      
    <summary type="html">&lt;p&gt;Class 文件是二进制文件 ，有两种数据类型：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;无符号数： u1、u2、u4、u8 分别代表 1/2/4/8 字节的无符号数 &lt;/li&gt;
&lt;li&gt;表： 多个无符号数或者其他表作为数据项构成的复合数据类型 &lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;在vim下输</summary>
      
    
    
    
    
    <category term="学习" scheme="https://ifwant.tech/tags/%E5%AD%A6%E4%B9%A0/"/>
    
    <category term="JAVA" scheme="https://ifwant.tech/tags/JAVA/"/>
    
    <category term="JVM" scheme="https://ifwant.tech/tags/JVM/"/>
    
  </entry>
  
  <entry>
    <title>深入理解JAVA虚拟机（六）：基础故障处理工具</title>
    <link href="https://ifwant.tech/2020/12/22/jvm_6/"/>
    <id>https://ifwant.tech/2020/12/22/jvm_6/</id>
    <published>2020-12-22T08:50:39.000Z</published>
    <updated>2020-12-22T15:40:42.257Z</updated>
    
    <content type="html"><![CDATA[<h1 id="jps"><a href="#jps" class="headerlink" title="jps"></a>jps</h1><p>查看正在运行的虚拟机进程，显示虚拟机执行主类（Main Class，main()函数所在的类）名称以及这些进程的本地虚拟机唯一 ID（LVMID，Local Virtual Machine Identifier）</p><p>参数：</p><ul><li><code>-l</code> 输出主类全名</li></ul><h1 id="jstat"><a href="#jstat" class="headerlink" title="jstat"></a>jstat</h1><p>监视虚拟机各种运行状态信息，主要三类信息：类加载、垃圾收集、运行期编译状况</p><p><code>jstat -option VMID 间隔时间（ms） 次数</code></p><p>参数：</p><ul><li><p><code>-gc</code> 监视java堆状况，Eden区、Survivor区（S0,S1）、老年代等的容量，已用空间，垃圾收集时间等信息</p></li><li><p><code>-class</code> 监视类加载相关信息</p></li><li><p><code>-compiler</code> 编译统计</p></li></ul><h1 id="jinfo"><a href="#jinfo" class="headerlink" title="jinfo"></a>jinfo</h1><p>实时查看和调整虚拟机各项参数</p><h1 id="jmap"><a href="#jmap" class="headerlink" title="jmap"></a>jmap</h1><p>生成堆转储快照、查询finalize执行队列、Java堆和方法区的详细信息，如空间使用率、当前用的是哪种收集器等</p><p>先运行这句</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">echo 0 | sudo tee /proc/sys/kernel/yama/ptrace_scope</span><br></pre></td></tr></table></figure><p>新版的Linux系统加入了 ptrace-scope 机制. 这种机制为了防止用户访问当前正在运行的进程的内存和状态, 而一些调试软件本身就是利用 ptrace 来进行获取某进程的内存状态的(包括GDB),所以在新版本的Linux系统, 默认情况下不允许 </p><p><code>jmap [ option ] vmid</code></p><p>参数：</p><ul><li><code>-heap</code> 查看堆的详细信息</li><li><code>-dump</code> 生成java堆转储快照，格式：<code>jmap -dump:[live,]format=b,file=&lt;filename&gt; vmid</code></li><li><code>-finalizerinfo </code> 查看F-Queue中等待finalizer方法执行的对象</li><li><code>-histo</code> 显示堆中对象统计信息，包括类、实例数量、合计容量</li></ul><h1 id="jstack"><a href="#jstack" class="headerlink" title="jstack"></a>jstack</h1><p>生成虚拟机当前时刻的线程快照，通常用于定位线程出现长时间停顿的原因，如线程间死锁、死循环、请求外部资源导致的长时间挂起等。</p><p><code>jstack [ option ] vmid</code></p><p>参数：</p><ul><li><code>-l</code> 显示关于锁的附加信息</li></ul>]]></content>
    
    
      
      
    <summary type="html">&lt;h1 id=&quot;jps&quot;&gt;&lt;a href=&quot;#jps&quot; class=&quot;headerlink&quot; title=&quot;jps&quot;&gt;&lt;/a&gt;jps&lt;/h1&gt;&lt;p&gt;查看正在运行的虚拟机进程，显示虚拟机执行主类（Main Class，main()函数所在的类）名称以及这些进程的本地虚拟机唯一 ID</summary>
      
    
    
    
    
    <category term="学习" scheme="https://ifwant.tech/tags/%E5%AD%A6%E4%B9%A0/"/>
    
    <category term="JAVA" scheme="https://ifwant.tech/tags/JAVA/"/>
    
    <category term="JVM" scheme="https://ifwant.tech/tags/JVM/"/>
    
  </entry>
  
  <entry>
    <title>深入理解JAVA虚拟机（五）：内存分配与回收策略</title>
    <link href="https://ifwant.tech/2020/12/22/jvm_5/"/>
    <id>https://ifwant.tech/2020/12/22/jvm_5/</id>
    <published>2020-12-22T06:58:39.000Z</published>
    <updated>2020-12-22T08:42:32.193Z</updated>
    
    <content type="html"><![CDATA[<p>对象内存主要分配在新生代的Eden区，少数情况直接分配在老年代，具体规则取决于垃圾收集器。</p><h1 id="对象优先在-Eden-分配"><a href="#对象优先在-Eden-分配" class="headerlink" title="对象优先在 Eden 分配"></a>对象优先在 Eden 分配</h1><p>大多数情况下，对象在新生代Eden区中分配。当Eden区没有足够空间进行分配时，虚拟机将发起一次Minor GC。</p><ul><li>Minor GC：回收新生代</li><li>Major GC：回收老年代</li><li>Full GC：回收整个堆</li></ul><h1 id="大对象直接进入老年代"><a href="#大对象直接进入老年代" class="headerlink" title="大对象直接进入老年代"></a>大对象直接进入老年代</h1><p> 大对象是指需要大量连续内存空间的 Java 对象，如很长的字符串或元素数量庞大的数组。 </p><p> 一个大对象能够存入 Eden 区的概率比较小，发生分配担保的概率比较大，而分配担保需要涉及大量的复制，就会造成效率低下。 </p><p> 虚拟机提供了一个 -XX:PretenureSizeThreshold 参数，令大于这个设置值的对象直接在老年代分配，这样做的目的是避免在 Eden 区及两个 Survivor 区之间发生大量的内存复制。（新生代采用复制算法回收垃圾） </p><h1 id="长期存活的对象将进入老年代"><a href="#长期存活的对象将进入老年代" class="headerlink" title="长期存活的对象将进入老年代"></a>长期存活的对象将进入老年代</h1><p> JVM 给每个对象定义了一个<strong>对象年龄计数器</strong>。对象通常在Eden区里诞生，如果经过第一次Minor GC后仍然存活，并且能被Survivor容纳的话，该对象会被移动到Survivor空间中，并且将其对象年龄设为1岁。对象在Survivor区中每熬过一次Minor GC，年龄就增加1岁，当它的年龄增加到一定程度（默认为15），就会被晋升到老年代中。 </p><h1 id="动态对象年龄判定"><a href="#动态对象年龄判定" class="headerlink" title="动态对象年龄判定"></a>动态对象年龄判定</h1><p>如果在Survivor空间中<strong>相同年龄所有对象</strong>的大小总和&gt;Survivor空间的一半，年龄&gt;=该年龄的对象就可以直接进入老年代。</p><h1 id="空间分配担保"><a href="#空间分配担保" class="headerlink" title="空间分配担保"></a>空间分配担保</h1><p>为防止Survivor空间不够Minor GC时的复制操作，老年代预留空间做担保。</p><ul><li>JDK6之前：<ul><li>检查老年代最大可用连续空间是否大于新生代所有对象总空间：大于则Minor GC,否则下一步</li><li>检查 HandlePromotionFailure 值是否允许担保失败<ul><li>允许： 检查老年代最大可用的连续空间是否大于历次晋升到老年代对象的平均大小 ：大于则Minor GC（有风险），否则Full GC</li><li>不允许：Full GC</li></ul></li></ul></li><li>JDK6之后：<ul><li>抛弃HandlePromotionFailure 值， 只要老年代的连续空间大于新生代对象总大小或者历次晋升的平均大小，就会进行 Minor GC，否则将进行 Full GC。 </li></ul></li></ul><h1 id="JVM触发Full-GC的情况"><a href="#JVM触发Full-GC的情况" class="headerlink" title="JVM触发Full GC的情况"></a>JVM触发Full GC的情况</h1><ul><li>System.gc() 方法的调用 ：建议但不一定会 Full GC </li><li>老年代空间不足</li><li>永久代空间不足</li><li>CMS GC时有新的对象放入老年代，老年代空间不足</li><li>统计得到的 Minor GC 晋升到旧生代的平均大小大于老年代的剩余空间 </li></ul>]]></content>
    
    
      
      
    <summary type="html">&lt;p&gt;对象内存主要分配在新生代的Eden区，少数情况直接分配在老年代，具体规则取决于垃圾收集器。&lt;/p&gt;
&lt;h1 id=&quot;对象优先在-Eden-分配&quot;&gt;&lt;a href=&quot;#对象优先在-Eden-分配&quot; class=&quot;headerlink&quot; title=&quot;对象优先在 Eden 分配</summary>
      
    
    
    
    
    <category term="学习" scheme="https://ifwant.tech/tags/%E5%AD%A6%E4%B9%A0/"/>
    
    <category term="JAVA" scheme="https://ifwant.tech/tags/JAVA/"/>
    
    <category term="JVM" scheme="https://ifwant.tech/tags/JVM/"/>
    
  </entry>
  
  <entry>
    <title>深入理解JAVA虚拟机（四）：HotSpot 垃圾收集器</title>
    <link href="https://ifwant.tech/2020/12/21/jvm_4/"/>
    <id>https://ifwant.tech/2020/12/21/jvm_4/</id>
    <published>2020-12-21T14:15:39.000Z</published>
    <updated>2020-12-22T06:57:31.294Z</updated>
    
    <content type="html"><![CDATA[<p>七种，前三种新生代，后三种老生代，G1通用：</p><ul><li>Serial（复制）</li><li>PalNew（复制）</li><li>Parallel Scavenge（复制）</li><li><strong>G1</strong>（整理、复制）</li><li><strong>CMS</strong>（清除）</li><li>Serial Old（整理）</li><li>Parallel Old（整理）</li></ul><p><img src="https://ifwant-pic-1303131123.cos.ap-nanjing.myqcloud.com/photo/gc.png" alt="垃圾收集器"></p><h1 id="Serial"><a href="#Serial" class="headerlink" title="Serial"></a>Serial</h1><p>只开启一条 GC 线程进行垃圾回收，并且在垃圾收集过程中停止一切用户线程 </p><p>适合客户端（内存小，创建对象少，回收时间短）</p><h1 id="ParNew"><a href="#ParNew" class="headerlink" title="ParNew"></a>ParNew</h1><p> Serial 的多线程版本,只能和CMS配合使用</p><p>追求降低停顿时间，适合交互式应用</p><h1 id="Parallel-Scavenge"><a href="#Parallel-Scavenge" class="headerlink" title="Parallel Scavenge"></a>Parallel Scavenge</h1><p>多线程</p><p>追求提升吞吐量，适合没有交互的后台</p><p>吞吐量=用户代码时间/（用户代码时间+GC时间）</p><h1 id="G1"><a href="#G1" class="headerlink" title="G1"></a>G1</h1><p>将堆划分为一块块独立的 Region。当要进行垃圾收集时，首先估计每个 Region 中垃圾的数量，每次都从垃圾回收价值最大的 Region 开始回收，因此可以获得最大的回收效率。 </p><ul><li>初始标记：Stop The World<ul><li>仅使用一条初始标记线程对所有与 GC Roots 直接关联的对象进行标记</li><li>修改TAMS 指针的值，让下一阶段用户线程并发运行时新分配的对象，都在指针地址之上（即默认为存活不回收）</li></ul></li><li>并发标记：并发执行，进行可达性分析，速度很慢。</li><li>最终标记：Stop The World，处理并发标记阶段更新的对象引用关系。</li><li>筛选回收：Stop The World，回收无效对象，选择价值最高的Region，复制移动Region。</li></ul><p>对于Region之间的对象引用问题，使用记忆集来解决。每个Region都维护有自己的记忆集，这些记忆集会记录下别的Region 指向自己的指针</p><p>缺点：</p><ul><li>内存占用更多（如每个Region都有记忆集）</li><li>额外负载</li></ul><h1 id="CMS"><a href="#CMS" class="headerlink" title="CMS"></a>CMS</h1><p>使用标记清除，追求最短停顿时间，用户线程和GC线程并发。</p><ul><li>初始标记：Stop The World，仅使用<strong>一条</strong>初始标记线程对所有与 GC Roots <strong>直接关联</strong>的对象进行标记，速度快。</li><li>并发标记：使用<strong>多条</strong>标记线程，与用户线程并发执行。此过程进行可达性分析，遍历标记出所有废弃对象。速度很慢。</li><li>重新标记：Stop The World，修正上一步，并发标记过程中，本来不可达的对象由于新的引用变得可达了，这种对象标记出来不回收，速度较快。</li><li>并发清除：只使用一条 GC 线程，与用户线程并发执行，清除刚才标记的对象。这个过程非常耗时。</li></ul><p><img src="https://ifwant-pic-1303131123.cos.ap-nanjing.myqcloud.com/photo/CMS.png" alt="CMS"></p><p>缺点：</p><ul><li>吞吐量低（并发占用线程，应用程序变慢）</li><li>无法处理浮动垃圾（并发标记过程中新出现的无效对象，包括本来可达变不可达）</li><li>标记-清除产生内存碎片化问题</li></ul><h1 id="Serial-Old"><a href="#Serial-Old" class="headerlink" title="Serial Old"></a>Serial Old</h1><p> Serial 的老年代版本 ,使用标记-整理算法</p><h1 id="Parallel-Old"><a href="#Parallel-Old" class="headerlink" title="Parallel Old"></a>Parallel Old</h1><p> Parallel Scavenge 的老年代版本 ，追求吞吐量</p><h1 id="其他"><a href="#其他" class="headerlink" title="其他"></a>其他</h1><p>并发与并行的区别：</p><ul><li>并发是两个队列<strong>交替</strong>使用一台咖啡机</li><li>并行是两个队列<strong>同时</strong>使用两台咖啡机 </li></ul><p><img src="https://ifwant-pic-1303131123.cos.ap-nanjing.myqcloud.com/photo/bingfa.jpg" alt="并发与并行"></p>]]></content>
    
    
      
      
    <summary type="html">&lt;p&gt;七种，前三种新生代，后三种老生代，G1通用：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;Serial（复制）&lt;/li&gt;
&lt;li&gt;PalNew（复制）&lt;/li&gt;
&lt;li&gt;Parallel Scavenge（复制）&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;G1&lt;/strong&gt;（整理、复制）&lt;/li</summary>
      
    
    
    
    
    <category term="学习" scheme="https://ifwant.tech/tags/%E5%AD%A6%E4%B9%A0/"/>
    
    <category term="JAVA" scheme="https://ifwant.tech/tags/JAVA/"/>
    
    <category term="JVM" scheme="https://ifwant.tech/tags/JVM/"/>
    
  </entry>
  
  <entry>
    <title>深入理解JAVA虚拟机（三）：垃圾收集策略与算法</title>
    <link href="https://ifwant.tech/2020/12/21/jvm_3/"/>
    <id>https://ifwant.tech/2020/12/21/jvm_3/</id>
    <published>2020-12-21T11:51:39.000Z</published>
    <updated>2020-12-21T14:13:11.185Z</updated>
    
    <content type="html"><![CDATA[<p> 程序计数器、虚拟机栈、本地方法栈：线程私有，内存分配和回收有确定性，无需过多考虑垃圾回收；</p><p>堆、方法区：动态分配内存和回收，垃圾收集器重点关注这块。</p><h1 id="判断对象是否存活"><a href="#判断对象是否存活" class="headerlink" title="判断对象是否存活"></a>判断对象是否存活</h1><p>一个对象不被任何对象或变量引用，则为无效对象，需要被回收。</p><p>JAVA主要用的可达性分析算法</p><h2 id="引用计数法"><a href="#引用计数法" class="headerlink" title="引用计数法"></a>引用计数法</h2><p> 在对象头维护着一个 counter 计数器，对象被引用一次则计数器 +1；若引用失效则计数器 -1。当计数器为 0 时，就认为该对象无效了。 </p><p>简单，但很难解决对象间相互循环引用问题。</p><h2 id="可达性分析"><a href="#可达性分析" class="headerlink" title="可达性分析"></a>可达性分析</h2><p>所有和 GC Roots 直接或间接关联的对象都是有效对象，和 GC Roots 没有关联的对象（图论中的不可到达）就是无效对象。</p><p>GC Roots 是指：</p><ul><li>Java 虚拟机栈（栈帧中的本地变量表）中引用的对象</li><li>本地方法栈中引用的对象</li><li>方法区中常量引用的对象</li><li>方法区中类静态属性引用的对象</li></ul><p>GC Roots 并不包括堆中对象所引用的对象，这样就不会有循环引用的问题。</p><h2 id="finalize"><a href="#finalize" class="headerlink" title="finalize()"></a>finalize()</h2><p>可达性分析认为不可达后：</p><ul><li>没有finalize或者已经调用过一次，对象被回收</li><li>执行finalize，对象放入一个队列中，虚拟机以较低的优先级执行这些 finalize()方法。对象在队列中被执行前如果被附加引用，则重生；否则被回收。</li></ul><h1 id="引用的种类"><a href="#引用的种类" class="headerlink" title="引用的种类"></a>引用的种类</h1><p>强软弱虚</p><h2 id="强引用"><a href="#强引用" class="headerlink" title="强引用"></a>强引用</h2><p>引用赋值，类似“ Object obj = new Object()” </p><p>有强引用，GC就不会回收被引用对象</p><p>如果错误地保持了强引用，比如：赋值给了 static 变量，那么对象在很长一段时间内不会被回收，会产生内存泄漏</p><h2 id="软引用"><a href="#软引用" class="headerlink" title="软引用"></a>软引用</h2><p>当JVM认为内存不足时，才会回收软引用的对象。</p><p>软引用对象都被清理了，内存仍不足，才会抛出OOM。</p><p>常用来实现内存敏感的缓存，有内存空闲就不清理，内存不足才清理。</p><h2 id="弱引用"><a href="#弱引用" class="headerlink" title="弱引用"></a>弱引用</h2><p>生存周期到下一次垃圾回收之前。当 JVM 进行垃圾回收时，无论内存是否充足，都会回收只被弱引用关联的对象。</p><h2 id="虚引用"><a href="#虚引用" class="headerlink" title="虚引用"></a>虚引用</h2><p>完全不影响对象生存时间，仅仅是用来在对象被回收时，通知你它被回收了。</p><h1 id="垃圾收集算法"><a href="#垃圾收集算法" class="headerlink" title="垃圾收集算法"></a>垃圾收集算法</h1><p>清除、复制（新生代）、整理（老年代）</p><h2 id="标记-清除算法"><a href="#标记-清除算法" class="headerlink" title="标记-清除算法"></a>标记-清除算法</h2><p>过程：</p><ul><li>标记：GC Roots遍历，标记存活（可达）对象</li><li>清除：遍历堆，清除未被标记的对象</li><li>或者反过来，标记非存活对象</li></ul><p>缺点：</p><ul><li>效率问题：对象越多越耗时</li><li>空间问题：内存碎片化，清除后产生大量不连续内存碎片</li></ul><h2 id="标记-复制算法"><a href="#标记-复制算法" class="headerlink" title="标记-复制算法"></a>标记-复制算法</h2><p>内存等分为两块，每次使用一块，GC时将存活者复制到另一块内存上，清空原来内存。</p><ul><li>优点：没有内存碎片化问题</li><li>缺点：浪费空间</li></ul><p>优化：</p><ul><li>内存分为三块： Eden、From Survivor、To Survivor，比例是 8:1:1，每次使用 Eden 和其中一块 Survivor。回收时，将 Eden 和 Survivor 中还存活的对象一次性复制到另外一块 Survivor 空间上，最后清理掉 Eden 和刚才使用的 Survivor 空间。 </li><li>分配担保：Survivor没有足够空间存放上一次新生代收集下来的存活对象，这些对象直接进入老年代</li></ul><h2 id="标记-整理算法"><a href="#标记-整理算法" class="headerlink" title="标记-整理算法"></a>标记-整理算法</h2><p>过程：</p><ul><li>标记：遍历GC Roots，标记存活</li><li>整理：移动存活对象到一起，清除末端内存地址以后的内存</li></ul><p>缺点：需要暂停应用程序，大量对象移动导致长时间停顿</p><p>对于老年代，关注延迟（内存回收）用清除算法，关注吞吐量（内存分配）用整理算法</p>]]></content>
    
    
      
      
    <summary type="html">&lt;p&gt; 程序计数器、虚拟机栈、本地方法栈：线程私有，内存分配和回收有确定性，无需过多考虑垃圾回收；&lt;/p&gt;
&lt;p&gt;堆、方法区：动态分配内存和回收，垃圾收集器重点关注这块。&lt;/p&gt;
&lt;h1 id=&quot;判断对象是否存活&quot;&gt;&lt;a href=&quot;#判断对象是否存活&quot; class=&quot;heade</summary>
      
    
    
    
    
    <category term="学习" scheme="https://ifwant.tech/tags/%E5%AD%A6%E4%B9%A0/"/>
    
    <category term="JAVA" scheme="https://ifwant.tech/tags/JAVA/"/>
    
    <category term="JVM" scheme="https://ifwant.tech/tags/JVM/"/>
    
  </entry>
  
  <entry>
    <title>深入理解JAVA虚拟机（二）：HotSpot虚拟机对象探秘</title>
    <link href="https://ifwant.tech/2020/12/21/jvm_2/"/>
    <id>https://ifwant.tech/2020/12/21/jvm_2/</id>
    <published>2020-12-21T09:09:39.000Z</published>
    <updated>2020-12-22T04:25:58.506Z</updated>
    
    <content type="html"><![CDATA[<h1 id="对象的内存布局"><a href="#对象的内存布局" class="headerlink" title="对象的内存布局"></a>对象的内存布局</h1><p> HotSpot 虚拟机中分为三块区域：</p><ul><li>对象头（Header）</li><li>实例数据（Instance Data）</li><li>对齐填充（Padding）</li></ul><p><img src="https://ifwant-pic-1303131123.cos.ap-nanjing.myqcloud.com/photo/object-memory-layout.png" alt="内存布局"></p><h2 id="对象头"><a href="#对象头" class="headerlink" title="对象头"></a>对象头</h2><p>记录了对象在运行过程中所需要使用的一些数据：</p><ul><li>哈希码</li><li>GC 分代年龄</li><li>锁状态标志</li><li>线程持有的锁</li><li>偏向线程 ID</li><li>偏向时间戳</li></ul><p>对象头可能包含类型指针，通过该指针能确定对象属于哪个类。</p><p>如果对象是一个数组，那么对象头还会包括数组长度。</p><h2 id="实例数据"><a href="#实例数据" class="headerlink" title="实例数据"></a>实例数据</h2><p> 成员变量的值，其中包括父类成员变量和本类成员变量 </p><h2 id="对齐填充"><a href="#对齐填充" class="headerlink" title="对齐填充"></a>对齐填充</h2><p> 确保对象的总长度为 8 字节的整数倍 </p><p> 对齐填充并不是必然存在，也没有特别的含义，它仅仅起着占位符的作用。 </p><h1 id="对象创建过程"><a href="#对象创建过程" class="headerlink" title="对象创建过程"></a>对象创建过程</h1><h2 id="类加载检查"><a href="#类加载检查" class="headerlink" title="类加载检查"></a>类加载检查</h2><p>jvm遇到new指令-&gt; 检查常量池中是否有这个类的符号引用 、 符号引用所代表的类是否已被加载、解析和初始化过 </p><h2 id="为新生对象分配内存"><a href="#为新生对象分配内存" class="headerlink" title="为新生对象分配内存"></a>为新生对象分配内存</h2><p> 对象所需内存的大小在类加载完成后便可完全确定 ， 从堆中划分一块对应大小的内存空间给新的对象 。</p><ul><li><p><strong>指针碰撞</strong><br>堆中内存规整，移动指针</p><p>说明采用的是“<strong>标记-复制算法</strong>”或“<strong>标记-整理算法</strong>”，空闲内存和已使用内存中间放着一个指针作为分界点指示器，那么分配内存时只需要把指针向空闲内存挪动一段与对象大小一样的距离。</p></li><li><p><strong>空闲列表</strong><br> 堆中内存不规整，从空闲列表中找内存分配</p><p>已使用的内存和空闲内存交错（说明采用的是<strong>标记-清除算法</strong>，有碎片）， VM 维护一个列表，记录其中哪些内存块空闲可用。分配之时从空闲列表中找到一块足够大的内存空间划分给对象实例。</p></li></ul><p>对象创建时的线程安全问题：</p><ul><li>CAS配上失败重试</li><li>本地线程分配缓冲：每个线程预留一块内存</li></ul><h2 id="初始化"><a href="#初始化" class="headerlink" title="初始化"></a>初始化</h2><p> 为对象中的成员变量赋上初始值，设置对象头信息，调用对象的构造函数方法进行初始化。 </p><h1 id="对象的访问方式"><a href="#对象的访问方式" class="headerlink" title="对象的访问方式"></a>对象的访问方式</h1><p>堆中放对象，栈里放对象引用，引用访问对象的方式有两种：</p><h2 id="句柄访问方式"><a href="#句柄访问方式" class="headerlink" title="句柄访问方式"></a>句柄访问方式</h2><p>对象引用-&gt;句柄池-&gt;实例数据和类型数据</p><p>更安全，对象移动，引用不变</p><p><img src="https://ifwant-pic-1303131123.cos.ap-nanjing.myqcloud.com/photo/handle-access.jpg" alt="句柄方式"></p><h2 id="直接指针访问方式"><a href="#直接指针访问方式" class="headerlink" title="直接指针访问方式"></a>直接指针访问方式</h2><p>对象引用-&gt;实例数据（含类型数据指针）-&gt;类型数据</p><p>更快</p><p><img src="https://ifwant-pic-1303131123.cos.ap-nanjing.myqcloud.com/photo/direct-pointer.jpg" alt="直接指针访问"></p>]]></content>
    
    
      
      
    <summary type="html">&lt;h1 id=&quot;对象的内存布局&quot;&gt;&lt;a href=&quot;#对象的内存布局&quot; class=&quot;headerlink&quot; title=&quot;对象的内存布局&quot;&gt;&lt;/a&gt;对象的内存布局&lt;/h1&gt;&lt;p&gt; HotSpot 虚拟机中分为三块区域：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;对象头（Header）&lt;/li&gt;</summary>
      
    
    
    
    
    <category term="学习" scheme="https://ifwant.tech/tags/%E5%AD%A6%E4%B9%A0/"/>
    
    <category term="JAVA" scheme="https://ifwant.tech/tags/JAVA/"/>
    
    <category term="JVM" scheme="https://ifwant.tech/tags/JVM/"/>
    
  </entry>
  
  <entry>
    <title>深入理解JAVA虚拟机（一）：JVM内存结构</title>
    <link href="https://ifwant.tech/2020/12/19/jvm_1/"/>
    <id>https://ifwant.tech/2020/12/19/jvm_1/</id>
    <published>2020-12-19T07:09:39.000Z</published>
    <updated>2020-12-19T14:48:03.095Z</updated>
    
    <content type="html"><![CDATA[<h1 id="JVM内存结构"><a href="#JVM内存结构" class="headerlink" title="JVM内存结构"></a>JVM内存结构</h1><p>JVM内存空间：</p><ul><li>程序计数器</li><li>Java 虚拟机栈</li><li>本地方法栈</li><li>堆</li><li>方法区</li></ul><p><img src="https://ifwant-pic-1303131123.cos.ap-nanjing.myqcloud.com/photo/jvm-memory-structure.jpg" alt="JVM内存结构"></p><p> JDK 1.8 同 JDK 1.7 比，最大的差别就是：元数据区（Metaspace）取代了永久代。元空间的本质和永久代类似，都是对 JVM 规范中方法区的实现。不过元空间与永久代之间最大的区别在于：元数据空间并不在虚拟机中，而是使用本地内存。 </p><h1 id="程序计数器"><a href="#程序计数器" class="headerlink" title="程序计数器"></a>程序计数器</h1><p>记录当前线程正在执行的那条字节码指令的地址 </p><h2 id="作用"><a href="#作用" class="headerlink" title="作用"></a>作用</h2><ul><li>字节码解释器通过改变程序计数器来依次读取指令，从而实现代码的流程控制。</li><li>在多线程情况下，程序计数器记录的是当前线程执行的位置，从而当线程切换回来时，就知道上次线程执行到哪了。</li></ul><h2 id="特点"><a href="#特点" class="headerlink" title="特点"></a>特点</h2><ul><li>是一块较小的内存空间。</li><li>线程私有，每条线程都有自己的程序计数器。</li><li>生命周期：随着线程的创建而创建，随着线程的结束而销毁。</li><li><strong>是唯一一个不会出现<code>OutOfMemoryError</code>的内存区域。</strong></li></ul><h1 id="Java-虚拟机栈"><a href="#Java-虚拟机栈" class="headerlink" title="Java 虚拟机栈"></a>Java 虚拟机栈</h1><p> 描述 Java 方法运行过程的内存模型 </p><p>Jvm栈为每个运行的java方法创建栈帧，存放信息：</p><ul><li>局部变量表</li><li>操作数栈</li><li>动态链接</li><li>方法出口信息</li><li>……</li></ul><p><img src="https://ifwant-pic-1303131123.cos.ap-nanjing.myqcloud.com/photo/jvm-stack.jpg" alt="stack"></p><h2 id="压栈出栈过程"><a href="#压栈出栈过程" class="headerlink" title="压栈出栈过程"></a>压栈出栈过程</h2><p>当方法运行过程中需要创建局部变量时，就将局部变量的值存入栈帧中的局部变量表中。</p><p>栈顶的栈帧是当前正在执行的活动栈，也就是当前正在执行的方法，PC 寄存器也会指向这个地址。只有这个活动的栈帧的本地变量可以被操作数栈使用，当在这个栈帧中调用另一个方法，与之对应的栈帧又会被创建，新创建的栈帧压入栈顶，变为当前的活动栈帧。</p><p>方法结束后，当前栈帧被移出，栈帧的返回值变成新的活动栈帧中操作数栈的一个操作数。如果没有返回值，那么新的活动栈帧中操作数栈的操作数没有变化。</p><h2 id="特点-1"><a href="#特点-1" class="headerlink" title="特点"></a>特点</h2><ul><li>局部变量表大小编译时确定，不会改变</li><li>栈会出现两种异常：StackOverFlowError 和 OutOfMemoryError。<ul><li>StackOverFlowError 若 Java 虚拟机栈的大小不允许动态扩展，那么当线程请求栈的深度超过当前 Java 虚拟机栈的最大深度时，抛出 StackOverFlowError 异常。（递归容易导致stackoverflow问题）</li><li>OutOfMemoryError 若允许动态扩展，那么当线程请求栈时内存用完了，无法再动态扩展时，抛出 OutOfMemoryError 异常。</li></ul></li><li>线程私有</li></ul><h1 id="本地方法栈"><a href="#本地方法栈" class="headerlink" title="本地方法栈"></a>本地方法栈</h1><p> 描述本地方法运行过程的内存模型 ，几乎所有的对象都存储在堆中。 </p><h1 id="堆"><a href="#堆" class="headerlink" title="堆"></a>堆</h1><p>存放对象实例和数组</p><h2 id="特点-2"><a href="#特点-2" class="headerlink" title="特点"></a>特点</h2><ul><li>线程共享，需要注意同步问题 </li><li>垃圾回收的主要场所</li></ul><h1 id="方法区"><a href="#方法区" class="headerlink" title="方法区"></a>方法区</h1><p>堆的一个逻辑部分，存放：</p><ul><li>已经被虚拟机加载的类信息</li><li>常量</li><li>静态变量</li><li>即时编译器编译后的代码</li></ul><h2 id="特点-3"><a href="#特点-3" class="headerlink" title="特点"></a>特点</h2><ul><li>线程共享</li><li>JDK8使用元空间（Metaspace）代替永久代来实现方法区</li></ul><h2 id="运行时常量池"><a href="#运行时常量池" class="headerlink" title="运行时常量池"></a>运行时常量池</h2><p>方法区的一部分， 常量就存放在运行时常量池中 </p><p>当类被 Java 虚拟机加载后， .class 文件中的常量就存放在方法区的运行时常量池中。而且在运行期间，可以向常量池中添加新的常量。如 String 类的 intern() 方法就能在运行期间向常量池中添加字符串常量。 </p><h1 id="直接内存"><a href="#直接内存" class="headerlink" title="直接内存"></a>直接内存</h1><p> 除 Java 虚拟机之外的内存 </p><p> 在 NIO 中引入了一种基于通道和缓冲的 IO 方式。它可以通过调用本地方法直接分配 Java 虚拟机之外的内存，然后通过一个存储在堆中的<code>DirectByteBuffer</code>对象直接操作该内存，而无须先将外部内存中的数据复制到堆中再进行操作，从而提高了数据操作的效率。 </p>]]></content>
    
    
      
      
    <summary type="html">&lt;h1 id=&quot;JVM内存结构&quot;&gt;&lt;a href=&quot;#JVM内存结构&quot; class=&quot;headerlink&quot; title=&quot;JVM内存结构&quot;&gt;&lt;/a&gt;JVM内存结构&lt;/h1&gt;&lt;p&gt;JVM内存空间：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;程序计数器&lt;/li&gt;
&lt;li&gt;Java 虚拟机栈&lt;/li</summary>
      
    
    
    
    
    <category term="学习" scheme="https://ifwant.tech/tags/%E5%AD%A6%E4%B9%A0/"/>
    
    <category term="JAVA" scheme="https://ifwant.tech/tags/JAVA/"/>
    
    <category term="JVM" scheme="https://ifwant.tech/tags/JVM/"/>
    
  </entry>
  
</feed>
